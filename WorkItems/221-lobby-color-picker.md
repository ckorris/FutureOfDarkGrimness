# 221 — Color picker in lobby

**Status**: in-progress (implemented + tested; awaiting GUI hand-verification)
**Related**: `LobbyScreen.cs`, `GameGuiWiring.cs`, `PlayerColorOptions.cs`

## Goal
Let each player pick their own color in the lobby (currently no player/team color selection exists there). Scope includes deciding where that color then shows up in-game (model highlight rings, UI accents, etc. — check what color-coding, if any, already exists elsewhere before building) and how collisions between two players picking the same color are handled.

## Decisions
- 2026-07-15: **Classic-RTS dropdown, 8 colors** (user direction): Orange, Purple, Green, Yellow, Red, Blue, Teal, Pink. Palette order is load-bearing twice - it's the dropdown order AND the default-assignment order, so the first four preserve the pre-picker slot defaults exactly (P1 orange, P2 purple, P3 green, P4 yellow); an untouched lobby looks identical to before.
- **Where color lands in-game**: no new plumbing needed - `GameGuiWiring.Launch` already assigns every player color consumed by the renderer (models, objective pips, scoreboard, chat names). The picker just feeds that assignment instead of the old fixed by-index palette.
- **Collisions (revised same day per user)**: any color that is currently another row's - explicit pick OR assigned default - is disabled "(taken)" in the dropdown (`PlayerColorOptions.IsTakenByAnother`). No stealing: a pick can never change another player's color. The resolver keeps its deterministic bump-to-next-free as a fallback for stale/raced inputs only. Host-side, a pick for an explicitly-held color is ignored (first-committed-wins; the roster rebroadcast corrects the loser's UI); the host can't reserve *defaults* since the palette is app-side - the dropdown enforces that half.
- **Network sync (engine change authorized by user same day)**: `LobbyPlayerInfoSummary`/`LobbyPlayerInfoFull` gained `ColorIndex` (-1 = no pick; opaque int to the engine - the palette stays app-side), new `PlayerColorUpdateMessage` (client -> host, mirroring `ArmyListUpdateMessage`), `ILobbyViewModel.SetPlayerColor` (host: apply + rebroadcast any row; client: own row only, request to host), and `NetworkProtocol.Version` 2 -> 3 (message-registry change; old/new builds refuse to pair). Launch reads picks from the synced roster on every machine, so host and clients always agree.
- Dropdown edit permission mirrors Load Army (`CheckCanModifyPlayerIDInfo`): host edits every row (incl. bots), a networked client its own. Works in resume mode too (colors are cosmetic, assigned at launch, not part of the save).

## Notes
- 2026-07-15 (later still): **Defaults are now slot-anchored.** Explaining the race heads-up exposed a no-race quirk: defaults were "first free in palette order" recomputed from scratch, so P1 picking a FREE colour (e.g. Red) re-flowed P2's default from Purple to Orange - a visible colour change nobody asked for. `ResolveIndices` now anchors an unpicked slot to its own palette position (slot i prefers Options[i]) and only bumps to first-free when that colour is actually taken. One player's legal pick can no longer shift anyone else. The bump fallback still covers the (rare, self-healing) stale-pick race: a client pick that lands just as a roster change makes that colour someone's default gets accepted by the host (defaults are app-side, the host can't see them) and the defaulted player bumps deterministically - cosmetic, no shared colours, all machines agree. Verify: app 345/0, engine 1647/0, smoke exit 0.
- 2026-07-15 (later): **No-default-stealing + network sync landed** (engine change user-authorized). Engine: `ColorIndex` on the roster records, `PlayerColorUpdateMessage`, `SetPlayerColor` on both view models, protocol v3; `Tests/LobbyColorSyncTests` (5 cases through the REAL host+client over the LobbyJoinGateTests-style loopback: host pick reaches both rosters, client own-row pick round-trips, wrong-row client pick throws, held-color pick ignored, freed color repickable). App: `LobbyScreen` dropped its local dict - picks read from / write to the synced roster; dropdown disables any other row's *effective* color via new `IsTakenByAnother` (+1 test, and the old steal test reframed as the stale-input fallback). Verify: engine 1647/0, app 344/0 (via scratch-output build - the running app locks bin), headless smoke exit 0. Remaining: GUI hand-verify, incl. a live two-machine sync check (relates #188).
- 2026-07-15: **Implemented.** New `PlayerColorOptions` (palette + pure pick-resolution, unit-tested); `GameGuiWiring.Launch` takes an optional per-player pick lookup (the `--scenario` call site is untouched and keeps pure defaults); `LobbyScreen` grows a Color column (swatch + dropdown, taken-options disabled) and passes picks at launch; picks reset with each new lobby session. Tests: `PlayerColorOptionsTests` (7 cases - palette shape/order, defaults, pick-wins, steal-bumps-default, no default collisions, out-of-range pick, >8-player wrap). Verify: full build clean, engine 1642/0, app suite 343/0, headless smoke exit 0. Remaining: GUI hand-verification.
- 2026-07-15: Filed from user playtest feedback. No existing player-color mechanism found in `LobbyScreen.cs`.

## Outcome
