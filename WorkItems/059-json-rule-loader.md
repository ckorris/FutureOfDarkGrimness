# 059 — JSON rule-definition loader

**Status**: in-progress (serialization foundation landed on master; loader consumption side underway on branch `059-json-rule-loader`)
**Related**: #042 (this is the "JSON loader" sub-stream of the special-rules architecture, now broken out), #026 (army-load rule resolution), #027 (weapon-scope), #058 (the eventual all-STJ migration)

## Goal
The engine can load special-rule *definitions* from a JSON file at startup and register them into the rule resolver alongside `CoreRuleCatalog.All`, so new/army-specific rules can be authored as data without a rebuild. "Done" = a `.json` rules file is read into `SpecialRuleDefinition[]`, validated at load (capability/hook correctness via the existing `RuleValidator`), and registered at `FDGServer` rule-resolver construction; a malformed file is rejected with a clear message rather than throwing at dispatch time.

## Notes
- 2026-06-13: **Workstream 5 (networking consistency) DONE.** (engine `4320c1c`) **Reframed:** the work item's "registry fingerprint" idea assumed host AND client each build a resolver and could diverge — but only the host runs `FDGServer`/the resolver (client = `FDGGame_AsClient`, no resolver), so there's nothing to fingerprint. The real bug: the client sends its full army to the host via `ArmyListUpdateMessage`, which crosses the **Newtonsoft `TypeNameHandling.Auto`** transport, and the STJ-only embedded rule graph does NOT round-trip through Newtonsoft (chokes on computed `RequiredCapabilities` → `JsonSerializationException` on `<>z__ReadOnlyArray<Type>`; confirmed via diagnostic). A networked game with an embedded-rules client army would fail on the host's deserialize. **Fix (Option A, blob):** `ArmyListUpdateMessage` now carries the army as an STJ string (`FromArmy`/`DecodeArmy`, `RuleJson.Options`); Newtonsoft just moves an opaque string. **Sweep confirmed `ArmyListUpdateMessage` is the ONLY wire path carrying a full `ArmyListFile`** — data-sync types (`UnitData`/`ModelData`/`ArmyData`) `[JsonIgnore]` the `ResolvedRule`/`SpecialRuleDefinition` graph; `UnitData.SpecialRules` is the empty legacy `SpecialRule` marker; `ArmyListFile`/`LobbyPlayerInfoFull` aren't stored types. **Saves NOT affected** (corrected an earlier over-claim): `PlayerSlotInfo` doesn't carry the army, attached rules are `[JsonIgnore]`, resume's `ArmyListFile` is vestigial. Tests `Tests/ArmyListUpdateMessageTests.cs` (2 — Newtonsoft-transport round-trip preserves embedded rules; FromArmy/DecodeArmy equivalence). Suite 436/436, full build clean, headless smoke exit 0 (local path; networked lobby needs 2 processes, covered by the transport unit test). **Pre-existing gap noted, out of scope:** attached rules being `[JsonIgnore]` means a *resumed save* wouldn't carry its rules — a real but separate issue, not the #5 crash.
- 2026-06-13: **Workstream 4 (argument expressiveness) DONE.** (engine `edda97b`) Arg-arity validation at attach: a definition whose effect reads `ValueSource.Arg(n)` only attaches when the referencing entry supplies ≥ n+1 args; `ResolveForScope` skips-with-warning otherwise (instead of a mid-dispatch `IndexOutOfRangeException`). New `Rules/Dispatch/RuleArgumentArity.MaxReferencedArgIndex` reflects each effect's `ValueSource` properties (drift-proof for a future 7th arg-driven effect; effects are flat, `ValueSource` only ever a direct effect property). **Decision: skip-with-warning at the attach seam** (matches `ResolveForScope`'s existing unimplemented/misscoped branches), NOT workstream-3's hard-throw — arg-arity is a definition↔reference *binding* mismatch, the class that seam already tolerates; trivial to flip to throw if desired. Tests `Tests/RuleArgumentArityTests.cs` (4). Suite 434/434, headless smoke exit 0. New `RuleArgument` types (`Str`/`Float`/`Enum`) remain deferred (on-demand). **#4 fully closed.**
- 2026-06-13: **Workstream 4 reframed + first real authored rule landed.** (engine `fd6c134`) Decided **not** to add new `RuleArgument` types (`Str`/`Float`/`Enum`) — speculative, nothing needs them; per design-on-demand they're one-line additions when a rule first needs one (deferred, not dropped). Instead exercised the whole pipeline against a **committed on-disk file**: `FutureOfDarkGrimness/armies/example-with-rules.fdgarmy` (an embedded "Frenzied" Furious-clone a unit references by name) + author doc `FutureOfDarkGrimness/docs/rule-json-schema.md` (points at `[JsonDerivedType]` source as the authoritative tag list) + `Tests/ExampleArmyFileTests.cs` (loads from disk, proves embed→validate→register→resolve end-to-end; 3 tests). **Placement decision:** artifacts live INSIDE the submodule (not superproject root) so the engine submodule stays independently buildable/testable — the contract test mustn't reach across the repo boundary. Suite 430/430. **Still-open in #4:** arg-arity validation (catch `Arg(0)` referenced with no argument at attach time, instead of a raw `IndexOutOfRangeException` mid-dispatch) — a concrete, non-speculative hardening; not built this slice.
- 2026-06-13: **Workstream 3 (validate-at-load) DONE.** (engine `c33b3a2`) `RegisterEmbeddedDefinitions` now runs every embedded `SpecialRuleDefinition` through `RuleValidator` first, aggregates all `RuleViolation`s, and **hard-fails** the whole army load with a new `RuleValidationException` (Rules/Dispatch) before registering anything — validate-all-before-register, so the resolver is never half-populated. **Fork resolved: reject-the-whole-army (throw), not skip-with-warning** — a capability/hook mismatch is an authoring bug in shipped data, distinct from `ResolveForScope`'s tolerance of valid-but-unimplemented rules. Tests: `Tests/EmbeddedRuleValidationTests.cs` (clean rule registers; one bad rule among valid ones throws + registers nothing; multiple violations all reported). Suite 427/427, headless smoke exit 0. **Note:** validator only checks `Passive` hook entries — `ActivatedAbility` validation is still a gap (pre-existing, not introduced here). Next: workstream 4 (argument expressiveness) or 5 (registry-hash consistency).
- 2026-06-13: **RESUME POINT (pre-compaction).** On branch `059-json-rule-loader`, both repos, pushed. Latest: submodule `f3d2229`, superproject `7e2c370`. Workstreams 1 (serialize) + 2 (file→registry) DONE; **next is workstream 3, validate-at-load**. Build clean, suite 424/424, two live headless smokes green.
  - **Workstream 3 plan:** run each embedded `SpecialRuleDefinition` through the existing `RuleValidator` (+ `HookContextCatalog`) at load so a malformed authored rule (effect whose capability the firing hook can't provide, etc.) is caught with a message, not a mid-dispatch throw. Hook it into `ArmyListRuleResolution.RegisterEmbeddedDefinitions` (or a pre-pass in `FDGServer.CreateArmies`, right after `CoreRuleCatalog.CreateResolver()`). **Entry points to read first:** `Rules/Dispatch/RuleValidator.cs` and `HookContextCatalog` (API + how/whether `CoreRuleCatalog` rules are validated today — likely only in a test), and `RegisterEmbeddedDefinitions` in `SaveLoad/ArmyListRuleResolution.cs`.
  - **OPEN FORK for workstream 3 (decide first):** on a validation failure, **reject the whole army load (throw)** vs **skip the bad rule with a warning** (mirroring how `ResolveForScope` already skips unimplemented/misscoped rules so partial armies still load). Lean = skip-with-warning for consistency, but authored-data errors may warrant a hard fail. Surface to the user before building.
  - **Still-dangling:** two IDE-noise files remain uncommitted in the submodule working tree — `Rules/Definitions/RuleOperation.cs` (doc `cref` qualifications) and `Rules/Foundation/IHasDistance.cs` (stray `using FDG.Rules.Dispatch;`). Excluded from every commit; `git checkout` them or keep, user's call.
  - **Collaboration mode:** for #059 slices the user asked me to implement directly + add tests (vs the earlier serialization steps where the user wrote the production code and I added tests/guidance). Confirm preferred mode on resume.
- 2026-06-13: **Slice A + registration landed on branch.** (A, engine `7e0e3cb` + app `d22fb3b`) `.fdgarmy` migrated Newtonsoft→STJ: `SpecialRuleEntry` decorated as an STJ polymorphic hierarchy, `ArmyListFile.RuleDefinitions` added (embedded rules), all three app call sites moved to `RuleJson.Options`. Format is now camelCase + `kind` tags (old Newtonsoft `.fdgarmy` files won't load — accepted, templates not yet authored). (Registration, engine `f3d2229`) `RuleResolver.RegisterOrReplace` + `ArmyListRuleResolution.RegisterEmbeddedDefinitions`; `FDGServer` registers every army's embedded definitions (core-first, then override-by-name) before unit rule-name resolution. Verified: 424/424 tests + a live headless smoke loading an army whose unit references an embedded-only rule.
- 2026-06-13: **Loader-shape decisions.** Rules travel **per-army, embedded in the `.fdgarmy`** (not a global monolith, not a separate companion file) — ~20+ army templates each carry their own rules, edited independently of the C# core, so rules can't get orphaned from their army. Registration is **core-first, then each loaded army's rules override by name**; the game's single resolver is shared across all armies in play (merged-global; rare same-name/different-meaning collisions across two armies → last-loaded wins, accepted for now, namespacing only if it bites). **Serializer wrinkle:** `.fdgarmy` is currently Newtonsoft-serialized (`ArmyListFile`, a flat DTO) but the embedded rules use the STJ `kind`-schema — embedding cleanly means migrating the `.fdgarmy` (de)serialization to STJ (a small, self-contained app-side slice of #058, since `ArmyListFile` has no polymorphism of its own). **Deferred (workstream 8):** a standalone/global rules layer that overrides core *independent of which armies are at play* — for global balance patches / house rules applied regardless of army selection.
- 2026-06-13: **Serialization foundation complete and on master** (engine commits `5154c5a` + `af3f5bd`, superproject bumps `a9333e7`/`90ac830`). The full `SpecialRuleDefinition` tree round-trips through System.Text.Json as clean `kind`-tagged JSON:
  - Six closed sum types decorated with `[JsonPolymorphic]`/`[JsonDerivedType]`: `ValueSource`, `Condition`, `Effect`, `RerollCondition`, `TokenClearTrigger`, `DiceExpression`, `Cost` (~60 stable tags).
  - `Rules/Serialization/RuleJson.cs` — shared `JsonSerializerOptions` (camelCase, string enums, indented) + a type-info modifier that strips get-only computed properties (`RequiredCapabilities`, `DiceExpression.Sides`).
  - Tests per hierarchy + a **reflection guard** (`PolymorphicRuleRegistrationTests`) asserting registration completeness and ctor-parameter survival, so the hand-maintained tag list can't silently drift.
  - **Corpus proof:** `EveryCatalogRule_RoundTripsStructurally` round-trips all ~27 `CoreRuleCatalog` rules via JSON idempotence — the vocabulary covers every live `Condition`/`Effect`/`Cost`/nested shape.
  - Library decision (see #058): STJ for the new rules format (closed non-generic hierarchies = STJ's sweet spot); the message/save layer stays on Newtonsoft `TypeNameHandling.Auto` (open generic polymorphism = STJ's weak spot). Mixing is correct here, not laziness.

## Remaining workstreams
1. ~~Serialize rule definitions~~ — DONE (above).
2. ~~File → registry~~ — DONE. Rules embed in `.fdgarmy` (`ArmyListFile.RuleDefinitions`, STJ); `FDGServer` registers each army's definitions into the resolver via `RegisterEmbeddedDefinitions` (core-first, then **override by name** — `RuleResolver.RegisterOrReplace`) before unit rule-names resolve.
3. ~~Validate at load~~ — DONE. `RegisterEmbeddedDefinitions` validates every embedded definition via `RuleValidator` and hard-fails the army load with `RuleValidationException` on any capability/hook mismatch (validate-all-before-register). (`ActivatedAbility` entries still unvalidated — pre-existing validator gap.)
4. ~~Argument expressiveness~~ — DONE. New `RuleArgument` types (`Str`/`Float`/`Enum`) deliberately **deferred** (speculative; add on demand). Committed a real on-disk example army + author schema doc; arg-arity validated at attach (`RuleArgumentArity` + `ResolveForScope` skip-with-warning) so an `Arg(n)` shortfall is caught at load, not as a dispatch-time throw.
5. ~~Networking/save registry-hash consistency~~ — DONE (reframed). Only the host runs the resolver, so no fingerprint needed; the real issue was the client→host `ArmyListUpdateMessage` carrying the STJ-only rule graph across the Newtonsoft transport. Fixed by sending the army as an STJ blob (`ArmyListUpdateMessage.FromArmy`/`DecodeArmy`). Saves unaffected. **Separate pre-existing gap** (not #5): attached rules are `[JsonIgnore]` so a resumed save loses its rules — track if/when save-resume of in-progress games with rules is needed.
6. Reconcile the app-side `SpecialRuleRegistry` (army-builder UI) with the engine catalog + retire legacy `GetRealSpecialRulesFromArmyList`.
7. Army creator: scope-aware rule picker from the unified registry. Open fork: "creator" = (A) assign existing rules vs (B) author new rules tool.

## Decisions
- 2026-06-13: Broke the JSON loader out of #042 into its own number — #042 is a sprawling umbrella and the loader is a distinct, sizable chunk with its own branch. The serialization half shipped to master directly (incremental, low-risk); the consumption half (#2/#3) gets a feature branch.

## Outcome
_(open)_
