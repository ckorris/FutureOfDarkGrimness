# 162 — Tactical overlay (range / threat / eligibility visualization)

**Status**: in-progress
**Related**: #041, #045, #055 (targeting overlay lineage); design doc `docs/tactical-overlay-plan.md`

## Goal
A tactical overlay system for movement/target planning: opportunity fields (filled banded regions where a friendly model could stand and shoot a pinned enemy, LoS-shadowed + cover-hatched), threat frontiers (contour lines of what unactivated enemies can reach this round), and per-model instruments (eligibility pips, live distance readout, snap-to-range, live summary counts). Built from the feature spec + `docs/tactical-overlay-plan.md`. Done when the plan's acceptance scenarios (spec section 8) demonstrate in a GUI session and the fidelity sampler reports low, edge-localized mismatch. **Invariant (spec section 0): instruments call the real rules functions, never the field texture.** App-side only; no engine changes planned.

## Notes
- 2026-07-07: **Two in-game feedback fixes.** (1) Pin accents were *sticky per pin* (assigned lowest-free slot at pin time, kept forever) -> a lone pin could be amber if it was once the 2nd pin, confusing. Now **positional**: `PinnedTarget.Accent` is reindexed to the pin's slot on every pin/unpin, so a single pin is always the lead cyan and colours follow pin order. Removed `NextFreeAccent`. (2) Cover hatch was a single diagonal that sheared oddly against angled band edges; now a **crosshatch mesh** (thin 2-texel lines in BOTH diagonals, `HatchLineWidthTexels`) that reads uniformly at any edge angle. CPU + GPU shader updated in lockstep; harness re-verified (cover crosshatch's doubled line-edge count nudged cover-wall to 1.53%, so `MaxMismatchFraction` 1.5%->2.0% -- still 10x under a gross-divergence signal). App suite 105/0.
- 2026-07-07: **Field visual pass (playtest feedback):** field read as an opaque saturated recolor of the mat, and bands were indistinguishable by fill. Fixes: lead accent teal->cyan (B-dominant `#2AC0DC`, off the mat green); band fill dropped to 0.14 base / 0.05 boost / 0.30 cap (from 0.30/0.10/0.60) and lerped 0.40 toward white so it reads as a luminous cool wash, not a green tint; boundary rings widened to 2 texels bold at 0.85 so the three ranges read as concentric LINES (the line is the information, the fill is atmosphere). All new tunables in `TacticalOverlayConfig`, threaded to BOTH the CPU compositor and the GPU shader as uniforms (harness re-verified parity: 0.11-0.80%). Added a `-onmat.png` harness export (field composited over the mat + inch grid) since parity-on-black can't judge figure/ground. Residual: the fill keeps a faint green cast (inherent to translucency over a green mat) but is now subordinate to the rings. Awaiting user judgment on the real board (grid/vignette/tokens/scale differ from the flat-mat proxy).
- 2026-07-07: **Code-review pass over the perf rework INCOMPLETE:** `/code-review high` over ad9c4aa..HEAD -- 9 of 11 agents hit the session limit (resets ~1:40pm America/Chicago); 2 finished, 0 findings. NOT a clean bill; re-run after reset. GPU/CPU field divergence (the highest risk) is independently covered green by the pixel-diff harness.
- 2026-07-07: **Perf rework (H1-H4, autonomous session, user-authorized):** the ~1s pin freeze (per-texel real-LoS raycasts) replaced by a polar/GPU pipeline.
  - **H1 `PolarSightMap`:** per target model, a 2048-bucket angle->nearest-blocker depth map (two layers: Blocking / Cover-not-Blocking, matching `TerrainData.EvaluateSightLine` flag priority), filled by exact ray-primitive intersections at bucket centres via `ZoneExtensions.Primitives` (rect / circle / rotated rect -- the whole "silhouette adapter"). Texel classification = one atan2 + two compares per source; `ClassifyInto` is THE shared CPU classify loop (controller + harness). Error bound: (2pi/2048)*distance ~ 0.2" at 60". 8 new tests referee it against the REAL `EvaluateSightLine` (deterministic per-primitive + 20 randomized scenes, off-boundary mismatches = 0, distance-aware tolerance). Band labels: marching-squares scan -> ray-march along the approach line (D4 fallback adopted). Pin freeze on the CPU path: ~1s -> low tens of ms.
  - **H2 `FieldHarness` + `GpuFieldRenderer`:** `--field-harness` CLI mode runs a HIDDEN GL window (never touches the desktop), renders 4 synthetic scenes (bands / blocking / cover / mixed+rotated+model-bases) on the GPU, reads back, pixel-diffs against the CPU reference (the hand-verified renderer). Caught 2 real GPU bugs blind: default (non-separate) blend squaring the composite alpha (fixed: write quad with AlphaPremultiply onto transparent black = verbatim store, RT stays straight-alpha) and a mirrored+overwide hatch (fixed: image-row conversion + integer-division threshold uniform). Final: ALL SCENES PASS at 0.10-0.30% (disc/fan edge rasterization only). Artifacts (cpu/gpu/diff PNGs) export for eyeballing.
  - **H3 GPU field default (per user):** 3-pass GPU pipeline -- band discs opaque-overwrite ascending (GPU max-blend twin), per-source lit-region triangle fans straight off the polar maps (additive union, R=visible / G=clear), fullscreen composite shader (the codebase's first shader). Rebuild = a few hundred triangles + one quad, ~free; per-frame draw = one textured quad, no CPU texel work, no 2MB uploads. Toolbar "Field: GPU/CPU" flips to the reference CPU path in one click; auto-fallback if shader init fails. CPU masks now computed only when CPU-drawing or the sampler needs them (`_fieldMasksValid` gates the sampler's field channels).
  - **H4 ghost-anchored mode (default OFF, feel-check pending):** toolbar "Anchor: Target/Ghost". Ghost = "what can I hit from my pending position": per-model weapon-range bands from the ghosts, LoS from ghost positions, rebuilt EVERY frame (GPU makes it ~free; CPU fallback works but is per-frame heavy, warn throttled to 1/5s). Documented approximations: reach discs inflate by own base radius + default 28mm target radius (no specific target); blockers exclude the mover's team only (`BuildBlockers(movingUnit, movingUnit)`); band labels + sampler field channels suppressed in this mode. Pins keep chips/counts/pips/secondary contours.
  - Headless smoke flaked once with the KNOWN auto-advance cohesion float issue (CLAUDE.md validation gotchas; overlay code never runs headless); 3 consecutive clean re-runs. App suite 105/0 throughout.
- 2026-07-06: **Self-review pass (`/code-review high`, workflow-backed) + fixes.** 8 distinct findings; fixed: (1) threat-signature position quantization 0.5"->0.1" so a sub-0.5" enemy shift rebuilds the discs `GhostInThreat`/`SnapInputPoint` read; (3) ghost instruments (pips/tint/distance) skip models absent from the ghost snapshot instead of falling back to start position (removes frame-1/animation-hold glitch); (4) `HandleEnemyPinClick` reads the lock-guarded `ActiveRequest`, not raw `_request`; (5) `ComputeVisibility` filters blockers to Blocking/Cover pieces and skips the per-texel LoS pass entirely when none can block/cover; (6) removed the mover's centroid from the field signature (field is target-anchored -> no full rebuild per committed step; labels use build-time centroid, cosmetic); (7) per-frame blocker cache (one `BuildBlockers` per pin per frame, not two); (8) dropped the unused `IModel` param from `TryHandleEnemyClick`. **Not fixed (noted):** finding (2) rotated-rectangular-base hit-test uses world deltas -- this mirrors `TableHitTester`/the resolver's own select and is a pre-existing codebase-wide limitation (`RectangleBase` is documented "axis-aligned, no facing yet"; default bases are rotation-invariant circles). Section-0 review checkpoints all pass. App suite 97/0, headless exit 0.
- 2026-07-06: **P7 (multi-pin, idle isolation, perf) done.** Secondary pins: each non-focused pin draws a single longest-range boundary contour (marching-squares of its disc union) in its own accent, no fill (`BuildSecondaryContours` at field rebuild, `DrawSecondaryContours` above terrain). Tab-focus swap already invalidates the field, so focused-fill/secondary-contour roles swap correctly. Idle isolation: clicking an enemy while the frontier is shown (F, no move job) isolates its charge/shoot frontier bright over the dimmed aggregate (`_isolatedUnit` folded into the threat signature; `BuildIsolatedFrontier` via a shared scratch mask; click again / empty ground clears). Perf: all rebuilds stay signature-gated and pan/zoom/resize change only the Layout (never a signature), so no rebuild on view change; per-frame cost is texture/polyline draws + bounded probe calls for pips/counts/distance (~sub-ms). Rebuild-budget warnings on threat + field. **Deferred (flagged, not silently cut):** shoot-stage pip lighting -- the existing `GuiChooseRangedAttackResolver` already draws hover fire-lines/target rings there, so overlay pips would duplicate it and require wiring the controller into a second resolver; low value, left as a clean seam. App suite 97/0, headless exit 0. **Multi-pin + isolation visuals await GUI hand-verification.**
- 2026-07-06: **P6 (snapping) done.** `SnapInputPoint`: within 0.4" of a focused-pin band boundary snaps radially just inside (0.05" margin); within 0.4" of a threat frontier (when inside one) snaps just outside via nearest-point-on-polyline; band wins; Alt disables. **Deviation (flagged for review):** snaps the ghost's *input point* every frame rather than only the committed drop -- this routes the snapped position through the resolver's existing difficult/enemy-standoff/budget/overlap/cohesion clamps unchanged, so it can never propose an illegal move (honoring "snapped positions must still pass placement validation"); the tradeoff is the ghost is magnetic on approach, not only on release. Pure on-drop would need the resolver's inline clamp pipeline refactored into a reusable call. Single-mode only; group-mode snap deferred (noted). Band snap uses circle-approx radii (rectangle bases may be a texel off; the live pip shows the truth). App suite 97/0, headless exit 0. **Snap feel awaits GUI hand-verification.**
- 2026-07-06: **P5 (instruments) done.** `RulesProbe.EvaluatePip` (real base-edge 3D distance + `EvaluateSightLine`, best over target models -> Lit/Hatched/Dim). Resolver exposes per-frame `CommittedPositions`/`GhostPositions` snapshots (last waypoint vs live mouse ghost). Controller draws: per-ghost pip rows (one per pin x distinct ranged weapon, styled by state), red emphasis ring on ghosts inside a threat frontier, distance readout on the ghost nearest the cursor (base-edge to nearest focused-pin model, `<=0.5"` -> promoted labeled measurement line to the target). Panel gains live count rows (`Weapon vs Target: before->after`, eligible = non-Dim pip at committed vs ghost positions) and an incoming-threat row. All values probe-sourced, never the texture. **Deferred (noted, not silently cut):** threat row says "enemy charge/shoot reach" generically rather than naming the unit -- per-unit frontier attribution lands with P7 idle-isolation. App suite 97/0, headless exit 0. **Instruments visual awaits GUI hand-verification.**
- 2026-07-06: **P4 (LoS shadows + cover) done.** Chose exact-per-cell over approximate shadow volumes: `RulesProbe.BestSight` calls the engine's real `EvaluateSightLine` (best over target models, "sees any model"), `BuildBlockers` assembles terrain + `BuildModelBlockers` exactly as the shooting stages do. `ComputeVisibility` walks only band cells (early-out on Clear) filling `_shadowMask` (blocked -> texture hole, no fill) and `_coverMask` (shot through cover -> diagonal world-space hatch in `FieldCompositor.Compose`). Because the field uses the real function, the fidelity sampler's new `field-band`/`field-los`/`field-cover` channels report ~0 mismatch (cell-edge quantization only) -- they now catch integration bugs (forgotten blockers, coord mapping). Deviation from plan's shadow-volume approach noted; spec section 5 permits implementer's-choice CPU methods. App suite 97/0, headless exit 0. **LoS/cover visual + sampler run await GUI hand-verification (needs terrain on the table).**
- 2026-07-06: **P3 (pinning + bands-only field) done.** Pin state machine in the controller (pin/unpin/focus, accent assignment, job-scoped clear), enemy-click capture in `GuiDefineMovementResolver` (`HandleEnemyPinClick` -> `TryHandleEnemyClick`, guarded by a per-frame `_frameEnemyPinConsumed` so it never doubles as a waypoint in single OR group mode), chips row in the move panel (`DrawPanelSection`, click=focus / click-focused=unpin / x=unpin), Tab cycles focus, Esc clears pins (no move-cancel exists, plan C1), 150ms hover-preview (dimmed field, no chip). `FieldCompositor` (band mask -> bilinear world-space `Texture2D`, neighbour-texel boundary lines, vertical-flip baked in) drawn under terrain; `OpportunityFieldBuilder` (nested effective-range band discs, max-blend, WeaponRangeOverride-aware); band label pills placed at each band's boundary vertex nearest the moving centroid (D4). Field anchored on the target (stable during a drag; no per-frame rebuild). 1 new test (band nesting). App suite 97/0, headless exit 0. **Field/chips/labels visual awaits GUI hand-verification.**
- 2026-07-06: **P2 (fidelity sampler) done.** `FidelitySampler` (generic: iterate coarse 2" grid, compare per-channel claim-vs-truth predicates, tally + collect mismatch points) + `FieldMask.SampleAt` / `ThreatFrontierCache.SampleChargeInside/ShootInside`. F10 toggles; draws magenta X markers at mismatched samples + a top-right summary window (per-channel + overall mismatch %). For P2 it validates the threat masks against point-in-reach truth (rasterization/inflation fidelity); band/LoS/cover channels plug in at P3/P4 with no sampler change. 2 new tests (mismatch counting; mask-vs-disc-truth <3% edge noise). App suite 96/0, headless exit 0.
- 2026-07-06: **P1 (threat frontiers, live) done.** `FieldMask` (world-space disc rasterizer, max-blend), `MarchingSquares` (16-case boundary extraction, endpoint-keyed join, Douglas-Peucker), `ThreatFrontierCache` (charge/shoot disc-union masks -> solid/dashed polylines), `RulesProbe.ThreatReach/ModalBaseRadius`. Controller rebuilds on a per-frame signature poll (round + unactivated-enemy set + quantized positions) -- change-driven, not per-frame rebuild, and no cross-thread subscriptions. Contours drawn in the Raylib pass above terrain; charge solid, shoot dashed (phase carried across vertices). Auto-shown during a move job; `F` key + toolbar "Threat" button toggle idle inspection. **Engine change** (authorized): exposed `IGameProgress.UnactivatedUnits` (engine `82fb3cc`) so the client reads the replicated unactivated set robustly (vs a fragile app-side activation tracker). 6 new `TacticalOverlayGeometryTests` (disc rasterization, single/separated/overlapping-disc loops, radius accuracy, DP). App suite 94/0, engine 1140/0, headless exit 0. **Visual (on-screen contours) awaits GUI hand-verification.**
- 2026-07-06: Planning session produced `docs/tactical-overlay-plan.md` (grounded via 4-way codebase recon). Build order P0-P7; conflicts C1-C8 + approximations A1-A3 resolved in the plan.
- 2026-07-06: **P0 (scaffolding) done.** New module `FdgRaylib/Rendering/TacticalOverlay/` (`TacticalOverlayConfig`, `TacticalOverlayController` skeleton). Controller is a renderer-owned field, Attach/Detach in TransitionToGame/ExitGame, draw slots wired at spec draw-order positions (field under terrain, contours above terrain, instruments on the ImGui background list). `GuiResolverOverlay.MovementResolver` accessor added so the controller and move resolver can be wired together. Build clean, headless smoke exit 0, zero behavior change.

## Decisions
- **Reference player derived from live state, not plumbed.** "Enemy" = not on the reference player's team; reference player = active move request's `TargetPlayerID`, else `Progress.ActivatingUnit?.PlayerID`. Avoids threading a local-player id through OnGameLaunched/TransitionToGame and works for hotseat + network identically.
- **Two-pass draw split forced by the Raylib/ImGui layering** (plan C5): field texture + contour polylines draw in the Raylib canvas pass (so they can sit under terrain / under tokens); pips/labels/readouts stay on the ImGui background draw list (above tokens, under windows). One `UpdateLayout` per frame right after ComputeLayout feeds both passes.

## Outcome
(pending)
