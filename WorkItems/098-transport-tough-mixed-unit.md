# 098 — Transport capacity-by-Tough mixed-unit corner

**Status**: done 2026-06-25 — closed by adding the deferred mixed-case tests. **⚠ Correction (2026-06-25): the "already-correct" verdict was wrong on Hero space cost — a Hero occupies 1 space, not 3. Fixed under #035 (submodule `d736011`); these tests' hero-cost expectations were updated with it. See the closing note.** Engine suite 815/0, headless smoke exit 0.
**Related**: #035 (Transport), #006 (Hero — per-model Tough/hero), #093 (per-model special rules)
**Branch** (both repos): `098-transport-tough-mixed-unit` — submodule + superproject branched from master.

## Goal
Harden the Transport(X) capacity / ride-eligibility rules for a **mixed-Tough unit** — a single unit that contains models with different ride costs/caps (e.g. a Tough≤6 hero joined to Tough≤3 grunts, or a unit of mixed-Tough models). The rule: a normal model occupies 1 space; a **Hero within the Tough(6) ride cap occupies 1 space**; a non-Hero with Tough(2–3) occupies 3 spaces; models above their cap can't ride. *(Corrected 2026-06-25 — this line originally said a Hero occupies 3 spaces, which is what the implementation did; the rule's worked example shows a Tough(3) Hero is 1 space. See the closing note.)*

`TransportUtilities` already computes per-model space cost (`GetModelSpaceCost`) and per-model ride eligibility (`CanModelRide`), and `GetUnitSpaceCost` sums living models. The corner: `CanUnitEmbark`'s per-model hero detection for a *mixed* unit (which model is the hero, applying the hero cap to it and the non-hero cap to the rest), and confirming the summed 3-space accounting is right when a unit mixes 1-space and 3-space models. Add targeted unit tests for the mixed cases (hero+grunts within caps embark; a unit with one over-cap model can't; cost sums correctly).

"Done" = mixed-Tough units embark/are-rejected correctly and are covered by tests.

## Notes
- 2026-06-21: Opened from the #035 slice A deferral. The pure-Tough single-model and uniform cases are already unit-tested in `TransportUtilitiesTests`; this is the mixed-unit hero-detection corner that was explicitly deferred.
- **2026-06-25 — investigated; implementation already correct.** Audited `CanUnitEmbark` → `IsHeroModel` → `CanModelRide` / `GetUnitSpaceCost` for the mixed case. `IsHeroModel` resolves the hero of a *joined* unit by `HeroAttachment.HeroModelId` (so a host of grunts + one joined hero is handled per-model: hero→cap 6, grunts→cap 3), and falls back to the single-model+Hero-rule branch for a lone hero. Space cost sums `GetModelSpaceCost` per living model, so a mixed 1-space/3-space unit totals correctly. No defect found — the deferred work was purely the missing coverage.
- **2026-06-25 — added 6 mixed-case tests** to `TransportUtilitiesTests` (new `MakeHeroJoinedUnit` / `MakeSoloHeroUnit` helpers built via the real `UnitData.AttachHero` / Hero-rule paths): joined hero(T6)+grunts(T3) embarks (proves the hero gets cap-6 via detection, not cap-3); hero over cap (T7) rejected; one grunt over cap (T4) rejected though the hero is fine; a non-Hero Tough(6) in a plain multi-model unit rejected (can't borrow the hero cap); a solo Hero(T6) unit embarks; mixed space cost sums (hero 3 + two standard 1 = 5). Suite 805→811/0; headless smoke exit 0. No production code touched.
- **2026-06-25 — CORRECTION: the verdict above was wrong on Hero space cost.** Later hands-on testing (a Tough(3) Hero joined to a 5-model squad wouldn't fit a `Transport(6)`, when the rule says it should — 5 + 1 = 6) showed the "ride caps right, cost right" audit was only half right: the ride caps (6/3) were correct, but the **space cost** mis-read the ambiguous rule and charged Heroes 3 spaces like any other Tough≥2 model. The rule's worked example is decisive — "10 regular models with a Tough(3) Hero occupy 11 spaces" = 10 + **1**. Fixed under **#035** (2026-06-25 note there): `GetModelSpaceCost` is now hero-aware (Hero within cap = 1; non-Hero Tough(2–3) = 3). Two expectations in *these* tests changed with the fix — the mixed-unit sum (hero 3 + two standard = 5 → hero 1 + two standard = **3**) and the `MixedUnit_HeroWithinSix` comment (1+3+3 = 7, not 9) — plus 2 new Hero-cost tests. So this item's "no production change" outcome no longer holds: the production fix lives in #035; this item now records the corrected expectations. Engine suite **815/0**.

## Decisions
- **Closed as a coverage gap, not a fix.** ~~The mixed-Tough logic was already correct~~ **— superseded; see the 2026-06-25 correction note.** The ride-cap half was correct, but the Hero space cost was not; the fix landed under #035. Following the codebase's "no speculative code" stance (cf. #104), nothing was hardened defensively against states that can't occur (e.g. a multi-model unit carrying the Hero rule with no `HeroAttachment`). The lesson: the audit checked the implementation against the team's (mis-read) intended logic, not against the rule's worked example — which is what actually pinned down the ambiguous "occupy 3 spaces each" clause.

## Outcome
Done 2026-06-25. The deferred mixed-Tough cases are now covered by targeted tests exercising `CanUnitEmbark`'s per-model hero detection and summed space accounting on genuinely mixed units (joined hero + grunts, solo hero, over-cap rejections) — this closed the #035 slice-A test deferral. **Correction (same day):** the original "verified correct, no production change" conclusion was wrong on Hero *space cost* — a Hero occupies 1 space, not 3 (the rule's worked example). The production fix landed under #035 (`GetModelSpaceCost` made hero-aware), and these tests' hero-cost expectations were updated with it. Final suite 815/0.
