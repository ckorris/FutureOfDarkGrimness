# 006 — Hero joins unit + takes morale on behalf of unit

**Status**: in-progress
**Related**: #023 (Tough wound-priority — shares the "heroes last" allocation), #031 (Defense/unit rules umbrella, lists Hero), #042 (rule framework), #027 (weapon-scoped rules)

## Goal
Implement the **Hero** special rule (GF Core v3.5.1) as a first-class ability. A 1-model Hero unit joins one friendly multi-model unit (with no other Hero) and fights as part of it for the rest of the game. Slice 1 "done": the merge happens at army setup from an army-file declaration, and the four stat-divergence facets resolve correctly — wounds assigned to the hero **last**, morale tested at the **hero's Quality**, the hero saves at the **unit's** Defense until it is the sole survivor (then its **own**), and the hero's weapons fire at the **hero's** Quality. Eligibility enforced (hero ≤ Tough(6); target multi-model; target has no existing Hero).

Rule text (v3.5.1):
> **Hero:** Heroes with up to Tough(6) may deploy as part of one multi-model unit without another Hero. The hero may take morale tests on behalf of the unit, but must use the unit's Defense until all other models have been killed.
> **Tough(X):** …(heroes must be assigned wounds last, even if already wounded).

## Decisions
- **Representation = merge + hero metadata (Approach A).** The hero's `ModelData` is physically moved into the host `UnitData.Models`; the host carries a small `HeroAttachment { heroModelId, Quality, Defense }`. This reuses ALL existing per-unit machinery (targeting, LoS, save rolls at the unit Defense, coherency, movement, activation) unchanged — the combined unit is one `IUnit` for everything hard to touch. Only the stat-divergence facets need new code, and each hangs off a seam that already exists. Rejected: per-model Quality/Defense on `IModel` (large blast radius — every `unit.Quality`/`unit.Defense` read would need auditing) and linked/attached units (breaks the "enemy shoots one combined unit" model; many special cases). The user explicitly accepted "a little hard-coded engine treatment" here, like Tough/AP.
- **Join is declared in the army file** (not interactive at deployment, not auto-picked). Merge happens at army-setup in `FDGServer.CreateArmyDataFromArmyFile`. Chosen for determinism + testability; no army-builder UI churn for slice 1.
  - **Target reference**: add an authorable `string? Id` to `UnitFileEntry` and a `string? JoinsUnitId` on the hero's entry. `StableID` is a per-process counter (get-only, unserialized) so it can't be the authoring handle. The unit still carries the `Hero` core special rule (marker + Tough-cap eligibility); `JoinsUnitId` names the host.
- **Why the merge is engine code, not a hook Effect**: the merge needs cross-unit info (the host unit), which the single-unit creation hook (`Lifecycle_OnUnitCreated`, used by Tough) can't see. So the merge runs as explicit setup code (the way `UnitCreationRules.Apply` runs after construction), and `Hero` is added to `CoreRuleCatalog` mainly as a recognized marker carrying eligibility.
- **Attacks batch by weapon type** (`CombatActionContext.GetTypeSortedWeapons` via `WeaponComparer`), and `AttackBeatPositions.FiringModels(unit, weaponType)` already maps a batch back to its owning models. So the hero's distinct weapon is already its own hit-roll batch — "hero fires at own Quality" is one conditional in `DetermineHitRollNeededStage`, not a refactor.

## Decisions (cont.)
- 2026-06-15 — **Per-model divergence is the real requirement, not just hero stats.** Realized a joined hero needs different *rules* too (Fast, Furious, Good-Shot…), not only different Quality/Defense. Re-evaluated the merge and confirmed it's still correct: the hero model must differ from its unit-mates regardless of merge-vs-attach, and the merge keeps all the hard per-unit machinery (targeting, coherency, saves, activation) free. **Key finding:** weapon-scoped rules *already* work post-merge — `WeaponComparer` includes rule attachments in equality, so a rule-bearing hero weapon stays its own attack batch (so "Good Shot" authored as a weapon rule already fires for the hero only). The gap is **unit-scoped** rules the hero carries (slice A discards them). Adopt **unit-level default + per-model overrides** (the faithful tabletop model): `ModelData` gains a rule list + optional Quality/Defense overrides; dispatch unions unit+model rules where the hook knows the model (hit→firing model, save→defending model, wound→target model); at merge the hero's unit-scoped rules move onto the hero model. Rejected: **linked/attached hero** (hero-only hack — special-cases targeting/coherency/wound-carry/activation, doesn't generalize to champions/mixed units) and **pause-to-build-a-standalone-per-model-framework** (larger upfront, Hero delayed). **Scope now:** per-model stats + per-model *combat* rules; defer per-model *movement* (unit-level budget) and hero *activated abilities* (activation layer). `HeroStatRules` (already stubbed) stays the stat seam, now reading model-override-then-unit.

## Deferred (recorded — not silently cut)
- **Same-weapon pooling collision**: if the hero carries a weapon *identical* (by `WeaponComparer`) to the rank-and-file, `GetTypeSortedWeapons` pools them into one batch and the hero's Quality can't be peeled out without splitting the batch. Rare (heroes almost always have unique gear). Slice-1 behavior: that pooled batch fires at the unit's Quality. Fix later by splitting the hero out of any shared batch in the attack-builder.
- **Per-model MOVEMENT rules on the hero** (Fast/Slow): the movement budget is unit-level, so a hero-only move bonus needs per-model budgets + a coherency interaction — a movement-subsystem change. Deferred to the general per-model rules epic (see [[../WorkItemsList|#093]]).
- **Hero ACTIVATED abilities while joined** (Caster/Vanguard/Martial Prowess): need activation-layer work to offer/resolve a single model's ability inside a host unit. Deferred to #093.
- **Same-weapon pooling collision**: if the hero's weapon is identical to a grunt's *by `WeaponComparer` including rules* (i.e. same stats AND same rules), they pool and the hero's per-model attack-Quality can't be peeled out. Rare; pooled batch fires at unit Quality.
- **Force-org Hero cap** (1 hero / 500pts) is #003, not here.

NOTE: "hero's own special rules while joined" is no longer wholly deferred — **weapon-scoped rules already work** (the hero's weapon stays its own batch), and **per-model COMBAT rules** are now in scope (slice F). Only movement + activated abilities are deferred (above).

## Plan (revised 2026-06-15 — per-model carriage; each slice: implement → integration test mirroring nearest *RuleIntegrationTests → verify → commit → ledger)
- **A. Merge primitive + eligibility.** ✓ DONE (engine `a17b535`). Army-file `Id`/`JoinsUnitId`; `HeroAttachment`; merge in setup; `Hero` marker; eligibility; hero-aware Tough. Plus army-builder join picker (app `6364af9`).
- **B. Wound-last ordering.** Hero assigned wounds last in `AssignWoundsResults` (ordered last + guard rejecting the hero while any rank-and-file has room). Hero-specific. *(in progress — `_heroModelId` field added, uncommitted)*
- **C. Morale on behalf.** Unit tests morale at the living hero's Quality via `HeroStatRules.GetMoraleQuality`, wired into `DetermineMoraleSaveNeededStage` + wound-driven morale. Hero-specific.
- **D. Last-model Defense.** `DetermineSaveRollsNeededStage` reads `HeroStatRules.GetSaveDefense` — hero's own Defense only when it is the sole survivor. Hero-specific.
- **E. Per-model attack Quality.** `DetermineHitRollNeededStage` reads `HeroStatRules.GetAttackQuality` — the hero's weapon batch fires at the hero's Quality (model override). Generic per-model stat.
- **F. Per-model rule carriage (NEW).** ✓ DONE (engine, this branch). `IModel.RuleDefinitions` (mirror `IUnit`/`IWeapon`; **no `ERuleScope.Model`** — scope is an army-load authoring concept the merge bypasses, so per-model is purely "lives on a model's list"); a model-aware `RuleEvaluator.EvaluateAll` overload (opt-in, option A) that unions the relevant model's rules, wired into the two **hit** stages (`DetermineHitRollNeededStage` = `Shooting_OnHitRollModifier`, `RollToHitStage` = `Shooting_OnHitRollComplete`); merge moves the hero's unit-scoped rules onto the hero model. Relevant models = the weapon batch's living owners, contributing rules **only when sole owner** (same conservative collision handling as the slice-E stat seam — extract the owner-predicate so stats + rules share it). Lands per-model COMBAT rules **Furious / Relentless / Thrust** on the hero only. **Counter dropped** (already `ERuleScope.Weapon` → rides the weapon, already works post-merge). **Impact(X) deferred** (`Melee_OnChargeContact` — not weapon-batched, no firing-model concept) → #093. Weapon-scoped rules already work (no change). Deferred corners (movement, abilities, Impact, defensive per-model rules) → #093.

Per-model stats (B–E) use the `HeroStatRules` seam, reading model-override-then-unit. The general "are rules unit-wide or per-model?" reckoning is its own epic — see #093.

## Notes
- 2026-06-19: **Fix (found in hands-on testing): the wound dialog now grays a joined hero until the rest of the unit is dead.** Slice B already blocked the hero in `TryAddWounds`, but the GUI resolver computed assignability from capacity alone (`remaining > 0`), so the hero's button stayed enabled and a click silently no-opped. Added a non-mutating `AssignWoundsResults.CanAssignWoundTo(PendingWounds)` (capacity AND the hero-last ordering, the same conditions `TryAddWounds` rejects on) and routed `GuiAssignWoundsResolver`'s button-enable, map-dimming, and hover label through it — the same mechanism that already grays full/dead/locked-Tough models. Engine tests added to `HeroWoundAssignmentTests` (hero un-assignable while grunts have room; assignable once they're full; full model rejected). Suite 593/0; headless exit 0.
- 2026-06-19: **Slice F done** (per-model combat-rule carriage) — the last build slice. Added `IModel.RuleDefinitions` (+ `ModelData` impl + `AttachRuleDefinition`, `[JsonIgnore]` like the unit/weapon lists); `HeroJoinResolver` now moves the hero's unit-scoped rules onto the hero **model** at merge (Hero marker excluded — structural, no hooks); `RuleEvaluator` gained an **opt-in** model-aware `EvaluateAll` overload that unions a participant's named model(s)' rules (bearer stays the unit, so dedup + logging unchanged); wired into both hit stages (`DetermineHitRollNeededStage`, `RollToHitStage`), passing the weapon batch's **sole living owner** via `HeroStatRules.WeaponBatchRuleOwners` (shared owner-predicate with the slice-E attack-Quality seam, so the same-weapon-pooling collision behaves identically — pooled batch keeps unit behavior). Lands **Furious / Relentless / Thrust** on the hero alone. Counter already worked (weapon-scoped); Impact + defensive per-model rules deferred → #093. Test: `HeroPerModelRuleIntegrationTests` (4 cases via the real `RollToHitStage`: hero-batch fires, rank-and-file batch doesn't, dead hero contributes nothing, shared-weapon collision suppressed). Verify: engine suite **590 passed / 0 skipped / 0 failed**, full build clean, headless 4-round smoke exit 0. **All build slices A–F done.** Remaining for #006: GUI hand-verification of the army-builder join picker (can't be driven headless) + the deferred corners (→ #093); item stays in-progress pending the user's hands-on test.
- 2026-06-19: **Branched + merged master; sharpened slice F from a rule-system read.** Safety branch `006-hero-slice-f` cut off `006-hero` (both repos) before hand-testing A–E. Merged `origin/master` in (submodule `3b50a5a`, super `1e495a3`); resolved two conflicts — `AssignWoundsResults` (hero wounds-last composes cleanly with master's #023 Tough pre-assignment: pre-assign walks the same hero-last order, and the `TryAddWounds` hero guard defers the hero even when already wounded — matches the rulebook) and `MoraleUtilities` (hero morale Quality applied on master's new async `TakeMoraleTest`). Suite 586/0, full build clean, headless exit 0. **Read the existing rule corpus before building slice F** and corrected the plan: `ERuleScope` is `Unit`/`Weapon` only and is an *army-load authoring* concept (the merge is engine code that bypasses it), so **`ERuleScope.Model` is unnecessary** — a rule is per-model purely by living on a model's list. **Counter is already `ERuleScope.Weapon`** (rides the weapon, already survives the merge) so it's dropped. **Impact(X) fires on `Melee_OnChargeContact`** (no weapon batch / firing-model) so it can't use the batch-owner trick → deferred to #093. Confirmed `RuleDefinitions` is `[JsonIgnore]`/re-attached at army-load only, so per-model rules inherit the exact save/load lifecycle of unit rules (no new burden). Clean slice-F deliverable = **Furious / Relentless / Thrust** on the hero via `IModel.RuleDefinitions` + model-aware hit dispatch keyed on weapon-batch sole-owner. Chose **option A** (opt-in dispatch) with the user.
- 2026-06-15: **Slice E done** (per-model attack Quality) — completes the stat facets. `HeroStatRules.GetAttackQuality` resolves the batch's living owners via `WeaponComparer`; if owned by the hero alone it returns the hero's Quality, else unit Quality (the deferred same-weapon-collision case stays unit Quality). Wired into `DetermineHitRollNeededStage` (shared shoot/melee hit path). Un-ignored the slice-E test + added the collision test. **All pending tests now green: suite 577 passed / 0 skipped / 0 failed; headless exit 0.** Slices A–E done (merge + wound-last + morale-Q + last-model-Defense + attack-Q). Remaining: slice F (per-model rule carriage — `ModelData.RuleDefinitions` + model-aware dispatch + merge moves hero unit-rules to the model).
- 2026-06-15: **Slice D done** (last-model Defense). `HeroStatRules.GetSaveDefense` returns the hero's own Defense only when the hero is the sole living model, else the unit's; wired into `DetermineSaveRollsNeededStage` (the shared save-needed computation). Non-hero units unchanged. Un-ignored both slice-D tests. Verify: suite 575 passed / 1 skipped (E) / 0 failed; headless exit 0. Next: slice E (per-model attack Quality) — the last one; then slice F (per-model rule carriage).
- 2026-06-15: **Slice C done** (morale on behalf). `HeroStatRules.GetMoraleQuality` returns a living joined hero's Quality, else the unit's; wired into both morale paths — melee (`DetermineMoraleSaveNeededStage`, loser's roll-needed) and wound-driven (`MoraleUtilities.ResolveWoundDrivenMorale`). Dead hero / no hero → unit Quality (passthrough; non-hero units unchanged). Un-ignored the slice-C test + added dead-hero-reverts and no-hero cases. Verify: suite 573 passed / 3 skipped (D×2, E) / 0 failed; headless exit 0. Next: slice D (last-model Defense).
- 2026-06-15: **Slice B done** (engine `1b11b31`). `AssignWoundsResults` orders a joined hero last and `TryAddWounds` rejects the hero while any rank-and-file model has room (AutoFill spares the hero; player-choice can't pick it early). `_heroModelId` serialized so the guard survives a mid-assignment round-trip; non-hero units unaffected. Added `HeroStatRules` (stubbed to pre-#006 behavior) as the C/D/E seam, with red `[Ignore]`-pending tests (`HeroStatRulesTests`) — verified genuinely red (un-ignored slice-C test failed Expected 2 / But was 4) then re-ignored. Slice-A coverage added: `HeroAttachment` JSON round-trip + hero identity after a grunt dies. Verify: suite 570 passed / 4 skipped / 0 failed; headless 4-round smoke exit 0. Next: slice C (morale on behalf) — fill `HeroStatRules.GetMoraleQuality`, wire `DetermineMoraleSaveNeededStage` + wound-driven morale, un-ignore its test.
- 2026-06-15: **Army-builder Hero support** (app-side, superproject only — no engine change). `ArmyBuilderScreen` now exposes the join wiring: when a unit carries the `Hero` rule (added via the normal picker, which lists Hero since it's in `CoreRuleCatalog.All`), a **"Joins unit"** combo appears under Special Rules listing eligible hosts (other multi-model, non-Hero units in this army) + "(none – deploys solo)". `Id`/`JoinsUnitId` stay invisible: picking a host auto-generates the host's `Id` (GUID) on demand and sets the hero's `JoinsUnitId`; the link is by Id so it survives host renames. Inline warnings mirror engine eligibility: hero not single-model, hero Tough>6 (`HeroJoinResolver.MaxHeroToughForJoin`), deleted target, now-ineligible target. Host units show a "Joined by: …" breadcrumb. UX forks decided with the user: surface-on-Hero-rule (not a separate toggle); filtered list + warnings. Build clean (0 errors). **Needs GUI hand-verification** (Raylib window can't be driven headless here). Engine merge itself already covered by `HeroRuleIntegrationTests`.
- 2026-06-15: **Slice A done** (engine `a17b535`, bump `7560eca`). Join primitive + eligibility shipped. `HeroJoinResolver.Apply` runs per-army at setup (`FDGServer.CreateArmyDataFromArmyFile`), merging a single-model Hero into the unit whose `UnitFileEntry.Id` matches the hero's `JoinsUnitId`; host gains the hero's model binding + a `HeroAttachment {HeroModelId, Quality, Defense, HeroWounds}`; the hero's standalone `UnitData` is never registered (so it's absent from deployment/activation). Eligibility: hero single-model, Tough≤6, host multi-model, host has no other Hero — rejected joins deploy solo with a `Debug.WriteLine` reason. **Tough ordering solved** by capturing the hero's wound count at merge (its standalone unit never hits the creation-rules loop) and making `UnitCreationRules.Apply` hero-aware: host Tough skips the hero model; the hero model is set from `HeroAttachment.HeroWounds`. `AttachHero` deliberately does NOT subscribe model-wound events (matches the fresh-game ctor; save/load `RewireModelWoundSubscriptions` now covers the merged binding). `Hero` added to `CoreRuleCatalog` as a marker. Verify: engine suite 564/0, full build clean, headless 4-round smoke exit 0. Tests: `HeroRuleIntegrationTests` (merge, own-Tough-kept, Tough-less-under-Tough-host, Tough>6 reject, single-model-host reject, second-hero reject, unknown-target solo, no-target solo). Next: slice B (wound-last ordering).
- 2026-06-15: Item opened. Deep analysis of rule + engine done. Forks resolved with the user: representation = merge+metadata; join = army-file declaration; slice-1 scope = join + all 4 stat facets + eligibility, deferring the same-weapon collision and hero's-own-rules-while-joined. Starting sub-slice A.
