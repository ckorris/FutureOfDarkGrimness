# Work Items — Archive

Completed and closed work items, moved verbatim out of `WorkItemsList.md` (2026-07-08) to keep the
always-read index lean. Numbers are permanent and never reused — the per-clone pre-push hook checks
this file as well as the index when blocking duplicate numbers.

- [x] 179 — (low) `PresentationRelayer` reaches into `PlayerSlotManager._playerSlots` instead of the public property. **Closed 2026-08-31**: field made private readonly; PresentationRelayer, TempVisualRelayer and FDGServer (the same reach, unfiled) all routed through `PlayerSlots`. Engine `2e52cca`.

- [x] 081 — Per-access allocations: `ModelData.MeshProvider`/`MaterialProvider` construct providers per get; `UnitData.Models` materializes a new list per access. **Closed 2026-08-31**: the TEMP default providers are static singletons (identical for every model; nothing currently reads them anyway); `Models` keeps a fresh list per access - a cache would serve stale objects after #190's networked re-Sets - but drops the LINQ chain for one pre-sized allocation. Engine `19d5134`.

- [x] 380 — GDF Melee Slayer gates on isMelee but the book text says "when this model charges" - shipped def over-granted on strike-back/non-charge melee; found by #375's variant review. **Closed 2026-08-31**: condition isMelee -> isCharging in `GdfRuleSupplement.json` (matching the AoF supplement's always-correct shape), 11 GDF books re-embedded via `--apply-rules` (two-line diffs each), `MeleeSlayerShippedDataTests` pins both supplements + an embedded copy. ([WorkItems/380](WorkItems/380-melee-slayer-charge-gate.md))

- [x] 392 — FdgLab parallel bench: concurrent games coupled through the shared per-matchup ArmyListFile. **Closed 2026-08-31, filed, root-caused and fixed the same day.** `UnitData`'s constructor sorted each entry's Weapons list IN PLACE on the caller's file; racing games captured different weapon orders (resolution order, dice consumption), so outcomes depended on which games overlapped - 7/16 flips between GC modes in the repro, each config eerily self-reproducing, dop-1 clean. Fixed engine-side (sort a copy, whole-file byte-identity pin `ArmyFileImmutabilityTests`, engine `16977e7`) and harness-side (fresh army per game in `Benchmark.cs`); ServerGarbageCollection + dop 24 adopted at the same re-baseline boundary after proving config-independence (identical hashes across GC mode / dop / log capture). Benches run ~3x faster; hashes recorded before this date are not comparable to post-#392 runs. Possibly the #210 segfault family's real cause (List.Sort racing an enumerator). ([WorkItems/392](392-parallel-bench-crossgame-coupling.md))

- [x] 388 - Human slots get a starter army too, extending #372 past the bots. **Closed 2026-08-30, filed, built and hand-verified the same day.** A human row arrived with an empty Army cell (the host silently substituted the 100-pt "Test Army" stub at launch) while every bot got a real list. The seeding pass now serves every row THIS machine may write, decided per row by `CheckCanModifyPlayerIDInfo` alone - the same gate Load Army and Random Army use, so the host serves its own row + its local humans + the bots, a client serves only its own row, and nobody rolls for a remote player (owner's call: clients seed themselves, since the host cannot write a Network row and the armies folder is the client's own). A human keeps an army it already holds (`IsAssigned` is a real signal for a human row and never for a bot's, which always carries the stub), while the served-set still guards the round trip during which a client's own update is in flight. Chris then played it and reported "Alien Hives three times in a row", which was not luck: `Rank`'s `ThenBy(Path)` tiebreak decided outright wherever armies tie on points - five bundled lists sit at exactly 1000 - and a fresh lobby drops the rotation, so every lobby opened on the same file (Alien Hives at 1000, Blessed Sisters at 2000, Battle Brothers at 3000). Fixed with `BandPercentUnderLimit` (5%): armies within 5% of the limit are interchangeable and one is taken at RANDOM, below the band closest-first still stands, and the band is capped at the limit as well as floored so the all-over-limit last-resort pool does not start rolling 5000-pt lists. Probed on the real folder: 12 fresh lobbies now give 4 / 6 / 8 distinct armies at 1000 / 2000 / 3000, all in band. A second bug fell out of the same report and was fixed with it - rows seeded in the SAME frame shared one stale roster snapshot, so `inUseByOthers` missed the picks before them and two rows could land on the same army (unreachable before #388, when only bots were seeded and they are added one at a time); `AssignRandomArmy` reads `PlayerInfos` itself now. Four `BotArmyPickerTests` had pinned the deterministic opening army - the behaviour the report called out - and now assert band membership, with a seeded `Random` injectable for the cases needing an exact answer. App suite 1554 -> 1556, engine 3070 green. ([WorkItems/388](388-human-starter-armies.md)) — superproject `abce25e`, `a3bcd90`

- [x] 384 — Two lobby house-rule toggles, both default off: See-Through Allies (on = the old any-same-team-model-never-blocks-LoS; off = official rules, only own unit + target transparent, AI lane-aware) and Unlimited Split Fire (lifts the 2-unit shoot-action cap). GUI hand-verified 2026-08-23. ([WorkItems/384](384-house-rule-lobby-options.md))
- [x] 368 — Shooting weapon rows now print the datasheet "3x" copy-count prefix the melee menu already uses (both front ends, off `WeaponOption.CopiesRemaining`), so a five-rifle volley and a single rifle no longer read identically. GUI hand-verified 2026-08-23 (seen in the #377 verification games). ([WorkItems/368](368-shooting-weapon-copy-count.md))
- [x] 243 — Objective placement mode (Auto-Placed / Player-Placed) mirroring terrain modes; Auto uses the solo-rules AI algorithm via a shared `ObjectiveAutoPlacer`, debug options sort last in Release, roll-off skipped in Auto. GUI hand-verified 2026-08-23. ([WorkItems/243](243-objective-placement-mode.md))
- [x] 033 — Caster(X) subsystem: token economy, cast loop, targeting, boost/assists, damage + buff effects. GUI hand-verified 2026-08-23 via the #377 spell-verification games (spell menu with token counts, boosts, target picks, rolls, effects, all end-to-end). ([WorkItems/033](033-caster.md))
- [x] 387 — Modified weapon range indicator: the eligibility check's effective range now rides the request (`WeaponTargetStats.EffectiveRangeInches`) and one composer (`RangeDeltaText`) renders it everywhere - shoot-panel target rows ("range 30\" (+6\")") + detail pane, out-of-range shortening explained, CLI rows, and pinned-target overlay band labels. Filed and implemented 2026-08-23 (user request during the #377 verify game), GUI hand-verified same day. ([WorkItems/387](387-modified-range-indicator.md)) — engine `c38a973`
- [x] 377 — All 240 AoF spells live as data (census 0 dead spell refs both systems, parity 240/240 vs print, corpus cast sweep 240 AoF + 282 GDF clean); fixed 21 silent-no-op spells in SHIPPED GDF books along the way (statModifier import shape, 8 core phrase rules, range-mark peek seam). GUI hand-verified 2026-08-23 via Scenarios/377-spell-verify.json: cast-debuff token, Shred mark claim, range grant + range mark all confirmed in play. ([WorkItems/377](377-aof-spell-authoring.md)) — engine `082fcc8`..`f2ba812`; superproject `fe51b50`..`83f0397`
- [x] 376 — AoF rules pt.2: new engine primitives. **Closed 2026-08-22, opened and closed the same session** (on top of the same-day #375 close). The AoF mechanics that could not be authored as data got real primitives, one vertical slice at a time, all owner-forked before building: **S1** IHasTerrain on `MoveActionDeclaredContext` + `SaveRollCompleteContext` (reflection-derived validator needed no registry; terrain threaded through EVERY AI movement-budget query - MacroActionGenerator, TacticianPlanner's #363 snapshot, the activation/placement/ranged resolvers, embark/disembark gates - so planner and resolver agree) making Grounded Speed / Grounded Protection (+Aura) plain data, with the per-action `actionTypeIs` gate against the three-action-fold triple-count; **S2** the Shaken-recovery threshold fold - `clearTokenOnRoll` emits a sink op, `TokenClearRollSink` keeps best (lowest) per token type, `TokenClearRolls` makes ONE decisive roll, so Vale Oath (4+) + Vale Oath Boost (authored at the full band 3, RerollSink doctrine) is one roll at 3+ never two chances (owner-ruled facet: distinct recovery rules fold too); **S3** Ravage Aura as DATA, zero engine change - the stage's threshold-group SUM makes a Unit-scoped 1-die-per-living-model def arithmetically identical to Ravage(X+1), proven by the additive test (Ravage(2)x2 + aura = 6 dice); **S4** a failure arm on `grantTokenOnRoll` (one die two outcomes, MoraleTestThen pattern) making Reckless Piercing data - opt-in YesNo at activation start, round-end boon / Subject-seat exposed tokens (the corpus' first hostile Subject-seat save modifier); **S5** the Bloodthirsty Fighter seam - defender block-roll 1s earn a REAL follow-up batch with the same weapon (`AddBonusAttack` effect + `BonusAttackSink` + `AttackCountOverride`/`IsBonusAttack` on the metadata + `ResolveBonusMeleeAttacksStage` inside the swing chain, strike-backs free), scripted-dice tests proving exact roll-count termination / no chaining / dead-defender lapse, CombatMath first-order pricing with an engine/AI pin. Census dead **34 -> 14 = Retreating Strike only**, which the owner deferred at fork sign-off and split out to **#381** at close. Engine 2969 -> 2986 green, app 1280 -> 1315, GDF books byte-identical at every rebake, headless + forced-charge melee smokes exit 0. ([WorkItems/376](376-aof-rule-engine-primitives.md)) — engine `e74517a`..`f22295c`; superproject `d29f2f0`..`0257e0e`

- [x] 375 — AoF rules pt.1: data authoring. **Closed 2026-08-22, opened and closed the same session.** Every AoF rule name expressible in the existing vocabulary resolves to a firing definition: census over a real 40-book AoF corpus (fetched off the Army Forge API into the local-only reference folder, imported via --import-opr, `--rule-coverage` unchanged) went **7,815 refs / 2,411 raw dead -> 495 after the GDF bake -> 34**, where the final 34 are exactly the 10 names handed to #376 with mechanism-level reasons (two IHasTerrain context gaps, the clearTokenOnRoll double-roll, the four filed primitives + wrappers) - zero dead refs attributable to authorable rules, 0 scope-mismatch throughout. 75 defs authored in the new `AofRuleSupplement.json` (rename clones per #196's data-not-aliases ruling, wrapper composites, Ethereal riding Effect.Teleport's effect-TYPE stage routing, Great Sergeant as two addExtraHit entries) + 2 AoF-global redefinitions of GDF-divergent same-names (Difficult Terrain Debuff LoS, Fortified Growth marker economy) + 9 per-book override files (`AofBookOverrides/<Book>.json`, baked as that book's LAST supplement - zero new code, the multi-supplement CLI from slice A) covering the 8 within-AoF variant rules the review found (the outlier is almost always a Havoc Warriors sub-book). Infra: later-wins multi-file supplement merge in `--import-opr`/`--apply-rules`/`--validate-rules`, `RuleSupplementSet`, `Defines()` over both files, lint fixture walking both supplements + every override file. Owner forks signed off before building: separate AoF supplement + local-import census + cloned defs; per-book override files; #380 filed separately for the GDF Melee Slayer isMelee over-grant the variant review exposed. Verified with the full #196 loop before each of 14 commits (engine untouched, 2969 green throughout; app suite 1182 -> 1280) and closed on complete headless games with compiled Lust Disciples and Ghostly Undead armies - zero rule drops. #378 pickups recorded: pass a book's override file at bake, the Disciples book-name collision, the census regression pin. ([WorkItems/375](375-aof-rule-data-authoring.md)) — superproject `b90bebb`..`7fea4ff`

- [x] 372 - Bot starter armies: a new bot arrived with the hard-coded 100-pt "Test Army" stub, which is never what anyone wants to play against. **Closed 2026-08-12, hand-verified in the running app.** `ArmyCatalog` streams the `armies/` folder with a `Utf8JsonReader` that `Skip()`s every top-level property it doesn't need (the bundled lists are Forge-built and carry a full book snapshot each, ~12 MB across the folder), on a background task, cached process-wide; `IndexedPointsMatchAFullDeserializeOfEveryShippedArmy` pins the shortcut against `ArmyListFile.TotalPoints` on every shipped army so it cannot drift (`UnattributedPoints` was the easy thing to miss). `BotArmyPicker` is pure ranking + rotation: closest to the limit first, skip what other players hold (identity is `name|faction|points`, because a remote client's army arrives as an `ArmyListSummary` with no path), never repeat until the slot has seen them all. App-side on purpose - the armies FOLDER is an app concept and the engine has no business reading the user's disk. Chris then played it and reported two faults, both fixed and pinned: re-rolling walked off the end of the legal armies into the over-limit ones (over-limit is now a separate last-resort pool, and exhausting the legal ones restarts that cycle), and the rotation did not reset when the points limit moved (both "legal" and "closest" are answers to that number). The two tests that had encoded the old walk-the-whole-catalog behaviour were rewritten - that behaviour was the bug. "New Army" became "Random Army" and moved onto every player row, gated by `CheckCanModifyPlayerIDInfo` alone - the same gate Load Army uses, so a client can roll only its own row and never a bot's, with no second permission rule to keep in sync. ([WorkItems/372](372-bot-starter-armies.md)) — superproject `5b32462`, `e182bcf`

- [x] 371 - Shooting mode lobby setting: **Declare First** (aim every weapon before any dice are rolled) vs **One At A Time** (default, and what the game has always done). **Closed 2026-08-12, hand-verified in the running app.** Two forks signed off before building: the mode reuses `ChooseRangedAttackRequest` and simply asks again rather than introducing a declaration screen (so both front ends, both AI profiles and the wire work unchanged), and shots declared at a unit an earlier weapon destroys are LOST, not re-aimed - re-prompting would hand back exactly the casualty information the mode exists to withhold. The declared target rides the pending-queue entry rather than `DefendingUnit`, which cannot describe several declarations aimed at different units; `ConsumeAttackIntoContext` re-points the defender as it dequeues, keeping `DefenderRemainingWoundsAtStart` a per-attack snapshot in both modes. Declare First then had to SHOW its queue - Chris played it and reported that pressing Fire with guns still left feels like a malfunction, which it did, because the panel silently lost a weapon and came back: declared shots now stay in the weapon list as inert accented rows numbered in firing order and naming their target, "Fire!" reads "Declare", "Done shooting" reads "Done declaring", and the CLI got the same list (wording in `DeclaredShotText`, ImGui-free, 9 tests incl. the ASCII gate). Limited needed no change and is now pinned: declaring IS the commit in both modes, so a once-per-game weapon is spent when aimed, cannot be declared twice, and a rocket declared into a doomed unit is spent for the game without ever rolling - the bargain, not a bug. **Two bugs were caught rather than shipped.** The declaration drain was first gated on the pending QUEUE instead of the MODE; five pre-existing tests caught it and were briefly, wrongly edited to fit the new code - Chris called that out, they are back verbatim, the production code was fixed instead, and `OneAtATime_WithAnAttackStillQueued_StillOffersTheNextWeapon` now states the invariant outright. And `AimPendingAttackOneCopyAtATime` / `TrimPendingAttack` both bailed unless EXACTLY ONE attack was queued - always true under One At A Time, so the guard was invisible, but under Declare First earlier declarations sit in front, silently disabling #340 Takedown splitting and #276 line-of-sight trimming from the second declaration onward (a sniper volley would have fired as one burst; a trimmed weapon would have rolled dice it had no right to). Both now edit the last queued entry, which turned the pending `Queue` into a FIFO `List`; two regression tests, both confirmed failing against the old guards. Verified headlessly in both modes against `340-sniper-split-targets` and `336-weapon-rules-showcase`. Engine 2961 green, app 1184 green. Left open on purpose: **#374** (the Tactician loses overkill avoidance, since its score reads live wounds and nothing is rolled between declarations), no scenario/headless switch for the mode, and no ShootStage-level harness test for the full re-entry loop. ([WorkItems/371](371-declare-first-shooting.md)) — engine `282cdf3`, `a3e2cf0`, `44bb6ee`; superproject `b5ccf4f`, `9d575e4`, `d65fe8a`, `2354991`

- [x] 367 — An upgrade granting a rule the unit already has was dropped as a duplicate, so the Procession Altar stayed Transport(6) with Extended Cargo bought (97 Tough / 23 Impact / 7 Transport / 2 Caster instances corpus-wide - every mounted hero at half Tough). Item-granted ratings now stack in both list paths; 3 sample armies migrated. Done 2026-08-08. ([WorkItems/367](367-item-granted-ratings-stack.md))
- [x] 365 — Cover as a habit: two-tier positioning for the Tactician, replacing #363 facet 3. **Closed 2026-08-06.** Tier 1 SHIPPED: threat is priced through walls again (the facet-3 boolean was fact-math on a forecast - a cliff can only produce "ignore cover" or "hide in it", never a bent route); cover instead earns a BOUNDED bonus, `MoveCoverHabit` (0.05, measured bracket) x (shadowed share of enemy guns - reachable share of enemy swords), each half normalised against its own kind of threat so an unavoidable threat cancels in the argmax, the melee denominator restricted to reach that matters this activation (slice 2a, pin 17 verified failing pre-fix). Engaged target exempt (chosen exposure is retaliation's bill). Pins 1-5, 11, 13-17; pool 85.39% vs 85.70% control (coin flip - the maps are 2.2% blocking terrain; pins are the spec, the pool the net). Tier 2 (the lethality gate) MEASURED AND REJECTED in every formulation: a morale-knee curve lost 4-14pp across three aggregations (share-weighted sum, army-normalised sum, ranked decay), monotone in perceived threat - the replay post-mortem showed 27 of 28 decisions changed, i.e. a second retaliation term, not a gate - and the wipeout-only veto rebuilt from that finding still lost 4.06pp (z -3.11), worst for melee armies whose correct play is crossing near-lethal fire. Structural lesson recorded in the weights file itself: in an argmax over one unit's candidates, f(threat) x candidate-constant IS a retaliation term; a goal-overriding caution term has no pool-safe magnitude here. ([WorkItems/365](365-cover-as-a-habit.md)) — engine `e94f498`..`e13e3be`; superproject `d7dc7b5`..

- [x] 363 — Tactician was line-of-sight-blind: `CombatMath.EstimateShooting` dropped a weapon only when `reach < distance`, so a squad behind a corner wall was credited a volley it could never fire, and the phantom offense propped up the move that earned it (Chris's `BattleBrothersJustMovedAShortDistanceAndDidntShootWhy.fdgsave`: a 6" advance into the wall's shadow at 0.2220, then "No actions available - passing", while a real firing lane sat 5" to the side). **Closed 2026-08-06.** Three facets. (1) `AttackContext.SightBlocked` -> `SightFactor`, a scale (default 1, so every geometry-free caller is untouched): the offense term passes 0 for a cut lane, since that shot would be taken FROM the endpoint being priced; Indirect is exempt per weapon via `SightRuleQueries.IgnoresTerrain`, so #360's artillery keeps its value. (2) `MacroActionGenerator.ClearLaneGoal` rotates a blocked band endpoint around the target in 15-degree steps (<=90 each way, deterministic order) to the first on-table sample that can actually see it - "step around the wall and shoot" now EXISTS as a candidate. (3) The mirror: incoming fire respects the same walls at `BlockedThreatShare` (0.4) - retaliation, projected threat (sighted from the projected position), the retaliation share's denominator, and the transport bail-out. Discounted, not silenced, deliberately: threat terms price the enemy's NEXT activation and it moves before it shoots, so a hard zero would invent perfect hard cover and teach the army to hug walls. Melee untouched - a charge needs a path, not a sight line. LoS is centroid-to-centroid against Blocking terrain (the engine's gate is per-model; the approximation errs toward undervaluing a marginal shot, never toward a phantom one). 7 pins (`TacticianLineOfSightTests`), suite 2900, scenario `363-wall-shadow-engage.json` + two signal armies (the Marksmen/Dummies pair carries ONE rifle per unit - too weak to demo shooting value). Pool gate 4 x 640 games, Tactician vs SoloRules: control 85.7%, control REPEAT bit-identical (hash `6638851179176049`, **zero flips** - so the harness added no noise and the deltas below are all signal), facets 1+2 85.2% (-0.55pp, z -0.70), facet 3 84.8% (-0.86pp, z -0.88), 0 faults anywhere, decision cost flat. The save now scores that advance -0.1477 and rushes the objective instead. Left open as **#364**: melee threat is still straight-line, through Impassible terrain. ([WorkItems/363](363-tactician-los-blind-shooting.md)) — engine `8c875a7`, `7498717`; superproject `03c056c`, `984f638`

- [x] 359 — Crowded-zone lane clearing (#296's deferred residuals, Chris's re-report). **Closed 2026-08-05.** Measured: the #296 frontline bias DECIDES ~15% of crowded multi-option activation picks (healthy, no retune; `activate` narration now rides --log-decisions, pinned). Built: `LaneGeometry` advance lanes over not-yet-activated friendlies, `MoveLaneBlock` (0.2) endpoint penalty scaled by lateral fade AND (1-t) along the lane - Chris's mid-review correction: walking FORWARD clears the lane too (the friendly steps into the vacated ground), so only the near-corridor wall prices and SideStep is the fallback for units with a real reason not to advance - plus the gated M13 SideStep family (Advance-budget, still shoots). Five pins incl. the walled-ahead lateral step. Pool A/B 3200 games: 84.84% -> 84.39% (flat, 0 faults), solo D1 bit-identical, decision cost flat. Honest limit, confirmed by observation: a rationally-packed round-1 zone has flanks as congested as lanes, so the packed-seed creep needs option 3 (friendly-aware routing/staging) - not built, file fresh if big games still show it. Found #358 while gating the bench. Was #356 pre-reconciliation-55. ([WorkItems/359](359-crowded-lane-clearing.md)) — engine `d0a7295`

- [x] 358 — Solo bot livelock: a wedged unit's main-activation-move decline (#208) reopened the action menu and the deterministic Charge > Move policy re-picked Move forever (~1.5M decisions until the FdgLab watchdog; surfaced as 2 'watchdog faults' gating the #359 bench, seed 1010 standalone burned 600s). **Closed 2026-08-05.** `DefineMovementPathRequest.MainActivationMove` (true only from DefinePathStage) + a `SoloMoveDeclineLatch` shared by each solo set's menu/path resolvers: the decline arms it, the next action pick consumes it and skips Charge AND Move once, ending the activation with Shoot/Pass. Triggered-move declines (GameOperationServices) stay latch-free and final; the Tactician's fallback pair is wired too (its planner path was already immune). Two pins; both fault seeds complete; 3200-game bench 0 faults; solo D1 bit-identical (no D1 game wedges). Recorded, not built: the charge-shaped analog (believed unreachable, #312 gates the offer). ([WorkItems/358](358-solo-move-decline-livelock.md)) — engine `e42d3ce`
- [x] 357 — A saved `.fdgarmy` stores the RESULT of its upgrade picks and never the picks, so the Forge could not reopen one (#307) and #356 only fixed files saved from then on. **Closed 2026-08-05, hand-verified in the running app** (`armies/3k - Titan Lords.fdgarmy`, previously session-less, opens correctly). `SelectionSolver` runs `ListCompiler` backwards: enumerate a roster unit's option space depth-first, compile every candidate through the REAL compiler, accept only a match — so the replace-chain algebra (#218/#261/#323/#324) never had to be re-derived in reverse and a wrong guess cannot pass. Matching is on size + weapon loadout, **not price**: these armies are OPR imports carrying Army Forge's per-unit costs, which our compiler is known to disagree with (#218/#219), and requiring equality converted **0 of 11**; price is the tie-break and the residual delta is reported. Selections are returned only when EVERY unit solves — a partial session would make an army's editable half disagree with the army. Combined pairs (#107) are *tried* rather than detected, since some merge without the "(Combined)" suffix (Robot Legions Warriors: 10 models from a 5-model roster entry with no add-models section); symmetric pairs only. Joined heroes (#006) carry their id reference across. Two bugs found by the corpus: `CountedBound` capped a counted Replace at the BASE loadout, so a section whose targets another section GRANTS could never reach its true count (the #323 starved-replace shape — a Titan swapping its shield into a second Heavy Hammer, then replacing both), putting the right answer outside the search; and the drift measure compared "Warriors (Combined)" against a rebuilt "Warriors" and reported a phantom dropped unit, which #356's reopen modal would have shown users as a unit being lost. App side: `--retrofit-editable <fileOrDir> [--dry-run]`, mirroring `--retrofit-effects`, which compares the playable projection before and after and REFUSES the write if a byte moved. Progression 0/11 → 3/11 → 10/11 → **11 of 11**; `armies/` is 22 of 22 editable, every playable half byte-identical to HEAD, points totals unchanged, two armies played to a result headless, re-running is a no-op. Left open on purpose: asymmetric combined pairs (none in the corpus, would square the search space), armies whose unit names are in no bundled book, and #354's `--refresh-army-rules` residual. ([WorkItems/357](357-retrofit-editable-armies.md)) — engine `ed6d997`, superproject `c71f68f`
- [x] 341 — A rotation was still applied to the square the model was STANDING on: every leg was swept at its arriving attitude, so a rectangular model parked beside a wall could not be told to "move out a way and then turn" — the turn was tested at the start square, where it clipped the wall. **Closed 2026-08-05, hand-verified in the running app.** #150's auto-facing tripped the same fault with no keypress at all (a rectangle setting off diagonally was swept at the diagonal attitude from its start square). A path is now a sequence of POSES: a leg is blocked only when its swept footprint collides at BOTH endpoint attitudes (owner's pick over departing-attitude-only, which is *stricter* than today for the auto-facing case), and every node's pose is checked strictly on its own — which is what still refuses a turn INTO a wall, and the only check a pure pivot has. The OR is per attitude over the whole obstacle set, never per obstacle. Legality widened; **hazard detection did not** — Dangerous/Difficult crossing stays on the arriving attitude behind an explicit `ELegAttitudeRule`, since "does this ground affect the model" is not "is this move legal". The impassible gate now delegates to the preview's finder, so the two walks `docs/ResolverGuide.md` requires never to diverge are one implementation. Animation was new plumbing, not a tweak: nothing interpolated facing before and `CommitPositions` snaps the model to its final attitude before the beat plays, so every turn popped on frame one; `ModelMove` now carries facings 1:1 with waypoints ([0] = the pre-move resting pose) and `GlideState` turns the model per leg, shortest arc. Movement (single + group) and consolidation. Left open on purpose: a model that STARTS overlapping impassible terrain can no longer even pivot (no "not worsened" guard, deliberately — the leg sweep never had one either), and the GUI's `EnemyClampTravel` two-attitude change has no test (protected by construction + the tested engine gate). Was #340 pre-reconciliation-52. ([WorkItems/341](341-rotation-belongs-to-the-node.md)) — engine `c0b0304`, `617e51f`, `1ffbc8e`, `ef75bb2`; superproject `fa3abbb`, `8fc736f`, `954ed01`, `192eaa0`

- [x] 340 — Takedown (Sniper) aimed a unit's whole rifle stack at ONE enemy unit: #157 gave each shot its own victim model, but every shot stayed locked on the unit the first pick chose, so a squad's snipers could never cover different targets. **Closed 2026-08-04, hand-verified in the running app.** Firing a Takedown weapon now commits exactly one copy (`AimPendingAttackOneCopyAtATime`) and hands the rest back to the shoot action's pool, so `DetermineCanKeepShootingStage` re-offers it and each rifle picks its own target unit through the existing picker — forecast, gating, CLI/GUI/AI resolvers and the wire all unchanged, which is why this flow was chosen over a new per-shot target prompt or model-level ("unit of 1") targeting. Owner ruling: `MAX_TARGETED_UNITS_PER_SHOOT_ACTION` still binds sniper shots, so three rifles spread over at most two units. A per-weapon fired-copy counter feeds #276's `BurstShotIndex` so consecutive shots still animate from different carriers; `LimitedRules.MarkOneCarrierFired` spends ONE carrier per firing, or the first rifle would burn the once-per-game shot of the rifles still waiting to be aimed (BlessedSisters' Crossbow-Mod is the shipped Limited+Takedown case). `WeaponOption` gained `AimedIndividuallyRule`/`CopiesRemaining` so both resolvers can say the weapon reappearing after it fires is the rule working, not a bug. `DetermineMorePendingShotsStage` deleted along with the pre-split burst it existed to drain — its "target destroyed -> remaining shots fizzle" rule goes with it, strictly better since the surviving rifles are re-offered and aim elsewhere instead. Fixtures: `Scenarios/340-sniper-split-targets.json` + compiled `.fdgsave` + two armies. Left open on purpose: "fire all remaining copies at one target" in a single pass (N rifles at one unit costs N passes today), and a networked re-check of the two new request fields. ([WorkItems/340](340-takedown-per-rifle-targets.md)) — engine `ce71a25`, superproject `bfa9b93`, `6301d70`

- [x] 321 — Companion actions on a menu row: a second button ON the option's row, not a peer list entry. **Closed 2026-08-02, hand-verified in the running app.** `StringSelectionRequest.SecondaryActions` maps an option to the action belonging to it; the GUI draws it to the RIGHT of its owner sharing the row's letter under Shift (`[^E] Hold back`, sized to its text, capped at 42% of the row, greyed with its reason when refused), the CLI prints it as an indented `[sN]`. Companions are skipped when building rows AND when handing out letters, so they neither duplicate a row nor burn the ten-letter pool. `ResolverHotkeys.IsLetterPressed` gained a shift-aware overload and the plain one now means "letter, without Shift" — required for a shared letter, and the one change here that reaches every other menu. The companion stays an ordinary option on the wire, so any resolver ignoring the map is unaffected. Was #317 pre-reconciliation-40. ([WorkItems/321](321-menu-companion-actions.md)) — engine `7ca1299`, superproject `7c8d411`

- [x] 320 — Limited was not enforced in melee at all: `ChooseMeleeWeaponStage` never called `IsSpent`/`MarkFired`, so a once-per-game melee weapon was usable every combat, all game, and could not be declined. **Closed 2026-08-02, hand-verified in the running app.** Spent weapons are offered as unavailable, choosing one marks it fired, and `LimitedRules.IsSpent`/`MarkFired` gained model-subset overloads so only the models within melee range spend a use — marking a carrier standing back would burn a charge it still has AND retire the weapon for the whole unit a melee early, since `IsSpent` asks whether every living carrier has used it. Hold back + an `OnNoWeaponsLeftToSwing` exit bound in both graphs that host the stage (charger's swing and strike-back); the AI never picks a hold-back (its catch-all is `ValidOptions[0]`, the Ambush trap). Narrowed by #318, re-presented by #321. Left open on purpose: the Charge gate still does not know about spent Limited weapons. Was #316 pre-reconciliation-40. ([WorkItems/320](320-melee-limited-not-enforced.md)) — engine `2f3ee24`, superproject `716eef4`

- [x] 319 — Limited weapons were mandatory: the shoot loop re-offered every weapon while any remained and withdrew Back as soon as one had fired, so a unit was forced to burn a once-per-game weapon. **Closed 2026-08-02, hand-verified in the running app.** Per-weapon Hold fire (a null `TargetUnit` on `RangedAttackChoice`) + Done shooting (confirmed, naming what goes unfired) + `AllowStopShooting` as the truthful mirror of `AllowCancel`; `DeclineWeapon`/`DeclinedWeapons` keep a declined weapon out of `AlreadyUsedWeapons`, which is what lets a declined Deadly+Limited weapon stop gating the unit's other weapons — the case a plain end-the-shoot exit could not serve. Visibility: ONCE PER GAME / SPENT badges, an amber consequence line, a spend log line, and `WeaponOption.LimitedRule`/`LimitedAlreadyFired` carried on the request because `IWeapon.RuleDefinitions` is `[JsonIgnore]` and does not cross the wire. Hand-test fixtures: `LimitedWeaponsTest.fdgarmy` + `Scenarios/limited-weapons-test.json`. Melee split to #320. Was #315 pre-reconciliation-40. ([WorkItems/319](319-limited-hold-fire.md)) — engine `4b64679`, superproject `cc8ab3f`, `9753d71`

- [x] 318 — Melee hold-back is a Limited-only exception, not a general opt-out. **Closed 2026-08-02, hand-verified in the running app.** Owner-supplied rule: "Models within 2" horizontally and 4" vertically of enemies must strike with all melee weapons", so #320's hold-back-on-every-weapon was too broad. Now offered only for weapons carrying Limited — an ordinary weapon gets no row at all, not even a greyed one, since that would imply the choice exists and is merely unavailable. The only-weapon refusal is unchanged and re-worded to cite the rule. Shooting's Hold fire (#319) is deliberately untouched: that rule governs melee, and a unit may decline to shoot entirely. ([WorkItems/318](318-melee-hold-back-limited-only.md)) — engine `dcf6e04`, superproject `203e9a5`

- [x] 317 — Moves silently snapped back at difficult terrain, so the rule read as a bug (2026-08-02 playtest). **Closed 2026-08-02, hand-verified in the running app (all 5 checks).** A clamped move now also draws the pose it WOULD have taken in pale gray, dotted-linked to the real ghost, under a two-line label naming the rule and what it cost — `Can only move 6"` when moving through, `Cannot enter - 6" used` when the cap was already spent, since telling a model that has already moved 6" that it may move 6" explains nothing. Single mode (selected model) and group mode (one phantom per held-back model, formation shape preserved by re-solving the whole rigid step with `Feasible(..., includeDifficult: false)`, one label at their centroid). The phantom is a true counterfactual — the move re-solved with ONLY the difficult clamp off, still subject to the enemy/table clamps — so a step shortened by the band cap, an enemy base or the table edge draws nothing and terrain is never blamed for a limit it didn't impose. Impassible terrain, which refuses the placement rather than shortening it, gained the matching text in red beside its existing contact footprint, once per frame (a blocked group step reports a crossing per phantom). Show/hide rules + wording in `DifficultShortfallPlan` / `ImpassibleBlockLabel` (12 tests, ASCII-pinned); both rules share `DrawTerrainReasonLabel` so they can't drift in voice or placement. App-side only, nothing deferred. Was #315 pre-reconciliation-39. ([WorkItems/317](317-difficult-terrain-shortfall-preview.md)) — superproject `d8c39cc`, `1bdf123`

- [x] 316 — Every round opened with the wrong player (2026-08-02 game): reported as intermittent, but in a two-player game the wrong player opened **every** round, round 1 included. **Closed 2026-08-02, confirmed in a live multiplayer game.** The order handoff was already right (`MainPhaseContext` seeds round 1 from the deployment roll-off and thereafter takes the finish order, so index 0 is always the team that should open); what was missing is that the round OPENS there. Each round builds a fresh `TeamPlayerAlternationCursor` at index 0 and `TryAdvance` means "move PAST the current position", so `DeterminePlayerTurnStage` - which advances before every activation, including the round's first - handed the opening pick to index 1, plus the same off-by-one on the within-team round-robin. Deployment and objective placement dodge this with a hand-rolled first-turn guard; the round loop had none. Fixed by parking the round's cursor one step short of the head (`ParkBeforeFirstTurn`, called only from `SingleRoundContext`'s fresh constructor): `TryAdvance`'s meaning is unchanged, so the three read-then-advance walks (both terrain passes + the points ledger) are untouched. Parking is a cursor POSITION not a flag, because the #052 snapshot is written before a round's first advance - it round-trips through the existing `CurrentTeamIndex` int, so no `GameProgressData` field and no old-save behaviour change; `ScenarioCompiler` had already hand-rolled the same trick. Two existing tests were passing on the buggy order and were repaired, not re-pinned. ([WorkItems/316](316-round-opens-on-wrong-player.md)) — engine `5895674`, superproject `d9f887c`

- [x] 313 — The shoot panel drew fire lines straight through blocking terrain: each shooter's line aimed at the nearest defender by raw distance with no sight test, while the volley itself (correctly) resolved against a model it could see. **Closed 2026-08-02, hand-verified in the running app.** New public engine `ShotEligibility` is now the one can-hit test — `BuildBlockers` (terrain + `BuildModelBlockers`, hand-rolled at every call site before) and `NearestVisibleModel` (nearest living, placed defender both seen and in range; a null blocker list means the weapon ignores line of sight) — and #276's attack-beat endpoints delegate to it, so the preview and the tracers cannot drift. No wire change was needed: `modelsThatCanShoot` already carries the range truth, so the nearest VISIBLE defender is necessarily the in-range one. Second ask (no can-hit / can-charge indicators on a teammate's units) was already correct everywhere but hand-copied in four places — now consolidated on `TeamAwareness` with tests. Deliberately unchanged: the targeting stage's own cached copy of the test (authority, not preview; guarded by an agreement test that runs the real stage) and the movement resolver's cover-aware preview (audited, already sight-correct). Was #312 pre-reconciliation-37. ([WorkItems/313](313-shot-eligibility-preview-parity.md))
- [x] 311 — Accidental passes in the Choose Action menu (both players in a 2026-08-02 game ended activations that still had actions left): Pass is pulled out of the option list and pinned to the bottom of the resolver panel with Back beneath it, separated from the actions above and raised off the panel edge, and choosing it (click or its X hotkey) opens a centred confirmation - Enter/Space passes, Esc cancels without opening the in-game menu. **Closed 2026-08-02, hand-verified in the running app.** Client-side only (`ActionMenuLayout` + `GuiStringSelectionResolver`); menus with no Pass option keep their original layout. Was #309 pre-reconciliation-35. ([WorkItems/311](311-pass-confirmation.md))
- [x] 309 — Networked client rendered late-deployed models (Ambush arrivals above all; disembark/spillout siblings) label-only — name/health floating with no circle/rectangle bases — until the unit next moved; the host never saw it. **Closed 2026-08-02, hand-verified on the original repro.** Two independent causes, both fixed: (1) engine flows applied placement positions BEFORE clearing the off-table token (InReserve / EmbarkedIn / OffTableFromForcedMove), and each `SetPosition` replicates immediately, so a client's renderer — which registers a model for base drawing on the position event and captures the owning unit at that moment — snapshotted a still-reserved unit; reordered at all six sites, pinned by 3 ordering tests that ride the position binding's `OnValueChanged` exactly as the renderer does. (2) The capture itself was unsound on clients, where every replicated `UnitData`/`ModelData` update deserializes a NEW instance (`TokenChangeBroadcaster` re-Sets whole objects; the host mutates in place, hence host-side invisibility of the bug): `DrawModels` now resolves the owning unit from live table state per frame (ModelID map, captured tuple as color source + fallback) and `OnModelPlaced` matches by ModelID, which also fixes a latent never-registers variant for models whose own slot got replaced before first placement. Disembark/spillout verified by engine tests only, not from a live client. ([WorkItems/309](309-client-invisible-late-deploy-models.md)) — engine `3c2ac8d`, superproject `91451c2`

- [x] 306 — Both weapon choosers keyed their pools/LoS maps/stat caches by `Weapon.Name`, and `Dictionary.Add` throws rather than overwrites, so a same-name split weapon (a multi-copy partial upgrade — 3 rifles where 1 buys Precise) **faulted the state machine mid-activation**. **Closed 2026-07-31.** New `WeaponProfileKey` (name + stat line + sorted rule multiset) built around one invariant: two weapons share a key exactly when `WeaponComparer` calls them equal, i.e. the comparer the pool already dedupes by — so one pool entry is always one option. Ordinal sort on it is name-primary, making #209's ordering a TOTAL order (the old name sort was not, once names could repeat) while staying byte-identical for the unique-name case. Both choosers, the effective-range cache, Deadly-first gating and `CountEligibleCopies` re-keyed; the Shoot gate now builds its pool through the same new `WeaponPool.GroupByProfile` that replaced `CombatActionContext.GetTypeSortedWeapons` (its own TODO, and O(n) not O(n^2)), so gate and stage can't disagree (#200). A networked reply is rehydrated before matching — only viable because #258 made rule-definition equality canonical-name-based. Melee label uniqueness made structural; filing's claim that melee labels drop a rule's argument was WRONG on the real path (`RequestedName` is the entry's `PrintableName`, already "Deadly(3)") — corrected in the detail file. Sergeant rename kept as attribution only (Chris). 11 pins in `SplitWeaponProfileTests`, 6 of which reproduce the fault pre-fix; play probe with a hand-authored split-Carbine army goes from `[GAME ERROR] ... same key ... Carbine` to a clean game with the two profiles firing as separate volleys. Deliberately not widened: the movement overlay's name-keyed sight profiles (silent, needs a wire-record design call). ([WorkItems/306](306-weapon-chooser-name-keying.md))

- [x] 305 — `ArmyLoader.LoadFromFile` folded EOF into "empty line" (`IsNullOrEmpty` + `continue`), so a piped headless run whose army failed to load spun on a `ReadLine()` that could never block again — 5.8 GB of `No path entered.` in one #197 probe. **Closed 2026-07-31.** `LoadFromFile` now returns `ArmyListFile?` with null meaning EOF, and `PromptForArmy` owns the fallback so the "no army" decision lives in one place; a blank line is still a real retry. The audit the item asked for came back clean — all 14 CLI resolver prompt loops already branch on `input == null` with a documented EOF default, and this was the only offender. 3 pins in `FdgRaylib.Tests/ArmyLoaderEofTests.cs`, bounded by a console writer that throws past 500 writes so a regression fails in milliseconds instead of hanging. Probe: `printf "1\n" | ... --headless` exits 0 with 43 KB of log and plays to a result. App-side only. ([WorkItems/305](305-cli-army-prompt-eof-loop.md))

- [x] 197 — Faction rule coverage pt.2: the dead rule names that needed new engine PRIMITIVES (the half #196 could not author as data), plus one regression where already-implemented rules never attached. **Closed 2026-07-31: corpus dead refs 2,342 -> 0 of 13,870** — every rule name any shipped book references now resolves to a definition that actually fires, and the `scope-mismatch` category is empty. Roughly two dozen new effects/conditions/capabilities/contexts, seven declared-but-dead hooks lit, and the dice invariant held throughout — the last slice (P12 Regenerative Strength) was deferred nine days rather than round a roll-derived value, and shipped on a fractional `TokenPayload.Magnitude` once it emerged that the filed "separate decisive roll" option could not exist (the wound pool reaching the ignore fold is already fractional, so a decisive re-roll must round first). Deliberately left: `Shooting_OnPreHitRollCount` dormant (no rule can be authored at it correctly), model-scoped markers unbuilt (no corpus carrier can hold two models — pinned by census tests). Two hygiene finds carried out separately: the CLI army-file EOF loop and the ranged chooser's weapon-name-uniqueness fault. Detail file keeps the slice-by-slice history, the six standing lessons, and every owner ruling. ([WorkItems/197](197-faction-rule-engine-primitives.md))

- [x] 300 — The dice caption strip spoke two color languages: the stripe and badge word color-coded the roll's category, but every success drew green and every failure gray regardless of what was being rolled — **GUI-verified 2026-07-28.** Settled successes now draw in `AccentFor(beat.Category)` (both the realistic dice row and the probabilistic bar fill), and the accent palette gained a fourth hue: `Defense` steel -> green, `Misc` neutral gray -> amethyst, plus a new `ERollBeatCategory.Magic` (arcane blue, `CAST` badge word) for the cast attempt, which had been falling into `Misc`. Only the cast roll is `Magic` — a spell's damage roll stays `Offense` and the save against it `Defense`, documented on the enum; the value is appended so networked ordinals are unchanged. Panel scaled 25% through a single `Sc()` knob so type/dice/chips/padding/badge grow together (`BottomMargin` left unscaled — dock gap, not panel geometry). Roll-off dice untouched (`RollOffBeat` has no category). Left to playtest: attack successes are ember-red while save successes are green, which encodes whose roll it is rather than good/bad. ([WorkItems/300](300-dice-panel-category-colors.md)) — engine `75c5837`

- [x] 280 — Other players stared at a frozen board with only "waiting on Player N" while someone planned a move — **GUI-verified live 2026-07-25 (two instances).** Networked decision previews: the engine transports opaque (slot, typeName, json) payloads (`IPreviewChannel`/`IPreviewFeed` on `IFDGGame`; host relay verifies sender-owns-player, rate/size caps, never deserializes; previews expire when the player's last outstanding task resolves), the app publishes the active resolver's `IPreviewSource` state at ~10 Hz with serialize-and-compare dedup and draws every other player's via per-family presenters. Two payload families: ghost+path (movement bands, consolidation, aircraft advance, place-objects — base/ghost slot split as the bandwidth cache) and marker (objective + terrain placement, footprints flattened to wire circles/quads). Per-resolver opt-in; new visual families need only a payload record + presenter. Filed as #277, renumbered on merge (Reconciliations 26). ([WorkItems/280](280-networked-decision-previews.md)) — engine `3546835`, superproject `085bcbc`

- [x] 279 — Lobby network teardown: an abandoned host lobby kept the port as a half-alive zombie (host UI blind to joins, one-way chat, ghost roster slots piling up on rejoin) because leaving a lobby never stopped the `FDGHost`, the replacement listener's bind failure vanished into a discarded task, and the old VM's `Dispose` left its greeting handler registered — **GUI-verified 2026-07-25.** Both lobby VMs' `Dispose` is now idempotent full teardown (deregister everything, detach the bus, host `Stop()` / client `Disconnect()` — unhooked first so teardown never reads as "host lost"); the app tears the active lobby down on Back / game exit / new-lobby adoption; HostModal + LoadGameFlow surface port-bind failures visibly. 4 pins in `Tests/LobbyTeardownTests.cs`. ([WorkItems/279](279-lobby-network-teardown.md)) — engine `c389648`, superproject `9b49d4d`

- [x] 275 — One always-blocking `BannerBeat` announced everything from "Victory" to "Warriors embarked Rhino", so the big moments stopped landing and nothing else could be announced at all — **GUI-verified 2026-07-24.** Split into `EBannerTier` Headline/Notice/Toast: Headline still blocks for its full 1300ms, Notice paces a 300ms lead-in, Toast paces nothing, the two lower tiers riding a concurrent held-banner track in `PresentationPlayer` (same seam as #232's cascade and #238's attack track) and staying out of `IsAnimating` so they never gate a prompt. Three screen bands (y=26% / y=40% / a ticker under the status HUD) and three voices; a new Notice supersedes the last, toasts stack to 5. All 28 announce sites re-tiered 4/15/9, and `Announce` defaults to Notice so halting the game is what a call site has to ask for. Drive-by: an em-dash in the Counter banner that rendered as `?`. Filed as #274, renumbered on merge (Reconciliations 23). New announce sites deliberately deferred — candidate list in the detail file. ([WorkItems/275](275-banner-tiers.md)) — engine `449310f`, superproject `1538d52`

- [x] 274 — Spell casting had no visual language of its own (banners and a die). One `SpellEffectBeat` type, six variants — **GUI-verified 2026-07-24.** Emitted from `CastSpellStage` as assist boost -> assist hinder (batched right before the roll) -> cast success/failure on the caster (after the result banner) -> per-target boon/bane. Success/failure and boost/hinder are deliberate visual inverses. Boon vs bane comes from `SpellDisposition`: target affinity decides, effect breaks the `Any` tie only — effect kind alone misreads "enemy unit gets a bad rule", which is an `AddRule`. `CollectCastAssist` now returns who spent on each side, but the roll still reads only `Net`, so no rules behaviour changed. Six synthesized cues, drop-in replaceable from `Assets/Sounds/`. 17 tests. Enemy-hinder variant is test-covered but needs human-vs-human play to see live (the AI assist resolver declines by default). ([WorkItems/274](274-spell-cast-visuals.md)) — engine `befce91`, superproject `dbf51ba`

- [x] 270 — A game resumed through the lobby (or `--scenario`) could not be saved and loaded again — **fixed 2026-07-23 at the data layer.** Re-crewing destroys and recreates `PlayerSlotInfo`, and `Create` pre-increments that slot's generation, so the re-saved entry was at generation 2+ while a store rebuilt from the file starts every slot at 0: replay rejected it as `FutureGeneration` and the whole save was unopenable. The generation is a stale-reference guard between a live sender and receiver and says nothing about a snapshot, whose references are self-consistent — so snapshot rebuilds now go through `CreateFromReplay`, which adopts the entry's generation and keeps the invariants that still bite (index, type, already-assigned, generation >= 1); the live incremental network path keeps both ordering guards. Fixes every type and every resume path at once, with no save-format change. Verified on the actual file that failed. ([WorkItems/270](270-resave-after-lobby-resume-unloadable.md))

- [x] 265 — Lobby "Battlefield" dropdown picks the table's look (Forest / Desert / Ice / Mars-Like / Urban / Barren) — **done 2026-07-23.** Cosmetic only, but a synced `GameSettings` field rather than a local preference so every player sees the same board and a resumed save looks like the game that was put down; Forest is the zero value so pre-#265 saves load unchanged. Six surfaces hand-verified in the app, dropdown verified in a live lobby; host->client sync covered by a loopback test, not two machines. ([WorkItems/265](265-lobby-table-background.md))

- [x] 262 — "Army Forge charges a 'Replace X' whose target the unit doesn't carry; we charge 0" — **closed 2026-07-23: ruled an upstream Army Forge bug (Chris), no change on our side.** The 80-pt residual after #261 on the reported High Elf Fleets list was four Retributors squads each billed 20 pts for "Replace one Shard Carbine" while carrying no Shard Carbine (it exists only if the 65-pt "Replace all Energy Swords and Combat Shields" is taken, which none did). `ListCompiler` clamps applications to the matched target count, so the swap applies nothing and costs nothing — the honest reading. Noted for later: the import preview reports only a total, so a gap like this reads as an unexplained number instead of a named selection. ([WorkItems/262](262-replace-target-absent-charged-by-opr.md))

- [x] 260 — "Five bundled-book rule names differ from the catalog only by case, so they never resolve" — **closed 2026-07-23 as not-a-defect; the premise was wrong.** `RuleResolver` has backed its registry with `StringComparer.OrdinalIgnoreCase` since engine `390ae86` ("#100: case-insensitive rule-name resolution"), so `Bane in Melee` resolves to the catalog's `Bane in melee`; verified empirically for all five names. The item was filed on the strength of a stale doc comment in `CoreRuleCatalog.UnitAura` that still claims the resolver is case-sensitive (written pre-#100; unfixed here — submodule text, no engine authorization this session). The real bug it exposed was in #259's brand-new `RuleGlossary`, deliberately built case-sensitive for the same wrong reason, which made the Forge tooltip report those five rules as "not enforced by the engine" — fixed by adopting the resolver's comparer, plus a test pinning that glossary and resolver agree name-for-name. Book coverage restated 93.8% -> 94.3%. ([WorkItems/260](260-book-rule-names-case-mismatch.md))

- [x] 256 — AI multi-model units barely moved (WayTooManyInBack stuck bots, solo + Tactician alike). **GUI-verified 2026-07-22.** S1 measure-and-correct budgets replaced the worst-case repack pre-clamp (0.12" advances -> ~full budget); S2 budget-circle re-aim instead of halving on friendly stacking (blocked advance 3.11" -> 4.40"; repro scenario `Scenarios/256-friendly-in-lane.json`); S4 on-path snake through corridors narrower than the formation (the walled pocket drains rounds 3-4 on the real save); S3 (activation order) refuted by forward-run evidence, not built. D1 benchmark re-pinned zero-fault (`3674C906996F34CC` / `CE3DC8150005FF2C`, in #191), catching + fixing a latent G3 gap (unvalidated stand-still early-outs). Spun off #258. Engine `eb38407`..`f7b6d78`. ([WorkItems/256](256-ai-repack-clamp-immobilizes-big-units.md))

- [x] 219 — Bundled Army Forge upgrades imported as free (weapon/wargear costs showing +0). **Validated 2026-07-22 (Chris).** Premise was twice wrong: OPR DOES publish the prices, in a per-unit `costs[]` array keyed by unit id (same option costs differently on different units) that the importer never read - it read only the singular `cost`, which OPR omits on internally-priced options. `OprBookImporter` now resolves cost from `costs[]` by unit id; new `--import-book` CLI + `ArmyForgeBookService` re-priced all 47 bundled books (3467 real prices, 0 left unpriced; 8434/8434 options match live OPR). `OprListImporter` attributes each share-import selection's per-unit price so imported units carry base+upgrades with nothing in `UnattributedPoints` (Chris's HEF Elven Noble imports as 120/0, matching Army Forge). `ListValidator` warns on any genuinely-unpriced selection (safety net, kept). Engine `d568b77`, pushed `8d32b5a`; engine 1820/0, app 399/0. ([WorkItems/219](219-army-forge-zero-cost-upgrades.md))

- [x] 258 — Rule-definition identity breaks after save/load resume: rehydration (#095) builds a fresh `SpecialRuleDefinition` per attachment and record equality compared list members by reference, so every `Definition ==` site (Transport/Hero/Caster/WeaponComparer grouping) was silently false on resumed games — loudest symptom: duplicate-weapon-name crash in `BuildWeaponOptions` when a resumed Sniper Team shot (WayTooManyInBack round 3). Root fix: definition equality IS the canonical Name (the registry key). 2 pins in `RuleRehydrationOnResumeTests`; forward-run of the save now completes. Found during the #256 re-probe. Engine `13908c7`. ([WorkItems/258](258-rule-definition-identity-after-resume.md))

- [x] 196 — Faction rule coverage pt.1: author the dead rule names that are clones of live primitives, as data in `GdfRuleSupplement.json`. **Fully closed 2026-07-22**: F16's wargear names pinned to OPR's common-rules layer (local Army Forge snapshots + common-rules API, matching rule IDs) — Banner (unit morale +1) and Musician (unit +1" moves) authored as data, Armor(X) as an owner-approved zero-hook marker (Defense-set mechanic -> #197), Sergeant's 12 refs -> #197 (owner ruled per-model fidelity required; attribution seam doesn't exist). Final: 1,205/1,243 refs (97%) resolve; corpus dead refs 2,197 -> 492 over the item's life. ([WorkItems/196](196-faction-rule-data-authoring.md))
- [x] 255 — Lobby team selection. **GUI-verified 2026-07-21.** Per-row Team dropdown (Team 1..N, N = player count); new players default to the first empty team; launch hard-blocked while ALL players share one team; picks sync host/client via `PlayerTeamUpdateMessage` (protocol v3 -> v4), cloning the #221 color triad. Stale out-of-range teams after a disconnect kept by design; team edits disabled on resume. Pinned by `LobbyTeamSyncTests` (8); engine 1801/0. Known limitation: victory is still per-player, not per-team. Engine `2cffa11`. ([WorkItems/255](255-lobby-team-selection.md))

- [x] 254 — Wound-driven (ranged) morale tested only on the blow that CROSSED into half strength, so a unit already at half or less (e.g. a Tough-18 Great Monolith at 8 remaining) never tested again when shot. Live-play report 2026-07-21; rule keys on wounds leaving the unit at half or less, not on crossing. `CrossedIntoHalfStrength` -> `WoundsLeftUnitAtHalfStrength` (took wounds this action + now at half or less); targeted-but-unwounded still doesn't test, multi-weapon actions still test once. Engine-only; 1793/0 rebased onto #201. ([WorkItems/254](254-wound-morale-every-activation.md))

- [x] 232 — Saved-hits beat removed + casualty cascade. **GUI-verified 2026-07-18.** The `SaveBeat` deflection pings + ping sound are no longer emitted (save dice-roll captions keep the info; class kept wire-compatible but dead). v2: a volley's deaths/flinches overlap on a 150ms `CasualtyStagger` (Overlap/Held casualty beats, `ApplyWoundsStage` flags all but the last, `PresentationPlayer` cascade track) so 5 kills play as rapid-fire deaths in ~1.1s with the last running out fully; `ModelWounded` stays 300ms. Engine 1689/0, app 378/0. ([WorkItems/232](232-remove-save-hit-beat.md))

- [x] 235 — Game Over card polish. **GUI-verified 2026-07-18.** Title/result text centered; card draggable (position forced only on appear, `NoMove` dropped) so the final board is inspectable before returning to the menu. App-side only. ([WorkItems/235](235-game-over-box-polish.md))

- [x] 217 — Tactician Bot lobby naming. **GUI-verified 2026-07-18.** `AddAiPlayer` numbers a bot by its rank among same-profile bots instead of the total player count ("Tactician Bot 1" even after two humans; Tactician/DerpBot counters independent). Pinned by `LobbyBotNamingTests`. ([WorkItems/217](217-tactician-bot-naming.md))

- [x] 240 — Stuck-key hardening: every panel keyboard shortcut (Enter on Primary commit buttons, Esc cancels) now uses edge-only `IsKeyPressed(..., repeat: false)`, so a key whose release was missed (sticky hardware / focus change mid-press) can no longer auto-fire panels for the rest of the session. Root cause of "selecting Move instantly skips like Pass" (a stuck Enter self-committed the movement panel's Done as a legal 0" move). App-side only; app 376/0. ([WorkItems/240](240-stuck-key-shortcut-hardening.md))

- [x] 215 — Move-as-group for post-melee movement. **GUI UNVERIFIED.** Only Consolidation lacked it (the post-combat/Harassing move already has group mode via the shared movement resolver; pile-in has no prompt). `GuiConsolidationMoveResolver` gained the shared `FormationModeState` + a full translate+rotate group mode reusing `GroupFormationUtilities` (PlanGroupMove budget solve, coherency repair) and the "Mode: Group/Single (G)" toggle; rotation baked into committed facings; `ValidateConsolidationPaths` still gates Done. App-side only. App 327/0; builds + headless clean. ([WorkItems/215](215-move-as-group-post-melee.md))

- [x] 213 — Moving THROUGH impassible terrain: GUI let you place the move but not press Done (silent dead-end). **GUI feedback UNVERIFIED.** Engine: new `MovementUtilities.DoesPathCrossImpassibleTerrain` (preview sibling of the Difficult/Dangerous swept-base checks) + test. GUI: `GuiDefineMovementResolver` now draws the ghost/model red + red move line and blocks the click/Done when the path crosses impassible, in both single and group mode (folded into `ghostOverlaps` / `blocked[i]`). AI already couldn't cross impassible (the ladder validates via `ValidatePaths`); the ~1/1800 solo-mover leak stays #211. Engine 1617/0, app 327/0. ([WorkItems/213](213-impassible-terrain-move-feedback.md))

- [x] 182 — Move through friendly units, but not stop on them. **UNVERIFIED in the GUI.** Implemented across #205 (engine + AI + CLI: `ValidateEndsOnFriendly`, friendly footprints through `ValidatePaths`; pass-through allowed, ending-on rejected, no standoff) and #212 (GUI: friendlies no longer pass-through clamps, Done gate blocks ending-on in single + group mode). Rule + integration tests green; GUI feel still needs Chris's eyeball. ([WorkItems/182](182-move-through-friendly-units.md))

- [x] 212 — GUI wouldn't let a unit move THROUGH a friendly (only ending-on should be illegal). `GuiDefineMovementResolver.EnemyClampTravel` clamped the ghost short of every other unit's base, friendlies included. Fix (app-side only): a team-based `IsEnemyUnit` so the clamp + enemy-footprint build skip friendlies (slide through freely, both single and group mode); the Done gate now passes friendly footprints to `ValidatePaths` so a group move ending on a friendly is still blocked (matches #205's `DefinePathStage` check, so the GUI never submits a move the engine rejects). Completes #182 alongside #205. App 327/0; GUI behavior for Chris to eyeball. ([WorkItems/212](212-gui-move-through-friendlies.md))

- [x] 204 — Save-roll beats stacked unreadably and didn't say why there were extra wounds. Fix (expanded on Chris's follow-up to cover Blast/Furious/etc. + show the weapon and why): `RollToSaveStage` batches its beats by save THRESHOLD (base + Blast + Furious = one roll; Rending's per-hit AP = its own roll), each captioned `{weapon}: {breakdown}` ("2 hits x3 (Blast) = 6", "(2 +1 Furious) x3 (Blast) = 9", "2 hits, Rending AP+1"). Provenance threaded via a `HitGroupSource` tag + `EvaluateAllNamedLive`, keeping the hit-group structure identical so it's outcome-neutral (verified: same DOP-1 bench hash). Engine 1603/0. GUI spacing of the 2-roll Rending case still to eyeball. ([WorkItems/204](204-save-beat-pacing-rending-groups.md))

- [x] 205 — AI units ended their move stacked on top of FRIENDLY units (not base-shape specific - circle bases too): the movement validator only checked enemy footprints, so nothing forbade ending on a friendly, and both AI resolvers did it. Fix: engine `ValidateEndsOnFriendly` (end-overlap only, pass-through legal, not-newly-stacked guard) threaded through ValidatePaths/ValidateConsolidationPaths + pile-in obstacles; DefinePathStage/ConsolidateStage/PileInStage/MovementExecutor enforce; AI + CLI resolvers back off (GUI already did). Engine 1583/0; cited bench + rect-base vehicle bench 0 faults; rejection fired 306x/20 games. Shifts #191 benchmark hashes (owner re-baseline). ([WorkItems/205](205-rect-base-drives-over-friendlies.md))

- [x] 208 — Optional triggered moves (Harassing / Hit & Run) faulted the game when a unit intermingled with the enemy after melee couldn't re-pack into cohesion: `MoveUnit` threw on the ladder's cohesion-breaking last resort instead of declining. Fix: `allowCancel = isOptional` on the movement request; optional Cancelled/invalid-path replies now decline (unit unmoved, budget kept), forced moves still fault; `AiDefineMovementResolver` replies Cancelled when stuck-and-cancellable. Engine-only (2 files + 2 test files); repro seeds 1021/1039 clean, cited 100-game bench 0 faults, engine 1577/0. ([WorkItems/208](208-triggered-move-validity.md))

Entries keep their original wording; relative links inside them are written from the repo root.
Detail files (where they exist) live beside this file as `NNN-slug.md`.

## Done

- [x] 207 — AI movement/consolidation submits "ends within 1\" without charging" moves in 2k pool games (Dark Elf transport list). ([WorkItems/207](WorkItems/207-ai-standoff-violating-moves.md)) **Closed 2026-07-10**: root cause was engine core, not the AI — embarked models parked at (0,0) counted as movement obstacles (GetEnemyModelFootprints had no on-battlefield filter); fixed in engine `ebd2c8f`, pinned by EnemyFootprintTests, all repro seeds clean in pool baseline v3.
- [x] 199 — `AutoFill` faults on a tiny fractional wound in probabilistic mode ("Required: 0.0555, assigned: 0" -> whole game Fault); deterministic repro: rich-army fresh game, seed 31415. ([WorkItems/199](WorkItems/199-autofill-fractional-wound-fault.md)) **Fixed 2026-07-09**: one capacity formula + WoundEpsilon comparisons in AssignWoundsResults (engine `3d9e01a`); four-seed graveyard pinned, mutation-verified.
- [x] 203 — Stage transitions chain synchronously, so stack depth grows with game length: long games kill the PROCESS with an uncatchable StackOverflow (surfaced by #200; long legit games walk the same cliff). Direction: Task.Yield at an activation boundary, with the plan's B0 spike. ([WorkItems/203](WorkItems/203-stage-machine-stack-growth.md)) **Fixed 2026-07-09**: Task.Yield at activation boundary + Choose Action (engine `5d8c939`); outcome-neutral (hashes unchanged).
- [x] 200 — Choose Action offers Shoot with no fireable target -> deterministic AI livelocks (Choose Action <-> Shoot forever); GetCanShoot needs the GetCanCast-style target gate. Repro: Orks 2k pool mirror, seed 42. ([WorkItems/200](WorkItems/200-choose-action-shoot-loop.md)) **Fixed 2026-07-09**: Limited-spent gates before Deadly-first; gate and stage share one pipeline (engine `a8b593b`).
- [x] 198 — Engine game outcomes varied run-to-run beyond the seed. **Done 2026-07-09.** Root cause (Chris called it): `PlaceTerrainStage.PlaceAutoLayout`'s 40% deployment-zone thinning drew from an unseeded `new System.Random()` — the fully-qualified spelling dodged #193's audit grep — so every game played on a different auto-layout table and everything cascaded (the filed "movement paths differ / ambush arrival flips" were downstream symptoms; the suspected async-void race was a red herring). Localized with FdgLab's new `GameTracer` (ordered position-write trace interleaved with the log; `smoke --trace`). One-line fix: draw from `IGameContext.Rng` (#193 seam); unseeded play still varies. Verified: formerly-flipping seeds now 20/20 and 16/16 identical; two 200-game bench runs -> identical outcome hashes on both army matrices at DOP 16; new rich-army engine determinism test, mutation-verified; suite 1380/1380; seeded CLI runs byte-identical. Bonus: zero #159 faults in 1,200 deterministic games (noted there). Engine `068f937`. ([WorkItems/198](198-engine-run-to-run-nondeterminism.md))
- [x] 194 — FdgLab self-play harness (Tactician prereq P3). **Done 2026-07-09 — gate 3/3.** New in-repo console project, engine-only dependency: in-process `GameRunner` (fresh store/bus/server per game, slot-keyed AI seeds), watchdog->Fault, per-decision timing, log capture + `GameTracer`, `bench` (seeded side-swapped matchup matrix, win+0.5*tie scoring, faults listed with messages, md+CSV, SHA-256 outcome hash), `smoke --repeat/--dump-logs/--trace`, `probes` scaffold, `builtin`/`builtin-basic` armies. Baseline 5.25 games/s Debug (~19k/hour) at DOP 16; 200-game mirror exactly symmetric, zero hangs. Its outcome hash caught #198 on day one and its tracer localized it; reproducibility facet re-verified post-#198 (identical hashes both matrices). Deferred to Phase C1 by design: per-round score trace + state snapshots; probes authored from Phase A; Chris's curated army pool pending. Superproject `39f0282` (+ the #198 close-out commit). ([WorkItems/194](194-fdglab-harness.md))
- [x] 195 — Resumed games play four MORE rounds instead of finishing the four-round game. **Done 2026-07-09.** `ReconcileObjectivesStage` decided the game was over from `_timesEntered`, a per-stage-instance counter; a resume builds a fresh instance, so it restarted at zero — a save resumed at round 2 played rounds 2..5 (an extra objective reconciliation, `RoundsPlayed = 5`), one resumed at round 4 played 4..7, and the end-of-round log printed the entry count as the round number. Now terminates on the authoritative round number (restored from the save): `SingleRoundStage` advances `RoundCount` on its way out, so on entry the round just finished is `RoundCount - 1`. New `ResumeRoundCountTests` pins resume at rounds 1/2/4; mutation-verified (old code reports 5 and 7). Round-1 resume passed either way, which is why it went unseen — the shipped example scenario resumes at round 1. Suite 1370/1370; end-to-end a round-3 resume now plays 3,4 and stops. Found while verifying #193. Engine `a19e6ab`. Relates #052/#167/#192/#191. ([WorkItems/195](195-resume-plays-extra-rounds.md))
- [x] 193 — Determinism & seeding pass. **Done 2026-07-09.** Same seed + same build => identical game, including 16 concurrent games in one process. Scope proved larger than filed (six unseeded RNG sites, not two): new `GameRandom` (salted per-consumer streams; `DeriveForSlot` keys AI seeds on the stable slot ID, never the per-run PlayerID GUID); `ProbabilisticDiceRoller`'s `static Random` -> per-instance seeded (the parallel cross-talk hazard); `DiceUtilities` roll-offs dropped both private `Random`s for `IDiceRoller.RollDecisiveFace()` (retiring the stale "can't use the context roller" workaround); `PlaceOneObjectiveStage` shuffles from the new `IGameContext.Rng`; both AI placement resolvers take an injected seeded `Random` from `AiResolverRegistryFactory` (solo-rules distribution unchanged, per D1); CLI `--seed N` on headless + `--scenario` (overriding the saved seed) + the GUI scenario path. Suite 1367/1367 with 18 new `DeterminismTests`; verified end-to-end (two `--seed 42` runs byte-identical, `--seed 99` differs, unseeded still varies) and mutation-verified (un-seeding the objective placer turns 3 tests red). Surfaced and filed #195 (resume plays an extra round). Engine `c9a2466`. Tactician prereq P2. ([WorkItems/193](193-determinism-seeding.md))
- [x] 192 — Structured game result (`GameResult` + `FDGServer.OnGameCompleted`). **Done 2026-07-09.** `VictoryCalculationStage` now builds `GameResult` (outcome `Win`/`Tie`/`Fault`, winner + display name, per-slot final scores, rounds played, message) and raises it via `IGameContext.NotifyGameCompleted`, which **replaced** the single-caller `NotifyGameEnded(string)`. `FDGServer` fans out from one `RaiseGameCompleted` choke point to the new `OnGameCompleted` and the unchanged `OnGameEnded(string)` (front ends + wire), so the two can never disagree; disconnect/engine-fault paths report `EGameOutcome.Fault` through the same point. Headless CLI prints one stable ASCII line (`Game result: outcome=... winner=... rounds=... scores=[...]`). No wire-format or GUI change. Suite 1349/1349, build clean, headless exit 0; Win path verified end-to-end via `--scenario`. Engine `9b1c0ba`. Tactician prereq P1. ([WorkItems/192](192-structured-game-result.md))
- [x] 169 — Transport Rout occupant spill (audit §8.18, high severity). **Done 2026-07-08 (Option B, signed off).** Spillout extracted from the two hard-wired combat-pipeline stages (`SpilloutOccupantsStage`, deleted) into `SpilloutExecutor`, invoked from the `UnitDestructionNotifier` choke point before the token sweep and killer-null early-return — so Rout AND the four latent gaps (impact hits, spells, strafing, overwatch) all spill; #165 (dangerous terrain bypasses the notifier) is now nearly free. No-auto-sweep + interactive placement preserved. Suite 1317/0; GUI hand-verified via seed-pinned `Scenarios/rout-spillout.json` (also exercised #035 mid-game embark; surfaced + fixed the #167 GUI `--scenario` segfault `1ec69c9`). Engine `9395afa`. ([WorkItems/169](169-transport-rout-spillout.md))
- [x] GUI `--scenario` direct launch (facet of #167). **Verified 2026-07-08** — first hand-run segfaulted before the window opened (TransitionToGame attached the #162 overlay's GL resources pre-InitWindow); fixed via `RaylibRenderer.OnWindowReady` deferral (`1ec69c9`), then verified: straight into the game, slot 0 playable, AI opponent acts. ([WorkItems/167](167-human-testing-tools.md))
- [x] 185 — Replace `RuleEvaluator` participant tuples with a `readonly struct RuleParticipant`. **Done 2026-07-08.** Mechanical, behavior-preserving refactor surfaced during #183: collapsed `EvaluateAll`/`EvaluateAllNamed` from 6 tuple-arity overloads + 2 shims to 2 methods; migrated every multi-participant call site (11 production files, 10 test files) plus `TestRuleHarness`'s own wrapper overloads. Engine suite unchanged at 1308/1308, full build clean, headless exit 0. Engine `a08d301`. ([WorkItems/185](185-ruleparticipant-struct.md))
- [x] 183 — Hero-join Subject-seat rule attribution (audit item 17, high severity). **Done 2026-07-08 (Option C).** A joined hero's relocated DEFENSIVE (Subject-seat) rules silently stopped firing (only Stealth/Fearless/Regeneration were gated + no Subject-seat site passed models), and host-carried ones over-applied to a hero that lacked them. Fixed both directions with one mechanism: `Condition.AllModelsHaveThisRule` added to all 12 unit-scoped defensive rules (Evasive/Melee Evasion/Artillery/Aircraft/Resistance/Protected/Shielded/Fortified/Ranged Shrouding/Darkborn-Defensive/Melee Shrouding/Counter-Attack) + a `RuleValidator` check enforcing it (army-load / supplement / OPR-import / fire-lint), and every Subject-seat dispatch site now threads the defender's living models (`HeroStatRules.LivingModels`, AnyOwner) so the hero's rule is collected, evaluated, and #163-traceable — the gate then governs whether it applies (sole-survivor hero fires). Grants now cover the joined hero; Resistance gained its 2+-vs-spells facet. Engine `4c2b86a`/`b271541`/`c926946` (all local); suite 1302->1308, build clean, headless exit 0. Deferred: Counter strike sequencing (#184); Fear/Fearless asymmetry stays #175; evaluator tuple->`RuleParticipant` struct cleanup (#185). ([WorkItems/183](183-hero-subject-seat-attribution.md))
- [x] 163 — Rule-trace log channel: verbosity toggle narrating every live hook evaluation (fired / condition failed / suppressed / ability offered) via the Debug log channel; `--trace-rules` + GUI Debug toggle. **Done 2026-07-08** — see detail file for trace points and decisions. ([WorkItems/163](163-rule-trace-channel.md))
- [x] 103 — Caster cast assist (friendly +1 / enemy −1 per token, within 18"). **Done 2026-06-30** — before the cast roll, other Caster units within 18" may spend their own spell tokens to sway it (friendly +1 each, enemy −1 each — the fuller OPR rule, chosen over friendly-only). Net modifier shifts the 4+ threshold as a decisive-roll threshold shift (clamped [1,6]); assist tokens spent regardless of outcome (like the cast cost). Count-style prompt (0..N) per Caster, friendly declaring first; `IsCaster` centralized onto `SpellTargeting`; AI + CLI-EOF both default to decline. Implemented as stage logic, **not** via the reserved `Casting_OnSpellAssistOffered` hook (core behaviour, no per-rule dispatch — recorded in the detail file). Engine `4ea14c3`. **GUI polish 2026-07-01** (after round-1 hand-verify): cast log now spells out the roll + assist math (`rolled 4, needed 3+ (base 4+, net +1 assist)`); canvas **click-to-target** for all unit picks (spell target, melee defender, #100 pre-attack — new `GuiUnitSelectionResolver`/`GuiCancellableUnitSelectionResolver`); and a dedicated `CastAssistRequest` + `GuiCastAssistResolver` drawing the assister **blue** (friendly +1) / **orange** (enemy −1) with a line to the caster + token count. Test aid `CasterCovenTest.fdgarmy`. Plus assist/result **text beats** (blue assist, orange hinder, blue/red cast result) + an em-dash→ASCII log fix. **GUI hand-verified + merged to master 2026-07-02** (engine `baefb44` / superproject `c83e422`); integrated cleanly with #151/#152 (engine suite 972/0, app 49/0, headless exit 0). (Opened as #094; renumbered — master's #094 is group-move coherency repair.) ([WorkItems/103](WorkItems/103-caster-assist.md))
- [x] 107 — Combined / "doubled up" squads, per the GDF "Combined" option. **Done 2026-07-02** (in the #156 Army Forge, per decision 8): two linked copies of the same roster unit (`BuilderUnit.CombinedWithId`, hero-join-style "Combine with" combo) — each copy buys upgrades under its own bounds (GDF "pay for both individually") and `ListCompiler` merges the pair into one play-time unit (models/cost summed, weapons profile-merged, rules deduped, "(Combined)" name, joined heroes follow); validation stays row-aligned and counts the pair as two copies for force-org caps. Engine `82ba5a6`, superproject `2d620f9`. **Reworked 2026-07-03** (branch `107-combined-unit-ux`) to the official Army Forge UX at the user's request: a **"Combined Unit" checkbox** that spawns/removes the linked copy (replacing the combine combo), a grouped **[Combined]** list card with a merged `[N units]` count, and whole-unit `Affects=All` upgrades **mirrored** across both copies (`[linked]`, charged on both — decision-8 "pay for both"); per-model upgrades stay independent. App-side only (`ArmyForgeScreen`); app suite 78/0; **GUI hand-verified 2026-07-03** (user confirmed in-window). Merged to master via `fa9ff2c`. See the #156 ledger. (Freeform-builder support out of scope — Forge is the authoring path.)
- [x] 084 — **(HIGH PRIORITY)** Stage-resolution thread safety (audit §5): `RequestMessageSender._pendingTaskAndResolvers` is mutated from engine + bus threads with no lock (→ `ConcurrentDictionary<TaskID,…>`), and its `TaskCompletionSource`s lack `RunContinuationsAsynchronously`, so replies resume engine stage code synchronously on the network read loop. **Done 2026-06-13** (with #086, engine commit `bbe9e19`) — see [WorkItems/084-086](WorkItems/084-086-stage-resolution-thread-safety.md). (Renumbered from a local 056.)
- [x] 085 — **(HIGH PRIORITY)** Tolerate unknown/duplicate `TaskID` replies (audit §5): `OnReceivedReplyMessage`/`OnReceivedErrorMessage` throw inside bus dispatch for unknown TaskIDs; with no dispatch try/catch a stray or duplicate reply can kill a connection. Log-and-ignore (idempotent), throw only in DEBUG. **Done 2026-06-13** — `ITextOutput` log-and-ignore, `#if DEBUG` throw; engine commit `846d955`. See [WorkItems/085](WorkItems/085-tolerate-unknown-taskid.md). (Renumbered from a local 057.)
- [x] 086 — **(HIGH PRIORITY)** Per-connection network write lock (audit §6): `CommandProtocol.WriteCommandAsync` makes three separate stream writes and `FDGHost`/`FDGClient` have no per-connection send lock, so concurrent sends (data sync + beats + requests + log relay) can interleave bytes inside a frame and corrupt the stream. **Done 2026-06-13** — per-connection `SemaphoreSlim(1,1)` (with #084, engine commit `bbe9e19`); see [WorkItems/084-086](WorkItems/084-086-stage-resolution-thread-safety.md). (Renumbered from a local 058.)
- [x] 088 — Route decision requests only to the target player's connection instead of broadcasting to all clients and filtering receiver-side (audit §5): saves bandwidth, stops baking in an open-information assumption, and stops shipping every player's decision payloads to everyone. **Done 2026-06-30** — new `IMessageBusHost.SendCommandToLocalAsync` (in-process dispatch) + `PlayerSlotManager.TryGetConnectionByPlayerID`; `RequestMessageSender` routes the payload via single-send (remote) or local dispatch (local), awaiting/resolved notifications stay broadcast. `StageTaskNotifyAwaitingMessage` now carries a `PlayerSlotInfo` value snapshot instead of a `DataBinding<PlayerSlotInfo>` (removes the deserialization-order coupling). Engine commit `0781b2c`; suite 947/0, full build clean, headless exit 0. See [WorkItems/088](WorkItems/088-route-requests-to-target.md). _(Renumbered from a local #059 on 2026-06-13 — collided with the branch-backed #059 JSON rule loader; numbers are never reused so the bare audit line moved.)_
- [x] 073 — **(HIGH PRIORITY)** Bus dispatch hardening (audit §6): message-bus dispatch had no exception isolation — one throwing handler propagated into `FDGHost.HandleClientAsync`'s catch-all and disconnected that client. **Done 2026-06-14** — per-handler try/catch in `MessageRegistrar.DispatchToHandlers` (unwraps `TargetInvocationException`, snapshot iteration), and `MessageSerializer.DeserializeMessage` now logs unknown wire types once per type via an injected `ITextOutput` (default `ConsoleTextOutput`, survives Release) instead of `Debug.WriteLine`. Engine `9330ed1`; 3 new `BusDispatchHardeningTests`; suite 514/0. ([WorkItems/073](WorkItems/073-bus-dispatch-hardening.md))
- [x] 074 — **(HIGH PRIORITY)** Fix the `_lastMessageConnectionID` race in `MessageBusHost_Networked` (audit §6). **Done 2026-06-21** — connection-aware handler registration (`RegisterForConnectionMessageEvent<T>`) + `DispatchToHandlers(message, ConnectionID?)` thread the source connection through dispatch; ambient `_lastMessageConnectionID`/`GetCurrentMessageConnectionID` deleted, so concurrent client read loops can't hand a handler the wrong connection. Engine `975d857` / merge `75f9f1d`; new `ConnectionMessageDispatchTests` (incl. concurrent no-cross-talk); suite 611/0. ([WorkItems/074](WorkItems/074-message-connection-id-race.md))
- [x] 078 — `CommandProtocol` read hardening (audit §13.25). **Done 2026-06-21** — inbound payload length clamped to `MAX_PAYLOAD_BYTES` (16 MB), rejected with `IOException` before the `ArrayPool.Rent`; added the missing `ConfigureAwait(false)` on the payload read; widened `Write/ReadCommandAsync` to `Stream` for testability. Engine `975d857` / merge `75f9f1d`; new `CommandProtocolTests`; suite 611/0. ([WorkItems/078](WorkItems/078-command-protocol-read-hardening.md))
- [x] 046 — `GetFirstBlockingHit` engine API: returns the closest `Blocking` terrain entry point along an (attacker, target) segment so overlays can draw a stub + marker; `IZone.GetFirstSegmentEntry` on circle/rect. 6 new `LineOfSightTests` cases, suite 135/135. ([WorkItems/046](WorkItems/046-los-first-blocking-hit.md)) — commit `d9e60fb`
- [x] 093 — Per-model special rules: the general reckoning. **Done 2026-07-05 — confirmed merged + green during the ledger audit** (was mislabeled `[~]` "awaiting GUI verify before merge"; the branch `093-per-model-special-rules` was in fact merged to master in both repos on 2026-07-02 via reconciliation 7 — the status line just never flipped). All agreed slices are live on master: foundational per-model dispatch (`EModelRuleScope` AnyOwner/AllOwners), defensive all-models gate (Stealth/Regeneration/Fearless via `Condition.AllModelsHaveThisRule`), per-model activated abilities (`GatherOffers`), and per-model movement budgets (`MovementActionContext.TryGetModelMoveBudget`). Audit verification: full engine suite 1138/0; `PerModelTest.fdgarmy` headless exit 0 with the joined-hero Vanguard firing live (`Line Squad used Vanguard`). Long-tail corners (`ERuleScope.Model` authoring, Impact(X) per-model, pooled-roll attribution, #104) durably deferred. ([WorkItems/093](WorkItems/093-per-model-special-rules.md))
- [x] 059 — JSON rule-definition loader. **Done 2026-07-05 — confirmed complete + merged during the ledger audit** (was mislabeled `[~]`). Workstreams 1-6 all shipped on master (branch `059-json-rule-loader` merged 2026-06-13): STJ `kind`-tagged rule tree, file->registry load with validate-at-load (`RuleValidator`), arg-arity validation, networked STJ army blob, scope-aware army-builder picker, legacy `SpecialRule`/`ISpecialRule_*` deleted. The one deferred piece (custom-rule authoring + standalone rules files) is separately tracked as the open **#087**. Symbols live on master (`RuleValidator`/`ArmyListRuleResolution`, 12 files each); suite 1138/0. ([WorkItems/059](WorkItems/059-json-rule-loader.md))
- [x] 026 — Unit special rules framework wiring. **Done 2026-07-05 — confirmed complete + merged during the ledger audit** (was mislabeled `[~]`). Army-list -> #042 rule-registry resolution shipped: `CoreRuleCatalog` + `RuleResolver.TryResolve`, `FDGServer` resolves each `UnitFileEntry.SpecialRules` and `AttachRuleDefinition`s it, so declared rules fire in live games; the legacy `GetRealSpecialRulesFromArmyList`/`SpecialRule` subsystem was deleted under #059. Live path is the #042 `ResolvedRule` dispatch. Symbols live on master; suite 1138/0. (No separate detail file.)
- [x] 091 — Morale core (Shaken / Rout at half strength) + shared `IsAtHalfStrength` predicate; melee `OnMoraleFailed` path. **Done 2026-07-05 — verified in live play** (user accepted live-run + test proof in lieu of the GUI banner eyeball). Across 11 headless games with a Fear/Fearless morale fixture (`VerifyMorale-091-092.fdgarmy`), both outcome branches fired live across multiple units - 13x `failed its morale test and is now Shaken` and 8x `failed its morale test at half strength and is Routed` - with zero exceptions; morale-cluster tests green (51/51, incl. `AssignMeleeMoralePenaltyStageTests`/`DetermineMeleeWinnerStageTests`). Un-eyeballed: the amber Shaken / red Rout `BannerBeat` render + the `UnitRoutedBeat` mass-death animation (presentation-only; headless can't show them). Engine `c99e20f`. ([WorkItems/091](WorkItems/091-morale-core.md))
- [x] 008 — Shaken activation behavior (idle the whole activation, can't seize/contest, recovers at end). **Done 2026-07-05 — verified in live play.** Headless runs show `Dreadguard/Zealots/Militia is Shaken - staying idle this activation and recovering.` firing for units Shaken at activation-start; `ShakenActivationTests` + `ShakenAlwaysFailsMoraleTests` green. Engine `797cf9c` + `32d70d2`. ([WorkItems/091](WorkItems/091-morale-core.md))
- [x] 009 — Wound-driven half-strength morale outside melee (shooting + dangerous terrain -> test -> Shaken on fail). **Done 2026-07-05 — verified in live play** (mechanics). `Resolving ranged morale` runs live each shooting activation; `ResolveRangedMoraleStageTests` + `DangerousTerrainMoraleTests` green. The wound-driven Shaken emits only a `BannerBeat` (no text log), so its on-screen amber banner is the un-eyeballed presentation bit. Engine `ff59994` + `ff28ccb`. ([WorkItems/091](WorkItems/091-morale-core.md))
- [x] 020 — Fatigue (charge/strike-back -> Fatigued -> hits only on unmodified 6s in later melee that round; Shaken counts as fatigued). **Done 2026-07-05 — verified in live play.** Headless runs show `X is Fatigued`, `X struck back and is Fatigued`, and the effect firing (`X is fatigued - hits only on unmodified 6s in melee`); `FatigueTests` green. Un-eyeballed: the Fatigued token chip render. Engine `07ffdb9`. ([WorkItems/020](WorkItems/020-fatigue.md))
- [x] 021 — Fear(X) + Fearless (+ generic morale-roll-modifier plumbing). **Done 2026-07-05 — verified in live play.** Headless runs show `Dreadguard counts as +2 wounds in melee (Fear)` folding the who-won-melee check, and `Zealots's Fearless triggered a Morale re-roll` -> `passed the Fearless re-roll (4+)` (plus the fail-through case where the reroll also misses -> Shaken); `FearRuleIntegrationTests` + `FearlessRuleIntegrationTests` green. Engine `d75c1a3` + `f73ef5c`. ([WorkItems/021](WorkItems/021-fear-fearless.md))
- [x] 092 — Decisive dice rolls (`IDiceRoller.RollDecisive` so single binary rolls resolve a real outcome instead of auto-failing under the probabilistic roller). **Done 2026-07-05 — verified in live play** (substrate; no independent GUI surface). Confirmed by proxy: under the default roller the whole cluster produces real varied pass/fail (`Morale test failed/passed (needed 5)`, Fearless rerolls) rather than universal auto-fail; `DecisiveRollTests` green. Engine `4459070`. ([WorkItems/092](WorkItems/092-decisive-rolls.md))
- [x] 027 — Weapon-scoped special rules. **Done 2026-07-05 — verified end-to-end in a live game** (user opted to accept live-resolver proof in lieu of the GUI eyeball). Weapons carry #042 `ResolvedRule`s resolved from `WeaponFileEntry.SpecialRules` at army load with `SpecialRuleDefinition.Scope` enforcement (misattached rules warn + skip); dispatch is per-weapon through the fire pipeline + defender melee weapons (Counter) + `SightRuleQueries`; legacy `ISpecialRule_Weapon` deleted (engine-complete 2026-06-11, suite 396/0). **Verification:** a hand-authored fixture (`VerifyRules-027-028.fdgarmy`) played headless shows per-weapon labels on the correct weapons only (`Sniper Rifle ... (Takedown ignores cover and line of sight)`, `Heavy Rifle ... (Blast(3) ignores cover)`, Death Ray = no sight label), weapon-scoped dispatch firing (`Snipers's Sniper Rifle's Takedown re-scoped...`, `Blasters's Heavy Rifle's Blast(3) multiplied hits by 3`), and defender Counter striking first when charged (`Blasters counters the charge - strikes first!`, `Blade's Counter rolled -1 impact dice`). The GUI resolvers consume the identical `SightRuleLabel`/`UnselectableReason` strings; only the ImGui visual render was not eyeballed. ([WorkItems/027](WorkItems/027-weapon-special-rules.md))
- [x] 028 — Deadly weapon priority (resolve first; wounds don't carry across models). **Done 2026-07-05 — verified end-to-end in a live game** (user opted to accept live-resolver proof in lieu of the GUI eyeball). Merged to master 2026-06-21 (submodule `3b81e2e`, superproject `ba4773f`); the ordering half added `WoundPriorityQueries.MustResolveFirst` (capability-based) gating non-Deadly weapons in the ranged + melee pickers until Deadly is spent, via the `UnselectableReason`/`InvalidOption` markers all six resolvers honor (no app-side change); the wound-no-carry-over half was pre-existing (`AssignWoundsStage.ConfineToClumps`). 6 tests; suite 633/0. **Verification:** the `VerifyRules-027-028.fdgarmy` fixture (Blasters carry Death Ray = Deadly(3) alongside Heavy Rifle) played headless shows the full lifecycle through the real CLI resolver - `Heavy Rifle ... [unavailable: Must fire Deadly weapons first.]` on all targets while Death Ray is selectable, then after firing Death Ray (`Deadly(3) multiplied wounds by 3`) the gate lifts (Heavy Rifle selectable, no tag). GUI resolvers consume the identical `UnselectableReason` string. ([WorkItems/028](WorkItems/028-deadly-weapon-priority.md))
- [x] 105 — In-game chat + log-panel relocation. **Done 2026-07-05** (branch `105-chat-and-log-layout`). Chat is now first-class and the layout is intentional: a full-width table with a **collapsible bottom console** (`RaylibRenderer.DrawBottomConsole`) whose **Log** and **Chat** are independent toggles (Chat on the left) that merge into one arrival-ordered column when both are on (shared `LogEntry.Sequence`, two-pointer merge); chat lives in its own store (`GuiPlayerMessageUI.ChatLog`, not the engine log) with **sender-palette colours** (name-keyed from the roster), an unread marker, and a **Global/Team channel toggle**. **Host self-echo de-dup fixed at the source** (engine): `NetworkPlayerController.SendPlayerMessage`/`SendLogMessage` now send per-client via `SendCommandToSingleAsync(ConnectionID)` instead of `SendCommandToAllAsync` — the broadcast had also dispatched in-process on the host, so every line showed twice (LocalPlayerController direct + loopback) and N clients multi-sent; new `InGameChatTests` case pins it. Also landed on this branch: GUI polish (blue theme dial-in, scoreboard panel removed, resolver dialogs right-aligned, table auto-fit +15%, measurement clears on release). Engine 1138/0, app 83/0, full build + headless smoke clean. Deferred as optional (not part of the done bar): displayed timestamps, new-line fade-in. Follows #077. ([WorkItems/105](WorkItems/105-chat-and-log-layout.md))
- [x] 070 — Save/load rename fragility (audit §9). **Done 2026-07-03** (engine on master `9b78d5b`). Decoupled the save format from C# type names so renaming/moving a persisted type no longer breaks saves — **comprehensively** (both rename surfaces, with the user): a single stable-ID `SaveTypeRegistry` (`Type ↔ "model"/"unit"/"baseShape.rect"/"clearTrigger.roundEnd"/…`) drives (a) the top-level type-map fingerprint (`SavedTypeEntry.FullName`→`TypeId`, resolve registry-first with a `Type.FullName` fallback) and (b) a `StableTypeSerializationBinder` on the store's JSON settings for the polymorphic `$type` payloads Newtonsoft records under `TypeNameHandling.Auto` (base shapes, token payloads, token clear-triggers, terrain zones incl. the `List<IZone>` composite wrapper). Purely additive (unregistered types fall back to FullName); `CurrentVersion` 1→2; **no `NetworkProtocol.Version` bump** (nothing distributed — user's call). `GetTypeMapFingerprint` (#075 build-skew hash) left FullName-based (orthogonal). Reflection pins (`SaveTypeRegistryTests`) fail CI if a new store type / base shape / payload / clear-trigger / zone ships without an ID. Engine suite **1124/0** (post-rebase onto master), full build clean, headless exit 0. Engine-only. The STJ rule-attachment blob is a separate serialization surface, already `kind`-tagged — tracked as **#160**. ([WorkItems/070](WorkItems/070-save-load-rename-fragility.md))
- [x] 039 — `GameDataStore.CreateFromTypeMap`. **Closed 2026-07-03 — stale; already implemented.** The "NotImplementedException" note was out of date: both overloads are fully implemented (finished as #052 Phase 2, exercised end-to-end by `GameSaveLoadTests`). Surfaced while scoping #070. No code change. ([WorkItems/052](WorkItems/052-save-load.md))
- [x] 154 — Destroyed-transport morale test. **Closed 2026-07-03 — not a gap; won't implement (decided with the user).** The transport rule as quoted in #035 ("units inside must take a dangerous terrain test, **are Shaken**, and must be placed within 6\"") makes occupants **unconditionally** Shaken — which #035 slice E already does (`TransportUtilities.ApplySpilloutEffects` adds the `Shaken` token directly). #154 had proposed a **morale test** instead (Shaken only on fail); but that would *replace* the auto-Shaken rather than add to it (a test whose only outcome is Shaken can't meaningfully coexist with a guaranteed Shaken — and `MoraleUtilities.TakeMoraleTest` auto-fails an already-Shaken unit), so it would diverge from the quoted rule text. Kept the auto-Shaken as authoritative. No code change. ([WorkItems/154](WorkItems/154-destroyed-transport-morale.md))
- [x] 069 — Clean removal of the legacy `Special Rules/` system (audit §8). **Done 2026-07-03.** The audit's 2026-06-10 premise had largely decayed: **#059 already deleted** the `SpecialRule`/`ISpecialRule_*` classes, the `IUnit`/`IModel`/`UnitData`/`ModelData` `SpecialRules` properties, `GetRealSpecialRulesFromArmyList`, and `Regeneration.cs`. Re-surveyed the whole repo against every audited symbol; removed the remaining residue. **Deleted 7 files:** three fully-dead corpse context files (`ISingleAttackContext.cs`, `SingleRangedAttackContext.cs`, `SingleMeleeAttacKContext.cs` — all 100% inside `/* */`, referencing the #059-deleted types); the `ICombatEffect`/`ICombatEffectsSink` interfaces (the sink was implemented only by `CombatStage` and its effect list was never populated); and `RuleHookBus`/`IRuleHookBus` (the audit's named vestige — a stub whose `Dispatch` always returned empty, reachable only from the test harness). **Simplified `CombatStage`** to its live `RunStage -> AddResult -> Finish` path (dropped the always-empty pre/post-execute gather); **retired the test harness's vestigial `Bus`** (`Fire()` keeps its real `UnitDestroyedContext` token-cleanup). Coordination precondition satisfied — every special-rules branch is 0 commits ahead of master (all merged). No new tests (pure dead-code removal, no new behavior); engine **1113/1113** (**1124/1124** after merging onto master's #070/Rending/Counter work), full build clean, headless exit 0. Engine `19e4b83`, merged to master via `22ce29e` (branch `069-remove-legacy-special-rules`). No "Awaiting verification" hold (no runtime behavior change to eyeball, per #024/#079 precedent). Leftover `<c>ISpecialRule_Attacker</c>` note in `ERuleSeat.cs` kept as accurate design lineage (a comment, not a `cref`). ([WorkItems/069](WorkItems/069-remove-legacy-special-rules.md))
- [x] 155 — Movement GUI: flag difficult/dangerous terrain a move will cross. **Done — GUI-verified + merged to master 2026-07-03.** Per user sign-off the two hazards differ: **Difficult is enforced in the preview** (the single/group ghost clamps so a move that would bust the 6" cap can never be drawn — capped while crossing, or stopped just short of the edge when entry is unaffordable; crossing lines dotted gray + two panel warnings) and **Dangerous is advisory** (doubled red `!` badge + solid-red path lines for every crossing model + panel line). Also added an **enemy/other-unit distance clamp** (the ghost stops just short of contact instead of only turning red), honoring `CanMoveThroughEnemies`. Engine (`75a593e`): `SweptBaseGeometry.MaxTravelBeforeZoneIntersection`, `BaseShapeGeometry.MaxTravelBeforeBaseCollision`, `MovementUtilities.DoesPathCrossDifficultTerrain` + `ClampTravelForDifficultTerrain{,Detailed}` (`EDifficultClampKind`); +20 tests, suite 1113 green. App (`799384c`/`6e9ce6a`): `GuiDefineMovementResolver` single + group, `GroupFormationUtilities.LargestFeasibleScale`. Opened 2026-07-02 (as #153; renumbered 153→155 in reconciliation 7 — master's #153 is shape-owned pairwise geometry). ([WorkItems/155](WorkItems/155-movement-terrain-indication.md))
- [x] 153 — Shape-owned pairwise geometry. **Done 2026-07-02** (engine `2c4291c`, landed with #150's collision-unification pass). Resolved via a **rounded-convex-hull footprint** rather than the originally-scoped GJK/support-function seam — same goal (shape-owned pairwise geometry, no shape-pair switch, a new shape = one method) with less code and obvious correctness for our shapes. Each `IBaseShape` implements `Footprint(centre, facing)` → `BaseFootprint` (convex-hull corners + Minkowski rounding: a circle is one rounded point, a rectangle four oriented corners). `BaseShapeGeometry.SurfaceGap2D`/`AreColliding` consume only that — one hull-vs-hull routine (SAT overlap/penetration + nearest-feature separation), zero branching on game-shape type; the only internal distinction is point-hull vs polygon-hull (a fixed geometric property). Circle-vs-circle stays byte-identical. GJK remains a valid future swap behind the same API if a curved base ever needs it — call sites wouldn't change. ([WorkItems/153](WorkItems/153-shape-pairwise-geometry.md))
- [x] 151 — Token display metadata + visual taxonomy. **Done — GUI-verified + merged to master 2026-06-30** (engine `fa71404`, superproject `3967981`). A small colored shape per token under the unit name / over each model, **color = valence** (bearer-relative good/bad) and **shape = identity** (stable FNV hash → curated palette; authored `ETokenColor`/`ETokenShape` overrides). Hover lists the unit's tokens *and* its innate special rules with plain-language descriptions; dev `T` reveals invisible bookkeeping tokens. **Step 1 (engine):** `EValence`/`ETokenProminence`/`ETokenColor`/`ETokenShape`, `TokenDefinitionCatalog` (single source of truth for token types), `Valence`+`Description` on every core rule, resolution chains + `Describe()` → `TokenDisplayInfo`. **Step 2 (app):** `TokenChipRenderer` + `TableTooltipOverlay`. Follow-ups in the detail file (custom-rule token metadata self-describe → #087; per-chip hover; strict two-tier layout for a future unit panel; live game resolver instead of `CoreRuleCatalog`). ([WorkItems/151](WorkItems/151-token-display-metadata.md))
- [x] 152 — Unit health bars on the table. **Done — GUI-verified + merged to master 2026-06-30** (superproject `3967981`). A Civ-style bar on each **damaged** unit (hidden at full strength), drawn above the unit name. Reflects **total** remaining/max wounds summed across models as granular floats (a `Tough` model that takes a wound but lives still drains it; deterministic-path decimals honoured), with an overkill-safe per-model `max(0,…)` clamp; colour snaps green→yellow at exactly 50% (the half-strength morale cliff). App-side (`HealthBarRenderer` + `TableTooltipOverlay`). ([WorkItems/152](WorkItems/152-health-bars.md))

- [x] 032 — Weapon rules (AP, Rending, Blast, Reliable, Indirect, Takedown, Unstoppable, Bane, **Limited**). **Done 2026-06-30.** AP/Rending/Blast/Reliable/Indirect/Takedown/Unstoppable/Bane landed via #042; **Limited** ("once per game") completed this item — **and with it all 33 GF v3.5.1 core special rules are implemented.** Limited = a weapon-scoped marker rule (`CoreRuleCatalog.Limited`, no hooks) read via `LimitedRules`. Spent-state is a per-MODEL `LimitedSpent` token (new `TokenType` + `TokenPayload.WeaponName`) keyed by weapon name, so two carriers spend independently, a model can hold two different Limited weapons, and casualties drop the mark; `ManualOnly` clear → lasts the game; count = times fired (Limited(X)-ready). `ChooseRangedAttackStage` marks it spent on its carriers when chosen (commit-to-fire), excludes it at the offer seam once every living carrier has fired (mirrors Deadly gating), and grays Shoot when the only ranged weapon is a spent Limited one. Engine-only (no app change). `All` → 112. Tests: `LimitedRulesTests` + 2 `ChooseRangedAttackStage` integration tests; suite 945/0, full build, headless 0. NOTE: same-name weapons fire together (a game rule), so a unit's copies spend in one firing — the model-binding is for clean casualty handling + Limited(X), not per-model independent firing.

- [x] 030 — Combat-modifier rules: Furious, Impact, Counter, Thrust, Surge, Relentless. **Done** (landed via the #042 catalog; verified against GF Core Rules v3.5.1 on 2026-06-30). All six catalogued + integration-tested. Counter carries **both** v3.5.1 facets (StrikeFirst when charged + `ReduceImpactDicePerModel`). The Furious "only when charging" gate (was #051) is in place.
- [x] 051 — Furious charge gate. **Done** (verified 2026-06-30). Furious's hook now gates on `Condition.IsCharging()` (And(IsMelee, IsCharging, UnmodifiedRollEquals(6))), so the extra-hit-on-6 fires only on a melee **charge**, matching the v3.5.1 text — no longer over-applying to any melee attack. (The `IsCharging` condition + melee charge-state threading landed with the melee subsystem since this was deferred.)
- [x] 031 — Defense/unit rules: Tough, Regeneration, Stealth, Fear, Fearless, Hero, Scout, Ambush. **Done** (landed via #042 catalog + #006 Hero-join + #091 morale-core; verified against v3.5.1 on 2026-06-30). All catalogued/built and integration-tested.

- [x] 029 — Movement-modifier rules: Fast, Slow, VeryFast, Immobile, Strider, Aircraft, Flying. **Done 2026-06-30.** All implemented/catalogued/tested: self move-distance (Fast/Slow/… #042, Immobile #100); terrain ignore (**Strider** #102, **Flying** full — `ETerrainIgnoreScope` + impassible flag + Dangerous-roll skip + move-through-units); target-perspective debuffs (per-target charge mechanic via `Movement_OnChargeDeclared`/`EffectiveChargeDistanceAgainst`, powering **Melee Shrouding** + **Darkborn (Off/Def)**); and **Aircraft** all facets (−12 range/−1 hit, terrain/unit ignore, can't-seize, can't-be-charged, deploy-first, and the forced straight-line movement mode: Advance-only + off-table→reserve→edge-redeploy). `All` → 111. **2026-07-01 aircraft UX pass** (hand-verification findings): heading = the facing placed at deploy (auto-aim + `AircraftHeadingSet` token removed — no more turn-on-activation), shoot allowed after the forced move but gated off when flown off-table, the 30/33/36 menu replaced by a continuous visual 30–36" segment move (`AircraftAdvanceRequest` + GUI/CLI/AI resolvers, fly-off confirm modal), and the redeploy now genuinely requires touching a table edge (`MustTouchTableEdge`, rule text verified vs the OPR wiki) — retiring the earlier heading-auto-aim / whole-table-redeploy simplifications. Remaining faithful simplification: reserve-as-activation-end, unit-level per-model gating. ([WorkItems/029](WorkItems/029-movement-modifier-rules.md))

- [x] 102 — Movement & range modifier validation threading. **Done 2026-06-29.** **Strider** (difficult-cap waiver, threaded through `ValidatePaths` across both repos); **Increased Shooting Range** (+6) and **Ranged Shrouding** (−6, "min 6\"" floor) via a new `Shooting_OnRangeCheck` hook + `RangeRuleQueries.EffectiveRange` folded into `ChooseRangedAttackStage`'s authoritative range check; **offensive Darkborn** (+3 range / +3 charge — charge half reuses the live `MovementBonus(Charge)` seam, no new primitive); and the **movement-overlay shooting preview** (`DefineMovementPathRequest.WeaponRangeOverrides` precomputed in `DefinePathStage`, read by "Show targeting"). All catalogued (+auras), fixing their Army-Creator-picker absence; `All` 100→106. Sole follow-up — **defensive Darkborn** (enemy −range/−charge, Subject) — moved to **#029** (needs the per-target charge-distance mechanic Aircraft also needs). ([WorkItems/102](WorkItems/102-movement-range-validation-threading.md))
- [x] 036 — Server readiness handshake. **Closed 2026-06-28 — already functionally implemented; stale comments only.** `FDGServer.LaunchStateMachineOnceReady` already `await`s `PlayerSlotManager.WaitUntilAllSlotsReady()`, which blocks on each slot's `IPlayerController.WaitUntilReadyAsync()` — network player completes on the client's `PostLaunchPlayerReadyMessage` (genuinely sent by `FDGGame_AsClient.AssignInterfaces`), local on resolver assignment, AI immediately. There is no `Task.Delay(500)`; the TODO + "half a second" comments predated the real handshake. Replaced the stale comments on both launch paths + added `PlayerReadinessTests` (3 tests) pinning the `WaitUntilAllSlotsReady` contract. Timeout / never-ready safety net deferred (possible follow-up, overlaps #076). Suite 843/0, build clean, headless exit 0. ([WorkItems/036](WorkItems/036-server-readiness-handshake.md))
- [x] 037 — Replace non-concurrent collections in `FDGHost`. **Done 2026-06-28.** `_connectedClients` is now a `ConcurrentDictionary<ConnectionID, ClientConnection>`; all five `lock (_connectedClients)` blocks removed (`TryAdd`/`TryRemove`/`TryGetValue`; `Values` snapshot for broadcast + `Stop`). Per-connection write serialization (#086 `ClientConnection.WriteLock`) untouched. Behavior-preserving refactor; direct socket-level test left to #065. Suite 843/0, build clean, headless exit 0. ([WorkItems/037](WorkItems/037-fdghost-concurrent-collections.md))
- [x] 038 — Resolve `LobbyViewModel_Host` `NotImplementedException` paths. **Closed 2026-06-28 — live paths already gone; dead-code cleanup.** The two live NIEs (old `:288`/`:400`) had grown into real lobby code; the only remaining `NotImplementedException` sat in a `/* … */` block (a commented-out `GetPlayerSlots` using a 2-arg `PlayerSlot` ctor that no longer exists). Deleted the dead block (matches #007/#014 precedent). Suite 843/0, build clean, headless exit 0. ([WorkItems/038](WorkItems/038-lobby-viewmodel-nie.md))
- [x] 109 — Single-model Tough unit auto-killed by a sub-lethal hit. `AssignWoundsStage`'s "single living model → auto-resolve" branch assigned the model's **full remaining health** (`defenderRemainingWounds`) instead of the wounds actually dealt (`totalWoundsDealt`) — so a lone Tough(12) monster shot for 3 wounds got "Applying 12 wounds" and died. Only reachable for single-model units with >1 wound (the kill case is handled earlier; a 1-wound lone model never enters the branch), which hid it. One-line fix + regression test (`WoundRuleIntegrationTests.SingleToughModel_SubLethalHit_AssignsOnlyWoundsDealt`); engine suite 840/0, headless smoke exit 0. ([WorkItems/109](WorkItems/109-single-model-tough-wound-overapply.md))
- [x] 098 — Transport capacity-by-Tough mixed-unit corner. **Done 2026-06-25 — closed with the deferred tests; later corrected.** Added 6 mixed-case tests to `TransportUtilitiesTests` (joined hero embarks under cap-6; hero-over-cap and grunt-over-cap rejected; non-Hero Tough(6) can't borrow the hero cap; solo hero embarks; space-cost sums). The audit's "already correct, no production change" verdict was **wrong on Hero space cost** — a Hero occupies **1** space, not 3 (the rule's worked example: 10 grunts + a Tough(3) Hero = 11 spaces). Fixed under **#035** (`GetModelSpaceCost` made hero-aware; submodule `d736011`); these tests' hero-cost expectations were corrected with it. Final suite 815/0; headless smoke exit 0. ([WorkItems/098](WorkItems/098-transport-tough-mixed-unit.md))
- [x] 101 — Keyword-buff / aura dispatch bridge: make `Effect.AddRule`/`Effect.Aura` grants actually fire. **DONE 2026-06-25.** `RuleEvaluator` projects granted `RuleGrant` tokens by name and fires them like innate rules (one-shot grants consume on occurrence); **auras land at unit creation** (`UnitCreationRules.Apply` now applies the `Lifecycle_OnUnitCreated` grant op it used to drop) and project **unit-wide, no radius** (GDF auras apply only to the bearer's own unit); and grants **survive a save/load resume** (tokens round-trip on the `[JsonProperty]` container — the resume `FDGServer` ctor rebuilds the resolver that reads them back via the new `BuildRuleResolver`, no re-grant). Engine `9781b01` + `852a99e`; suite 766/0, build clean, headless exit 0. The one no-consumer edge (single-unit-`Evaluate` NextTrigger consume) is spun off to **#104**; conferred-rule *implementations* are **#034**. (Opened as #095; renumbered — #095 was taken on another instance.) ([WorkItems/101](WorkItems/101-keyword-buff-bridge.md))
- [x] 022 — Vertical melee range handling. **Done 2026-06-25.** The melee-engagement cylinder (2" horizontal AND 4" vertical, GF v3.5.1) is now a single definition in `MeleeRangeUtilities.AreModelsInMeleeRange`, consumed by all three gates via the new unit-level `AreUnitsInMeleeRange`: charge availability (`ChooseActionStage.GetCanCharge`) and defender eligibility (`ChooseMeleeDefenderStage`) — both formerly horizontal-only — plus the #017 strike check (now delegates to it). A unit within 2" horizontally but beyond 4" vertically is no longer engageable/offered, so the gates can't disagree. 7 new tests (`VerticalMeleeRangeTests`); suite 768/0; headless exit 0. **No-op at runtime today** (no vertical model movement exists yet — all `Position.y == 0`), so no "Awaiting verification" hold; it's a consistency fix that becomes load-bearing when vertical movement lands. ([WorkItems/022](WorkItems/022-vertical-melee-range.md))
- [x] 077 — In-game chat: **finished** (audit §13.20). **Done 2026-06-25.** The relay was already fully wired (`FDGServer` builds `LogAndChatMessageRelayer`; both game types build `LogChatMessageEndpoint`); only the host-side `NetworkPlayerController` handler for `NetworkPlayerSubmitChatMessage` was commented out, so network-player in-game chat vanished. Re-enabled it — **plus** a `message.PlayerID != ID` sender filter the dead code lacked (every network player's controller is registered for the message, so without the filter 2+ players would each re-raise/re-broadcast the same chat, misattributed). 2 new tests (`InGameChatTests`); suite 782/0. **Follow-up (2026-06-25): added a minimal in-game chat GUI** — closing the relay revealed the GUI had no chat surface at all (it launched with a `CliPlayerMessageUI` console stub). New `GuiPlayerMessageUI` (received chat → the side `GameLog`) + a bottom-of-window input bar (`RaylibRenderer.DrawChatInput`), wired through `OnGameLaunched`; app-side only, 3 new app tests (`GuiPlayerMessageUITests`). **Awaiting GUI hand-verification** in the running window. Engine `293b29c`. ([WorkItems/077](WorkItems/077-ingame-chat.md))
- [x] 076 — Disconnect lifecycle (audit §6/§10) — **fail + surface scope; AI takeover deferred.** **Done 2026-06-25.** A dropped player's pending decision requests no longer hang the game: `IMessageBusHost.OnClientDisconnected` (forwarded by `MessageBusHost_Networked` from the network host) lets `RequestMessageSender` react; it maps the dropped `ConnectionID`→`PlayerID` (new `NetworkPlayerController.ConnectionID` + `PlayerSlotManager.TryGetPlayerIDByConnection`) and `FailPendingRequestsForPlayer` faults that player's tasks with a typed `PlayerDisconnectedException` (atomic `TryRemove` claim so a racing reply still wins once; clears the task from clients' outstanding-task UI; logs the count). **Deferred:** AI takeover of the slot, a `RequestDecision` timeout (separate safety net), and the socket-level loopback test (#065). 4 new tests (`DisconnectLifecycleTests`); suite 780/0, build clean, headless exit 0. Engine `f4e6934`. ([WorkItems/076](WorkItems/076-disconnect-lifecycle.md))
- [x] 075 — Version / type-map handshake on client join + enums-as-strings (audit §6). **Done 2026-06-25.** New `NetworkProtocol` (`Version` const + cached `LocalTypeMapHash` = SHA-256 of the ordered store type-map `FullName`s + `TryValidateJoin`). The client greeting now carries `(ProtocolVersion, TypeMapHash)` (primitives, so they survive a format mismatch); the host validates at the top of `OnReceiveNewClientGreeting` and refuses a mismatch with a single-connection `LobbyJoinRejectedMessage` **before** roster add. **Host-validates** (not the audit's literal client-validates) so there's no half-joined client; `ClientModal` awaits `LobbyViewModel_Client.JoinResultTask` (8 s timeout) and shows the reason in its status line, disconnecting on failure. `StringEnumConverter` added to the central `GetJsonSettings()` (reads ints too → old saves still load, no save-version bump). 9 new tests (`NetworkProtocolTests`); suite 776/0, build clean, headless exit 0. Deferred: message-registry hashing (the `Version` counter covers it) + end-to-end loopback dispatch test (#065). **Awaiting GUI hand-verification** of a real cross-build rejection. Engine `ce06a2b`. ([WorkItems/075](WorkItems/075-version-typemap-handshake.md))
- [x] 082 — AI & player-controller lifecycle nits (audit §10). **Done 2026-06-25.** (a) `AiYesNoResolver` no longer answers a blanket `true` — `YesNoRequest` carries an explicit `DefaultAnswer` (renamed from `AiPrefersYes` to decouple it from the AI resolver so other no-live-player paths can reuse it; a `[JsonProperty]` private-setter defaulting to `true`, kept out of the `[JsonConstructor]` so a missing member can't silently flip the default to "no"); all 5 call sites set it by name (`defaultAnswer: true`); the CLI `YesNoResolver` EOF fallback now reads `DefaultAnswer` too (first additional consumer, app suite 29/0). (b) `LocalPlayerController.EnsureMessageSubscription` is idempotent (`-=` then `+=`) and re-runs on resolver assignment, closing the null-then-non-null race + double-subscribe. (c) `NetworkPlayerController` ready handler guards on `!IsReady` so duplicate ready messages don't double-fire `OnReadyStateChanged`. 6 new tests (`AiControllerLifecycleTests`); suite 767/0, build clean, headless exit 0. No GUI hand-verification needed (no new visible behavior). Engine `0b05843`. First of the `082-network-robustness` cluster (#082/#075/#076/#077). ([WorkItems/082](WorkItems/082-controller-lifecycle.md))
- [x] 023 — Tough wound-priority (wound the same Tough model until killed; heroes last). **Done 2026-06-21 by reconciliation — no new code.** Core ordering (mandatory pre-assignment to already-wounded models + finish-before-next + prompt suppression when there's no choice) was implemented in the `AssignWoundsResults` ctor; the illegal-split backstop landed under #024; and the one deferred clause, **"heroes last,"** landed under #006 (Hero), which supplied the hero-model identity it needed and implemented the ordering — the two compose in the same constructor. Verified on master: suite 633/0 (`ToughWoundOrderingRuleIntegrationTests`, `HeroWoundAssignmentTests`), build clean, headless exit 0. Only the rare "which wounded model absorbs the shortfall" sub-choice stays deferred (documented). ([WorkItems/023](WorkItems/023-tough-wound-priority.md))
- [x] 015 — Attack-count modifiers in shooting flow. **Done — merged to master 2026-06-21.** Attack count relocated into the renamed `DetermineHitRollStage` (was `DetermineHitRollNeededStage`), exposed as `DetermineHitRollResults.AttackCount` (the attack-count modifier-fold point, mirror of the hit-roll fold); `RollToHitStage` queries it instead of recomputing. Shared by both ranged (`FireStage`) and melee (`SwingMeleeWeaponStage`). Modifier *producer* deferred — only the seam exists; build it when a rule (e.g. Rapid Fire) needs it. No "Awaiting verification" hold: no live consumer yet, so no in-app behavior to eyeball (seam + integration test only). Engine `36e3631`, in master pin `5efe827`; suite 627/0; headless smoke exit 0 (`out of N total attacks` confirms `AttackCount` flows). ([WorkItems/015-016](WorkItems/015-016-shooting-roll-seams.md))
- [x] 016 — Hit→wound effect propagation. **Done — merged to master 2026-06-21.** Per-hit `SuccessfulHitInfo.SaveModifier` (default 0) applied per hit group in `DetermineSaveRollsNeededStage`, stacking with the unit-wide `RollToHitResults.SaveModifier` (same sign convention). Rending→per-hit migration deferred (separate slice). No "Awaiting verification" hold: data plumbing with no live consumer yet (seam + `PerHitSaveEffectIntegrationTests` only). Engine `1b16dc6`, in master pin `5efe827`; suite 627/0; headless smoke exit 0. ([WorkItems/015-016](WorkItems/015-016-shooting-roll-seams.md))
- [x] 024 — Validate wound splits in `AssignWoundsResults`. **Done 2026-06-21.** The illegal split the old TODO described was latent — reachable only via `TryRemoveWounds`, which had zero callers, and the pure-add path can't split (greedy fill makes a partial assignment exhaust the pool and end the loop). `TryAddWounds` now refuses to *start* a fresh model while another is alive and mid-fill (mirrored in `CanAssignWoundTo` so the UI gray-out stays honest), and the unused `TryRemoveWounds` is deleted. 3 new `ToughWoundOrderingRuleIntegrationTests`; suite 623/0; build clean; headless smoke exit 0. No "Awaiting verification" hold — the guard is unreachable through the shipped UI, so there's no new in-app behavior to eyeball (correctness backstop + dead-code removal). Submodule `b97c9c5`. ([WorkItems/024](WorkItems/024-validate-wound-splits.md))
- [x] 061 — Defuse the store-capacity time bomb (audit §2). **Done 2026-06-21** (Option B — growable): `ComponentStore<T>` now doubles its backing arrays on demand instead of throwing once full, so capacity is an initial hint, not a ceiling — two ~40-model armies no longer crash at army creation. `DataReference` value identity means a grow keeps every existing reference valid; `Create` grows when full, `CreateFromReference` grows to fit a foreign (save/network-replay) index, `IsValid` stays strict. Save/wire compat preserved (capacities are recorded per-type in saves; host/client both `GetDefault()` and grow symmetrically). Folded in the #062 dead-code deletions in the same files (`_setValueCache`, the unused parameterless ctor, the builder's dead capacity-normalization). New `GameDataStoreTests` (also satisfies part of #063 for the store). Engine `f467933`; suite 620/0. ([WorkItems/061](WorkItems/061-store-capacity.md))
- [x] 040 — Post-game navigation back to main menu in GUI mode. On game-end a centered "Game Over" card shows the winner (display name, not the PlayerID GUID) with a **Return to Main Menu** button — the player must click to leave (no auto-return); the click runs `ExitGame()` teardown + navigates. Signal: `ILobbyViewModel.OnGameEnded` (host forwards `FDGServer.OnGameEnded`; networked client raises it from a new broadcast `GameEndedMessage`) → `RaylibRenderer.ShowGameOver`. Host/local **and** networked client covered. `VictoryCalculationStage` now reports the winner's `IPlayerSlotInfo.Name` (fallback "A player"). Tests: `GameEndedMessageTests` + updated `VictoryCalculationStageTests`. GUI hand-verified 2026-06-21. Merged to master: submodule `eced7a8`, superproject `16b5c8c`. ([WorkItems/040](WorkItems/040-postgame-nav.md))
- [x] 010 — Custom actions branch in `ChooseActionStage`: the generic seam letting a special rule contribute actions. A rule carrying an `ActivatedAbility` at `Activation_OnActionChoice` surfaces as a selectable action (one per rule, collision-guarded); choosing it routes to the new `CustomActionStage`, which resolves it against the bearer, pays the cost, and loops back **without** consuming the move/shoot (layered overlay). New `ActionChoiceContext`; `UnitActionContext` carries the chosen offer; custom actions ride the existing `StringSelectionRequest` resolvers (no new GUI). Caster is the motivating example but **not** built here — first live consumer is #033/#034. Engine `d56167b`; suite 615/0; headless exit 0. ([WorkItems/010](WorkItems/010-custom-actions.md))
- [x] 006 — Hero joins unit + takes morale on behalf of unit. Merge hero model into host unit + `HeroAttachment` metadata. Slices A–F all on master (`006-hero` + `006-hero-slice-f`, merged): join + eligibility (single-model ≤Tough(6), multi-model host, no existing Hero) + army-file `Id`/`JoinsUnitId` + army-builder join picker + hero-aware Tough + wound-last + morale-Q + last-model Defense + attack-Q + per-model combat-rule carriage (`IModel.RuleDefinitions`; Furious/Relentless/Thrust on the hero). GUI hand-verified 2026-06-21; engine green, headless exit 0. Deferred corners (per-model movement, activated abilities while joined, Impact(X), defensive per-model rules) + same-weapon-pooling collision → #093. ([WorkItems/006](WorkItems/006-hero.md))
- [x] 007 — Resolve `DeployAllUnitsStage.Enter` `NotImplementedException` + "actually move the models" TODO — **stale, closed 2026-06-15**: both referenced lines lived only inside a dead `/* ... */` block in `DeployAllUnitsStage.cs` (an abandoned hand-rolled `Enter` + `DeployHandlerRepeater`). The live deployment is the `ParentStage` transition graph (`DetermineNextDeployPlayer → ChooseUnitToDeploy → ChooseDeployAction → DeployUnitStage → PlaceDeferredUnits`), and models are placed for real in `DeployUnitStage.Enter` via a `PlaceObjectsRequest<ModelData>` + `SetPosition`. Dead comment block deleted (engine `346442e`); suite 556/0.
- [x] 090 — Enemy-check consolidation + executor moves; Strafing fly-over exemption (follow-up to #011's deferred item (b)). `ConsolidateStage` (+ GUI/CLI/AI resolvers) and `MovementExecutor.TryMove` (Vanguard triggered move) now run the #011 move-through/standoff check. Fixed the regression where #011 blocked fly-over rules: new `RuleOperation.IgnoreEnemyMovementBlock` (the doc-foreshadowed "Flying-only facet") granted by a passive hook on Strafing, queried via `MovementRuleQueries.CanMoveThroughEnemies`, threaded as a `canMoveThroughEnemies` flag that waives the pass-through block (ending-stacked + standoff still enforced). Verified end-to-end through the real `DefinePathStage`. Suite 511/0, headless-verified. Engine `bf2b41b` (merged to master). ([WorkItems/090](WorkItems/090-enemy-check-consolidation-executor.md))
- [x] 089 — AI movement aims at base contact / standoff (not blind backoff): follow-up to #011. The AI/auto movement resolvers used to stop "1" short of the enemy **centre** (actually base overlap) and lean on a halving backoff that only retreats, so the AI never completed a charge. Now they aim at an explicit end gap — a melee/hybrid unit that can reach charges into base contact; everyone else advances to the 1" standoff — solving the centroid step by measuring the packed result's nearest-enemy gap and correcting (~1:1 response). Validate-and-backoff stays as the safety net, with a new `HoldExactPositions` rung (zero-length paths) for a unit so intermingled with enemies that any re-pack would cross an enemy base (this fixed a `Moves through an enemy unit` throw the smoke caught once melee actually started happening). Engine `415b621`; suite 497/0; headless-verified 5× (exit 0, 3–5 melee resolutions each). ([WorkItems/089](WorkItems/089-ai-charge-to-contact.md))
- [x] 011 — Moving-through-enemy-units validation + 1" standoff: `MovementUtilities.ValidateMovingThroughEnemyUnits` now blocks paths that pass through or stack on an enemy base, and a non-charging model must end at least 1" (base-to-base) from enemies. "Charging" is detected geometrically — ending within melee range of an enemy unit waives the standoff for all of that unit's models. Only moves that close the distance are penalised, so a model already inside the standoff (e.g. left by a pile-in) can still move away or hold. Enemy data flows as `EnemyModelFootprint` (centre + radius + per-unit key) through the charge `ValidatePaths` overload; `DefinePathStage` and the AI/GUI/CLI resolvers supply it. Engine `ae1baaa`; suite 497/0; headless-verified. **DEFERRED (recorded):** advance-vs-charge-into-contact can't be distinguished without action-type plumbing; consolidation + the Vanguard/Strafing executor move paths aren't enemy-checked yet. ([WorkItems/011](WorkItems/011-move-through-enemy-units.md))
- [x] 050 — Movement validation base-radius for terrain footprints: swept-disc `IZone.DoesPathIntersectZone(start, end, inflationRadius)` across all zone shapes; the impassible/difficult/dangerous `MovementUtilities` validators pass `BaseRadiusInches`. Landed 2026-06-13 (engine `57e667b`, bump `896303d`, plus the `AiDefineMovementResolver` follow-up); index checkbox was overdue. ([WorkItems/050](WorkItems/050-movement-base-radius.md))
- [x] 017 — In-range attacker/defender determination (melee): real 2"/4" range gating + in-range sets on `CombatActionContext` + dead-model distance fix in `MinDistanceBetweenUnits`. **Follow-up (2026-06-14):** the empty-pool guard checked `inRange.Count`, not the swing pool, so a charge where only ranged-only models survive in melee range still crashed `ChooseMeleeWeaponStage` ("Available weapon dictionary was empty", ~2/8 smokes). Now gates on `AvailableWeapons.Count` (+ integration test) — engine `2bb10eb`, smoke 22/22 clean. (Separate, still-open: an intermittent `DefinePathStage` 1"-cohesion crash from AI/auto movement — now filed as #159 (as #154 pre-renumber, reconciliation 9).) ([WorkItems/017](WorkItems/017-melee-in-range-checks.md))
- [x] 048 — Block deployment of models into impassible terrain: shared `PlacementUtilities.OverlapsImpassibleTerrain` (base-radius disc-vs-zone, built on #050's swept-disc overload) wired into the AI, CLI, and GUI place resolvers. Engine + AI + CLI unit-tested (`AiPlaceObjectsResolverTests.DoesNotPlaceModelsOnImpassibleTerrain`); GUI block (red ghost + click toast) verified by hand. ([WorkItems/048](WorkItems/048-deployment-into-impassible.md))
- [x] 041 — Factor line of sight into movement resolver's ranged-targeting overlay: both the per-enemy-unit weapon list and the per-model fire lines now require LoS (terrain + model-base blockers), with a red block-stub when no model in the unit is visible. ([WorkItems/041](WorkItems/041-movement-resolver-ranged-los.md)) — commit `ec2f552`
- [x] 045 — Cover indication in targeting overlay and shot UI: fire lines through cover render dashed yellow; shot picker spells out "Cover (+1 Def)". Presentation-only, no engine change. ([WorkItems/045](WorkItems/045-cover-indication.md)) — commit `cc341b0`
- [x] 005 — Scout deployment: set aside during normal deployment, then placed after all others within 12" of the zone (forward deploy). Built on the #042 deploy defer/reserve primitive (`PlaceDeferredUnitsStage` + forward-expanded zone). Headless-verified, unit-tested, GUI-verified by hand. (#042 INTEGRATION PROGRESS cont. 13.)
- [x] 004 — Ambush deployment: kept in reserve, brought on at the owner's choice from round 2+ and placed anywhere >9" from enemies. Built on the same primitive (`StartOfRoundExtraActionStage` arrival + `PlaceObjectsRequest.MinDistanceFromEnemiesInches` honored by CLI/GUI/AI place resolvers; reserves excluded from activation/targeting via `IUnit.GetIsOnBattlefield`). Headless-verified, unit-tested, GUI-verified by hand. DEFERRED: "can't seize the round it arrives" objective nuance. (#042 INTEGRATION PROGRESS cont. 14.)
- [x] 083 — **(HIGH PRIORITY)** Await through the stage-machine transition chain (audit §4). Made `ParentStage.Transition` return `Task` and awaited it through `ExecuteTransition` → `SignalEvent` → `StageBinding.Activate` → `TransitionToSibling`, the combat `onFinished` callback chain (`Action<TResult>` → `Func<TResult,Task>` across `CombatStage` + 11 `RunStage` impls), and `base.Enter` calls. Stage faults after the first transition now propagate out as faulted Tasks instead of escaping the async-void delegate unobserved — `FDGServer.LaunchStateMachineOnceReady` already awaited `Enter` end-to-end inside a top-level try/catch, so the chain now genuinely surfaces engine faults. `Task.Yield` at the round boundary (`ReconcileNewRoundStage`) caps synchronous-continuation stack growth. ~65 engine files; new fault-propagation regression test in `ParentStageOrderingTests` (suite 466→467). Submodule `c4fb4f2`, bump `6a8ecb1`. ([WorkItems/083](WorkItems/083-await-transition-chain.md))
- [x] 064 — Stage-machine tests: 16 new engine tests (suite 424→440). `VictoryCalculationStageTests` (winner/tie `NotifyGameEnded` contract), `DetermineMeleeWinnerStageTests` (winner routing + recorded result), `ParentStageOrderingTests` (enter/exit/reconcile/resume ordering pinned as characterization before #083), and 2 new `AmbushRuleIntegrationTests` cases (arrival token stamp + non-reserve unit not offered). `StartOfRoundExtraActionStage` arrival was already covered, so only the gaps were added. Submodule `a1fead9`. ([WorkItems/064](WorkItems/064-stage-machine-tests.md))
- [x] 079 — csproj dependency cleanup: dropped `System.Drawing.Common` (Windows-only GDI+; only `System.Drawing.Color` was used and it ships in the base-framework `System.Drawing.Primitives`) and deleted the dead duplicate ImageSharp comment. Engine suite 424/424, build clean, headless smoke exits 0. Submodule `2b14fd9`. ([WorkItems/079](WorkItems/079-csproj-dependency-cleanup.md))
- [x] 060 — Stage-resolution cleanup (audit §5): dead `StageResolverRegistry._gameDataStore` field (commit `b0aebc9`), "Remove client" → "Remote client" exception text, and the commented-out PlayerID filters in `RequestMessageSender` — engine commit `5822954`, bump `d40f132`. Suite 416/0.
- [x] 025 — `AssignWoundsResults.AutoFill` bug — **stale, closed 2026-06-13**: verified the described `modelWoundsRemaining always 0` bug no longer exists. `AutoFill()` was rewritten to fill via `TryAddWounds` and throws if it cannot place every wound (`AssignWoundsResults.cs:96`); the AI wound resolver relies on it and the suite is green (416/0). Remaining wound-assignment gaps stay tracked as #023/#024.
- [x] 001 — D3+2 objective placement: interactive alternating-team placement w/ validator + AI strategy + debug auto-place toggle ([WorkItems/001](WorkItems/001-objective-placement.md))
- [x] 012 — Decouple Advance / Rush / Charge distances: engine splits `MaxRushDistance` from `MaxChargeDistance`, `PathTemplate` carries all three explicitly (no more hardcoded half), `ValidatePaths` gains a "beyond Rush ⇒ a model must end in melee" check + Pass-gating; GUI movement resolver gains a three-band (Advance/Rush/Charge) ring preview + Done gating. Tests added (`ChargeReachValidationTests`, `ChooseActionPassDisableTests`). Engine `a967fa1`, GUI `3a6f189`.
- [x] 013 — Weapon-group target selection (up to 2 targets per shoot action): already implemented via `GameWideConstants.MAX_TARGETED_UNITS_PER_SHOOT_ACTION` + `attackedDefenderRefs` tracking in `ChooseRangedAttackStage`; item was stale
- [x] 014 — `RangedContext` NIE paths: file was dead code (entire body in a `/* ... */` block); actual ranged flow uses `CombatActionContext`. File deleted.
- [x] 018 — Pile In move: defender models not already in BTB step up to 3" toward nearest charging model, with impassible-terrain and strict coherency fallbacks ([WorkItems/018](WorkItems/018-pile-in.md))
- [x] 043 — Filter dead models out of `IUnit.AllWeapons` so dead models no longer contribute weapons to attack/strike-back/shoot lists or the tooltip readout
- [x] 019 — Consolidation moves after melee resolution: 3" Wipeout / 1" Disengage with per-model GUI path-builder, AI resolver, table-bounds clamp, and validation against terrain + cohesion + cap ([WorkItems/019](WorkItems/019-consolidation-moves.md))
- [x] 044 — Allied/same-team models don't block line of sight: `LineOfSightUtilities.BuildModelBlockers` now excludes every model on the attacker's team (via `tableState.Teams`) plus the defender unit's models, falling back to attacker-player-only when no team is registered. New `ModelBlockerTests` cases (ally exclusion, third-party enemy still blocks). ([WorkItems/044](WorkItems/044-los-ally-exclusion.md)) — commit `8701abf`
- [x] 002 — Terrain placement workflow: three-mode lobby setting (AutoFromLayout / Alternating / LoadFromFile), AI + human + CLI resolvers, `CompositeZone` for L-shapes, `RotatedZoneWrapper` + SAT for 45° rotation, GUI thumbnails + R-key rotate. (Includes terrain rotation, formerly tracked separately as #045.) ([WorkItems/002](WorkItems/002-terrain-placement.md))
- [x] 047 — Deployment zone selection: draw labelled zones on the canvas, allow clicking zones directly, synchronise hover between dialog and table, and renumber in reading order ([WorkItems/047](WorkItems/047-deployment-zone-labels.md))
- [x] 206 — Standoff relocated to a proximity Pass gate: a non-charge move may end right up against an enemy (movement validator no longer rejects it), and `ChooseActionStage.GetCanPass` gates Pass when within 1" of an enemy so it must Charge/reposition. Charge band (2") stays wider than the forced band (1"). Aircraft keep their own standoff. Fixes the "can't press Done, must charge" block and restores the move-close-then-forced-charge design. ([WorkItems/206](WorkItems/206-standoff-blocks-move-then-charge.md)) — engine `6053061`
- [x] 211 — Solo AI mover pathed through impassible terrain (~1/1800): the pre-#256 stand-still early-outs submitted unvalidated StayInPlace re-packs; fixed by #256's StayInPlaceValidated + #208 decline gate, verified with a 3200-game gate-replica bench (0 faults). ([WorkItems/211](WorkItems/211-solo-mover-impassible-terrain.md))
- [x] 273 — (security) FDGHost pre-auth connection limits: accepts beyond 32 total / 4 per-IP closed immediately; un-greeted connections capped at 64KB frames, lifted at join accept (per-frame `Func<int>` cap — the parked-read race is pinned by test). First real-TCP FDGHost tests. ([WorkItems/273](WorkItems/273-fdghost-unauthenticated-connection-limits.md)) — engine `842c43b` (was #266 pre-reconciliation-22)
- [x] 186 — (security) Wire deserialization hardening: `$type` allowlist (registered stable IDs / engine-assembly types / benign collections; framework gadget class refused) at all three wire sites - message envelope AND the separately-deserialized stage request/reply bodies (the load-bearing half found in review). 12 tests. ([WorkItems/186](WorkItems/186-network-deserialization-hardening.md)) — engine `2ecf201` + `e292e18`
- [x] 272 — (security) File-load `$type` allowlist: one shared `AllowlistSerializationBinder` (replaced `StableTypeSerializationBinder`) enforced on the store rebuild (save/load AND network full-state sync - a gap #186's envelope pass missed), both terrain loaders, and the save type-map resolver. Army/scenario files are STJ closed-polymorphism, out of scope. Saves lose the permissive fallback (owner-approved). 6 tests; save round-trip verified via --make-scenario/--scenario. ([WorkItems/272](WorkItems/272-untrusted-content-file-deserialization.md)) — engine `f432c03` (was #265 pre-reconciliation-22)
- [x] 329 — In-game Army List overlay: L (or the bottom-left button) opens every player's list over the table area (right column stays live) - Army Forge-style cards + condensed table modes, tabs per player, all live state (wounds, shrinking weapon counts, DESTROYED/Activated, tokens, joined heroes, hoverable rules, flashing Action Needed close). Points + army identity plumbed engine-side (UnitData/ArmyData, save/sync-safe); unit labels rebound to N. Hand-verified v1-v8. ([WorkItems/329](WorkItems/329-army-list-overlay.md)) — engine `ae684f2`
- [x] 330 — Pile-in maximizes base contact: contact-slot assignment around chargers (36 sampled directions, most-constrained defender first, terrain/overlap-swept validation, teammates transparent mid-shuffle but hard at end state) replaces the formation-keeping ray march; old ray kept as fallback, coherency revert kept + residual-overlap revert added; log line gains a contact tally. 5 new tests incl. partial-terrain wraps. The field note's 2" both-sides attack gate was already live (#017). Hand-verified same day via `Scenarios/pilein-wrap-demo.json`. ([WorkItems/330](WorkItems/330-pile-in-contact-maximization.md)) — engine `23fdbf2`
- [x] 332 — Matches ground on after the result was already fixed: a tabled opponent plus a sole objective lead at the end of round 3 still played round 4, leaving the winner holding every move to reach a foregone conclusion. **Closed 2026-08-04, GUI hand-verified.** `ReconcileObjectivesStage` now takes its existing `ToVictoryCalculation` edge early when `MatchDecision.IsResultFixed` says no remaining play can change the outcome — exact, not heuristic: objective ownership is sticky, so a side with no living models has a non-increasing score and the lone survivor a non-decreasing one, making a sole lead final; all sides dead freezes the board and is also decided. "Tabled" means no living units ANYWHERE (never `GetIsOnBattlefield`) — reserve, embarked, flown-off, and the copy Reinforcement queues all keep a side in the game, and that is the entire false-positive surface. One `Announce` carries banner + log, and both channels already replicate, so no wire / `GameResult` / client change was needed. 18 tests, mutation-verified. Reachability and the Aircraft-only refinement deliberately not built. Was #330 pre-reconciliation-46. ([WorkItems/332](332-early-match-decision.md)) — engine `05e2d65`, superproject `ddeedab`

- [x] 331 — Victory fireworks behind the game-over card, in the winning side's player colours (team wins alternate teammates' colours shell by shell, ties fire every tied side's, nobody winning fires nothing). **Closed 2026-08-04, GUI hand-verified.** The load-bearing half was not the particles: `GameResult` is host-side only and never crosses the wire, so the #257 pooled tally was extracted out of `VictoryCalculationStage` into `TeamScoreTally` and both now call it over the REPLICATED objectives + slots — host and client derive the same winner rather than the renderer running a lookalike that could disagree. Tuned for the `SetTargetFPS(30)` cap, which is the real constraint: cost is nothing against 33ms, but pinpoint sparks strobe, so each particle draws as a LINE from its previous position to its current one (that segment is the spark, and it is cheaper than the circle it replaces). Preallocated 900-entry pool that never grows, peak pinned under 600 by a two-simulated-minute test. Draw order free — raw Raylib drawing precedes `rlImGui.Begin()`, so sparks sit over the board and under the card. Off-switchable via Options > Victory fireworks. 18 tests across both repos. ([WorkItems/331](331-victory-fireworks.md)) — engine `8e339eb`, superproject `9b6eab6`
- [x] 170 — Mixed-base AI deploy stranded small models out of cohesion: `AiPlaceObjectsResolver` spaced its whole grid by the LARGEST base (the #159 PackGrid bug's unfixed deploy sibling). **Done 2026-08-05.** Mixed-base units now lay out through `FormationLibrary.LayoutOffsets` with PackGrid's conventions (per-model extents in-row, rows stacked by tallest, no lonely last-row model); uniform-base units keep the historical grid to the byte, so solo deployments are unchanged (solo D1 mirror `4B73F1B9DBBC8102` / basic `E86503B238B27EA1` bit-identical, same-session controls). Sweep/penalty machinery consumes the offsets; dead `BlockIsValid` removed. New red-by-design pin: `MixedBaseUnit_DeploysInCohesion_WithoutOverlaps` (r=1.81 leader + 6x r=0.55 - pre-fix the leader sat 2.62"+ out of cohesion). Suite 2868/2868; smoke exit 0. ([WorkItems/170](170-mixed-base-deploy-cohesion.md)) — engine `ec33c70`
- [x] 281 — `StringPull` erased the router's difficult-terrain preference; `DifficultCostMultiplier` was a no-op on open tables (and hid Strider). **Done 2026-08-05.** Turned out to be THREE stacked bypasses: the planner only routed when impassible-blocked, FindPath's early exit skipped the A* on any impassible-clear straight shot, and StringPull (route + per-model legs) pulled every mud bend straight. Fixed with a cost-preserving pull under the A*'s own 2x grid metric plus the two gate fixes; same-session follow-up made the score gradients (`RouteMetrics.Route`) price the same detour. Strider needs no special casing (its grid marks no difficult cells) and shows its first observable geometry difference. 5 red-by-design pins incl. the upgraded #264 Strider pin; suite 2869/2869. Solo D1 bit-identical both matrices; pool A/Bs flat (geometry 84.23->83.71, gradient 84.42->84.84, sigma ~0.65pp; 0 faults final run; decision cost flat). Known soft edge recorded: the 6" cap is not priced into deadline slack for unavoidable crossings. ([WorkItems/281](281-stringpull-erases-difficult-routing.md)) — engine `9a8a15b` + `ce124b5`
- [x] 361 — Tactician never considered the reachable charge (Hive Lord save): FOUR stacked defects. Value-only TopEnemies pruning never evaluated the nearest enemy (nearest-2 union added); all planning geometry inflated terrain by the inscribed base radius while the #341 swept-base validator tests the true oriented rect, wedging big rect-based units into all-candidates-Blocked-at-zero (circumscribed `TerrainClearanceRadius` everywhere the Tactician routes); value-first enumeration let hopeless far targets eat the charge family's pruning slots (nearest-first); and the routed BuildCharge branch placed contact goals inscribed (rect front edge inside the target, ladder stall) and never closed grid-quantization scatter (circumscribed goals + validated `NudgeToContact`, target-only gap). **Done 2026-08-05.** The exhibit went from "slid 6 inches west" to "charges the APC": analyze replay picks Charge->Blood APC, Reachable contact, 1.51. Four pins proved red; suite 2892/2892; solo D1 bit-identical both matrices throughout; pool 85.52%/85.42% vs 84.39% baseline (+1pp), 0 faults, decision cost flat. Same-day follow-up: charge feasibility grades vs the declared target only (bystander-contact mislabel; Hives melee proxy 90.38% vs 90.00%). Recorded not built: difficult-terrain circumscribed conservatism. ([WorkItems/361](361-charge-target-pruning-blind-spots.md)) — engine `401f2fe` + `0edaead` + `1183f55`
- [x] 226 — In-app bug reporting: escape-menu **Report a Bug** takes the player's notes, always writes a local bundle to `BugReports/` beside the exe, then fire-and-forget uploads it gzipped to `POST /reports` on the EXISTING #271 Cloudflare Worker (no new service, free tier) — retrieval is pull via `fetch-reports.sh` + an `ADMIN_TOKEN` secret. **Closed 2026-08-05, deployed and prod-smoked.** Local-first ordering means an offline/failed upload still leaves a complete report on disk and the status line says so rather than claiming success. Bundle: description, build stamp, protocol version, platform, player name, log+chat merged in arrival order (newest 4000), crash.log tail, and the full save on the host only (#054). A disclosure above the Send button itemizes all of it, including the parts that are NOT the reporter's own data (other players' chat, their army lists in the save) and the IP the drop box records. Pulled in two things missing generally: an app build stamp (`InformationalVersion` from git via `build-dist.sh`) without which a report can't be tied to a binary, and gitignoring `tools/list-server/reports/`. Abuse posture mirrors #271: unauthenticated uploads capped 1MB compressed / 16MB decompressed, per-IP interval, total caps that REJECT rather than evict (a flood must not delete real reports); reads fail CLOSED when the secret is unset (verified in prod: 503 before, 403 after). 2862 engine + 1118 app green, prod `smoke.sh` all 14 sections. **Full round trip confirmed 2026-08-08**: a report filed in a live game uploaded to the deployed Worker and came back through `fetch-reports.sh` complete — 138 merged log lines (debug/chat tagged, in arrival order), a replayable 234KB save, 311.8KB raw -> 20.1KB gzipped (2% of the cap). Known wart found by reading a real bundle: `crashLog` is the tail of an append-only file, so it can carry crashes from days earlier that look related and aren't. ([WorkItems/226](226-bug-reporting-system.md))
