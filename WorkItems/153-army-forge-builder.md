# 153 — Army-Forge-style catalog army builder

**Status**: blocked (proposal only — awaiting a go decision; OPR-data path additionally gated on OnePageRules clearance)
**Related**: #106 (army builder authoring UX), #107 (combined squads), #003 (force-org validation), #059 (embedded rule definitions), #033/#034 (spells), `ArmyBuilderScreen`, `.fdgarmy` / `ArmyListFile`

## Goal

Add a **catalog-driven** army builder to FdgRaylib modelled on OnePageRules' Army Forge: pick a faction "book", add pre-statted units from its roster, customise each unit through grouped, costed **upgrade options** (never typing raw stats), with a live points total validated against a limit — then compile the result into the `.fdgarmy` the game already plays. "Done" = a player can build a legal list end-to-end from a book and launch a game with it, with no engine changes.

This is a multi-phase epic. It will fragment into per-slice items when picked up. This file is the master plan.

---

## TL;DR — the load-bearing insight

Our `.fdgarmy` (`ArmyListFile` = `List<UnitFileEntry>`, each a fully-resolved unit: name, model count, quality, defense, weapons-with-rules, point cost, embedded rule/spell definitions) is **already the compile target**. `FDGServer.CreateArmyDataFromArmyFile` builds runtime `UnitData` straight from a `UnitFileEntry` (`FDGServer.cs:208–216`). So an Army-Forge-like builder is:

```
   .fdgbook (catalog) ──┐
                        ├─►  Compiler  ─►  .fdgarmy  ──►  [existing game load path, unchanged]
   builder selections ──┘                 │
                                          └─ the SAME .fdgarmy also embeds the selections + a book slice,
                                             so the builder can reopen and re-edit the exact same file.
```

Everything new lives **app-side**: a catalog data format, a compiler, and a UI. The engine submodule stays untouched — including the single-file "embed the editable list inside the `.fdgarmy`" design (see Persistence, below), which works precisely because the engine's deserializer ignores unknown JSON. That is what makes this tractable and keeps it off the read-only submodule.

---

## Background — how Army Forge actually works

Verified 2026-06-30 by reading Army Forge's public JSON API (the site is a client-rendered Next.js SPA; the list page HTML is an empty shell, so data comes from `GET /api/army-books/<uid>` and `GET /api/share/<id>`). Concrete reference: the **Alien Hives** book (`w7qor7b2kuifcyvk`, v3.5.3, 41 units, 38 upgrade packages / 56 sections).

**Interface / flow:** game system → army book (faction) → points limit → add units from roster → configure each unit via upgrade sections → live `total / limit` header → save / share (`/view/<slug>?listId=<id>`) / export (TTS, PDF).

**Data model (the important part):**
- **Book** = `{ name, version, units[], upgradePackages[], customRules[], customWeapons[], spells[] }`.
- **Unit** = pre-statted: `{ name, size, cost, quality, defense, weapons[], rules[], upgrades[] }`. You don't author stats; you pick a roster entry.
- **Weapon** = structured, not a string: `{ name, range, attacks, specialRules[] }` + a derived `label` (`Shredder Cannon (18", A4, Rending)`).
- **Special rule** = a reference `{ id, name, rating }` → `AP(1)`, `Blast(3)`, `Deadly(3)`. `rating` is the numeric arg.
- **Upgrade section** = `{ label, variant, affects, targets, options[] }`:
  - `variant`: **replace** (34/56 in Alien Hives) swaps an existing weapon/item; **upgrade** (22/56) adds wargear/rules. (Other OPR books also have *pick-N* and *add-models* variants — model our grammar to cover them even though Alien Hives doesn't use them.)
  - `affects`: `{ type: "any" | "all" | ... }` — how many models in the unit the choice touches.
  - `targets`: which existing weapon/item the section replaces (e.g. `["Heavy Razor Claw"]`).
  - `options[]`: each `{ label, cost, gains[] }`. **gains** are `ArmyBookWeapon` (95), `ArmyBookItem` (58), or `ArmyBookRule` (2) — a chosen option grafts these onto the unit and adds `cost` to the running total.

So: *a book is a bag of units; a unit is base stats + a list of upgrade sections; picking an option mutates a working copy and sums cost.* That mutate-and-sum step is the compiler.

## Where our app is today

`ArmyBuilderScreen` (~980 lines) is the **inverse** model — a freeform stat editor. You type Models / Quality / Defense / Points / weapons / rules per unit and set `PointCost` yourself; `ArmyListFile.TotalPoints` just sums the fields you typed. #106 added a read-only stat-block preview + Duplicate; #107 (open) wants "Combined" squads. There is **no** catalog, roster, upgrade group, or auto-costing. It's excellent for authoring arbitrary units (which we still want — see "keep both", below) but it is not the guided, points-validated Army Forge experience.

## Licensing / OPR-clearance framing

Two separable things, so a green-light on one need not wait on the other:
1. **The tool** (catalog format + compiler + UI) — our own IP, buildable now if you want the mechanic.
2. **The data** (actual OPR army books / rules text / point values) — this is what OPR clearance governs. OPR publishes game rules under Creative Commons, and the Army Forge data is reachable via public API, but *shipping or importing their army-book content* is the part to clear with them.

**Design consequence:** make the catalog format **game-agnostic and source-neutral** so it can hold (a) our own FDG factions we author, or (b) imported OPR books — with the OPR-import path as an isolated adapter. Then "build the tool" and "populate it with OPR data" are independent switches.

## OPR versioning strategy (decided 2026-07-01)

**Constraint (user):** OPR's Army Forge API serves only the *current* version and updates army data + rules **simultaneously**, with **no legacy versions hosted anywhere**. The next update is expected **September 2026**. So the API is a *moving target*, not a stable dependency — if it advances to rules our engine can't yet handle, a live-API design would simply break.

**Strategy:** the API is a **one-time ingestion tool**, never a runtime dependency.
- **Snapshot now → local files.** Import the *current* online versions into **bundled local `.fdgbook` files** shipped with the app (a checked-in "book library"). The app builds armies from these local snapshots, offline, against a known-good version.
- **Advance on our schedule.** When OPR updates (Sept 2026), we stay deliberately **one version behind** until we (a) re-run the importer to capture the new snapshot and (b) confirm the engine handles any new rules. We are never *broken* by an upstream change — only intentionally behind.
- **Time-sensitive:** capture the **entire current book set before the September update**, or that version is gone for good. This is the one genuinely deadline-bound task in the whole feature.
- Each bundled `.fdgbook` records the source **OPR version string** (e.g. `V3.5.3`) for provenance and to detect drift. (`BookFile.Version` already exists.)
- **Scope of capture:** rules/stat data only (unit profiles, weapons, upgrade options, point costs) — **not** the books' background/lore prose, which we neither need nor want to reproduce. This keeps the snapshot to the game-mechanics data and off the copyrightable creative text.
- This composes with decision 6 (each *army* also embeds a full book snapshot): the **library** freezes versions for *authoring new* lists; the **per-army embed** freezes the version for *re-editing an old* list. Two layers, same principle.

---

## Target architecture

Pieces (the backend lives in the **engine submodule** under `FDG.ArmyBuilding`; only the screen is in `FdgRaylib/`):

- `ArmyBuilding/Catalog/` — the `.fdgbook` model + loader (`BookFile`, `RosterUnit`, `UpgradeSection`, `UpgradeOption`, `Gain`).
- `ArmyBuilding/List/` — the *selection* model (`BuilderList`): what the user is editing — unit *instances*, each = a `RosterUnit` ref + chosen option ids + model count. **Not a separate file** (see Persistence): it is embedded inside the compiled `.fdgarmy`.
- `ArmyBuilding/Compiler/` — `ListCompiler.Compile(BookFile, BuilderList) → ArmyListFile` (which then also carries the `BuilderList` + book slice back inside it). Pure, deterministic, unit-testable headless. **This is the heart of the feature.**
- `ArmyBuilding/Import/` — the OPR `/api/army-books/<uid>` → `.fdgbook` adapter (built in parallel per the data-source decision; *content use* stays clearance-gated).
- `ArmyBuilding/Validation/` — points-limit, min/max model counts, per-section legality, force-org (#003).
- `Rendering/ArmyForgeScreen.cs` — the three-pane UI (new screen; keep the freeform `ArmyBuilderScreen` alongside).

**Where do the catalog types live — app or engine?** **Engine submodule** (`FDG.ArmyBuilding`), *decided 2026-07-01* (was app-side; reversed — see decision 4). Portability: any app on the FDG library reuses the whole define/compile/import backend; the app is only the GUI. It also sits naturally next to its output (`ArmyListFile` in `FDG.SaveLoad`) and the types it subclasses. Only `ArmyForgeScreen` + its presentation formatters stay in `FdgRaylib/`.

### Persistence — one self-contained `.fdgarmy`, editable list embedded (decided)

No separate `.fdglist` file. The builder saves a **single `.fdgarmy`** that is both playable and re-editable:
- The top-level `Units[]` (+ embedded rule/spell defs) are the compiled, playable army — read by `FDGServer` exactly as today.
- An extra top-level block carries the **editable state**: the `BuilderList` selections **plus a full snapshot of the source `.fdgbook`** (the entire book as it existed when the army was authored — every unit, section, rule, and cost, not just the referenced ones). The army is therefore a self-contained, version-frozen artifact.

**No engine/submodule change required.** Verified 2026-07-01: `RuleJson.Options` (`RuleJson.cs:9`) leaves `UnmappedMemberHandling` at STJ's default (Skip), so the engine deserializing the file into `ArmyListFile` **silently ignores** the extra block. Mechanics: the app serializes/deserializes an app-side extended type (e.g. `BuiltArmyFile : ArmyListFile { BuilderList Selections; BookFile Book; }` — or an equivalent wrapper) using the same STJ options; the engine reads the base `ArmyListFile` view. Only the app ever *writes* `.fdgarmy` (the engine never re-saves it), so the extra block can't be silently dropped in normal play.

**Version-stability (why full book, decided 2026-07-01):** because the whole book is embedded, an old army re-opens and re-edits against the *book it was built with*, not whatever the current catalog has become. Play is doubly frozen — the compiled `Units[]` + `RuleDefinitions`/`Spells` already lock behaviour, and the embedded book locks the *options and costs* you'd see on re-edit. On open, **the embedded book is authoritative**: the builder never silently re-points the army at a newer catalog book. A later slice can offer an explicit, opt-in "**migrate this army to the current book version**" action (re-resolve selections against today's book, surface any options/costs that changed) — but never automatically. A hand-authored or legacy `.fdgarmy` with no embedded block still plays; the builder just can't re-edit it (offer "duplicate into freeform" instead). Cost is a larger file (a full book ≈ 150–200 KB of JSON, e.g. Alien Hives ≈ 168 KB) — negligible for a local army file, and the price of reproducibility.

### `.fdgbook` data model (proposed)

```csharp
class BookFile {
  string Name; string Faction; string Version;
  List<RuleDefinition> RuleDefinitions;   // reuse #059 embedded-rule types
  List<SpellDefinition> Spells;            // reuse #033 spell types
  List<RosterUnit> Units;
}
class RosterUnit {
  string Id; string Name;
  int MinModels; int MaxModels; int BaseModelCount;   // fixed-size or ranged
  int Quality; int Defense; BaseFileEntry Base;
  int BasePointCost;                                   // cost at BaseModelCount, no upgrades
  int PointsPerExtraModel;                             // for add-models variant, if used
  List<WeaponFileEntry> Weapons;                       // reuse existing type
  List<SpecialRuleEntry> Rules;                        // reuse existing type
  List<UpgradeSection> Sections;
}
enum UpgradeVariant { Replace, Upgrade, PickN, AddModels }
class UpgradeSection {
  string Label; UpgradeVariant Variant;
  AffectsSpec Affects;        // one model / any / all / count
  List<string> Targets;       // weapon/item names this replaces (Replace only)
  int MinPicks; int MaxPicks; // PickN
  List<UpgradeOption> Options;
}
class UpgradeOption {
  string Id; string Label; int Cost;
  List<Gain> Gains;           // weapons / rules / (items→rules) to graft on
}
```

Note we can **reuse** `WeaponFileEntry`, `SpecialRuleEntry`, `BaseFileEntry`, `RuleDefinition`, `SpellDefinition` — the vocabulary already exists. `Gain` is the only genuinely new leaf, and it's a thin union over "add weapon / add rule / set stat".

### The compiler contract

`Compile(book, list) → ArmyListFile`:
1. For each list unit instance: clone its `RosterUnit` into a working `UnitFileEntry` at the chosen model count (start from base weapons/rules, `PointCost = BasePointCost + extraModels * PointsPerExtraModel`).
2. Apply each selected option in order: `Replace` removes `Targets` from the unit's weapon/rule set and adds the option's `Gains`; `Upgrade`/`PickN` add gains; `AddModels` bumps count/cost. Accumulate `option.Cost` into `PointCost`.
3. Carry embedded `RuleDefinitions`/`Spells` from the book onto the `ArmyListFile` (the #059/#033 rehydration path already registers them at load).
4. Wrap the result as the app-side extended file: the compiled `Units[]` (playable) **plus** the `BuilderList` selections and a **full snapshot of the source book** (editable, version-frozen). To `FDGServer` this is indistinguishable from a hand-authored `.fdgarmy`; to the builder it round-trips back into an editable session.

Properties to test: **compile is total and deterministic**; a compiled army re-loads and plays (headless smoke); and **round-trip** — save → reopen in builder → the reconstructed `BuilderList` equals the original (and recompiling yields the same `Units[]`).

---

## Audit findings (2026-07-01, deeper model pass)

Two codebase probes + a PDF check tightened the plan:

1. **Per-model weapons are aggregate, round-robin-distributed (viable, with a caveat).** `UnitData` (`UnitData.cs:134–180`) expands each `WeaponFileEntry` by `Quantity` into individual `Weapon`s (after sorting by `Quantity` ascending) and round-robins them across models; each `ModelData` owns its `Weapons` list (`ModelData.cs:64`). Heterogeneous units work, but only *implicitly* via counts — there is **no explicit "model 3 carries the plasma" field**. So the compiler operates on **aggregate weapon quantities**: "replace one X with Y" = `X.Quantity−−, Y.Quantity++`, which distributes cleanly. OPR's "Replace one/any/all X" grammar is itself aggregate, so this fits — but *exact per-model loadout control* is a fidelity limit we accept (documented in non-goals).
2. **A compiled `.fdgarmy` needs almost no new launch wiring.** The lobby already has a per-player **"Load Army"** button (`LobbyScreen.cs:243` → `TryLoadArmyForPlayer` → `UpdateArmyListFile`), and headless `ArmyLoader` loads a file too. Because our output *is* a `.fdgarmy`, P2's "play it" reduces to **Save → Load Army** through the existing path; a dedicated "Use my built army" hand-off button is a nice-to-have, not required. Networked players already ship their army via `ArmyListUpdateMessage.FromArmy()`.
3. **The one hard launch gate: rule validity.** Embedded `RuleDefinitions` must pass `RuleValidator.Validate()` or army load **throws** `RuleValidationException` (`ArmyListRuleResolution.cs:31–51`); unit rule *references* with no matching definition are silently skipped with a debug warning (`:70–79`). ⇒ the compiler must carry the book's (already-valid) rule/spell definitions onto the army, and a compiler test must assert a compiled army with rules **loads without throwing**. Force-org caps (`ForceOrgValidator`) are advisory and never block launch — so points/legality enforcement is entirely *our* UI's job (P4).
4. **`BuiltArmyFile` must be serialized as the derived type.** `ArmyBuilderScreen.Save()` does `JsonSerializer.Serialize(_army, RuleJson.Options)` with `_army` typed `ArmyListFile`; STJ serializes by the *declared* type, so the embed only survives if the forge screen serializes a `BuiltArmyFile`-typed value (and `Deserialize<BuiltArmyFile>` to reopen). `Deserialize<ArmyListFile>` on the same file skips the block — confirming the no-engine-change claim end to end. `SpecialRuleEntry` is polymorphic (`_Core`/`_CoreNumeric`) and already handled by `RuleJson.Options`.
5. **Cost scaling for `any`/`all` sections.** OPR "Replace/Upgrade **any** X" applies 0..N times, cost = `count × option.cost`; "**all**" applies to every eligible model. So an `UpgradeOption` needs a per-application cost and the UI a count stepper for `any`; the compiler multiplies cost by applied count. (A flat per-section cost only covers the `one`/pick-one case.)
6. **Import source:** the OPR **JSON API** (`/api/army-books/<uid>`) returns the exact book, cleanly structured — vastly easier than parsing the two-column PDF tables. Used as a **one-time ingestion tool** to snapshot the current version into bundled local `.fdgbook` files (see "OPR versioning strategy") — **not** a runtime dependency. PDFs (in `../GDF Armies/`, all 47, V3.5.3) are the human ground-truth for verifying a snapshot.
7. **New deferred-fidelity items (recorded, not silently cut):** inter-upgrade **dependencies / mutual exclusivity** ("if you took X you can't take Y") — our data model has no `requires` predicate yet (P4/P5); **hero-join UI** — `UnitFileEntry.Id`/`JoinsUnitId` (#006) let a Hero attach to a unit; the forge must expose that (P3/P5); **`ArmyBookItem`→rules mapping** — OPR "items" (58 gains in Alien Hives) are named bundles of rules/weapons; `Gain` must model an item as such (P0b import). The embedded book need **not** travel over the network (only compiled `Units[]` matter for play) — keeps `ArmyListUpdateMessage` lean.

## Phased delivery plan (vertical slices)

Per house rule — one slice at a time, each with an integration test mirroring the nearest `*SerializationTests` / builder test, verified green, committed, ledgered. Each slice below is independently shippable and leaves the app working.

- **P0 — Catalog format + compiler + round-trip file + tests (no UI).** Define `.fdgbook` + `BuilderList` types and the app-side extended `.fdgarmy` wrapper (STJ round-trip like `ArmyListFileSerializationTests`), and `ListCompiler`. Ship **one hand-authored FDG demo book** (2–3 units, a couple of replace/upgrade sections). Tests: build a `BuilderList` in code → compile → assert the `ArmyListFile` matches an expected hand-written one; **reopen the saved `.fdgarmy` → reconstructed `BuilderList` equals the original**; the *engine* loads the same file ignoring the embedded block; headless smoke plays it. *Done-when:* compiler + round-trip are covered and a compiled army plays via `printf | dotnet run -- --headless`.
- **P0b — OPR import adapter + snapshot the current book library (TIME-SENSITIVE, before ~Sept 2026).** Map OPR's `/api/army-books/<uid>` JSON → our `.fdgbook`, then run it once to capture the **current** version of the book set into bundled local `.fdgbook` files (checked in under `FdgRaylib/Assets/Books/`). Stat/rule data only, not lore prose; stamp each with the OPR version. This both pressure-tests the format against real data *and* freezes the current version before the API advances (see "OPR versioning strategy"). *Done-when:* the current books import to valid `.fdgbook` files that compile playable armies, and the snapshot is committed.
- **P1 — Read-only book viewer + points header.** New `ArmyForgeScreen`: left pane lists the book's roster; center shows a selected unit's stat block (reuse #106's read-only block); top shows `0 / limit`. No mutation yet. *Done-when:* screen loads a book and renders roster + stats.
- **P2 — Add / remove units → compile → save → reopen → play.** Clicking a roster entry adds an instance; remove/duplicate; live points via the compiler; **Save writes the single embedded `.fdgarmy`, Load reopens it back into an editable session**; a "Launch" path routes the compiled army into the existing `LobbyScreen`/`ArmyLoader` flow. *Done-when:* a no-upgrades list built in the GUI saves, reopens, and launches a game.
- **P3 — Upgrade sections (replace + upgrade) with auto-costing.** Per selected unit, render its sections; `Replace` = radio/dropdown (one of N), `Upgrade` = toggles; selecting re-compiles and updates points + stat block live. *Done-when:* choosing options changes weapons/rules/points and the compiled army reflects them in-game.
- **P4 — Validation + legality.** Points-limit enforcement (block/flag over-limit), min/max model counts, per-section pick limits, and force-org (#003) hooks. Surface violations inline (Army Forge shows a red invalid marker). *Done-when:* an illegal list is clearly flagged and can't launch.
- **P5 — pick-N / add-models variants + Combined squads (#107).** The remaining upgrade variants and the "doubled-up" merged unit, which falls out naturally as an `AddModels`-style compile step. *Done-when:* a unit with a variable model count and a combined squad compile correctly.
- **P6 (optional) — share / export.** Shareable list link and/or PDF/text export, mirroring Army Forge's view/print.

Slices P1–P4 are the core product; P0 must precede all of them; P0b runs alongside P0/P1; P5+ are extensions.

## Integration points (real files)

- `FdgRaylib/Rendering/ArmyForgeScreen.cs` — **new** screen. Keep `ArmyBuilderScreen` (freeform) for authoring/one-offs; add a menu entry to choose builder mode.
- `FdgRaylib/Program.cs` — wire the new screen into the screen graph (MainMenu → ArmyForge), same pattern as `ArmyBuilder`.
- `FdgRaylib/Cli/ArmyLoader.cs` + `LobbyScreen` — the compiled `ArmyListFile` feeds the *existing* army-selection path; no new game plumbing.
- `FDGServer.CreateArmyDataFromArmyFile` — **unchanged**; it already consumes `UnitFileEntry`.
- Reuse: `WeaponFileEntry`, `SpecialRuleEntry`, `BaseFileEntry`, `RuleDefinition` (#059), `SpellDefinition` (#033), `RuleJson.Options` for STJ.

## Testing strategy

- **Serialization:** `.fdgbook`/`.fdglist` round-trip tests mirroring `ArmyListFileSerializationTests`.
- **Compiler:** table-driven — (book + selections) → expected `ArmyListFile`; cover replace/upgrade/pick-N/add-models and cost accumulation; a "compile is total" fuzz (random legal selections never throw).
- **Play-through:** headless smoke on a compiled army (exit 0 + expected log line), per house rule for playable paths.
- **UI:** follow #106's `ArmyBuilderScreenTests` approach for the new screen's logic.

## Decisions (locked 2026-07-01)

1. **Scope depth:** ✅ Full catalog + upgrades — build P0–P4 as the real target.
2. **Data source:** ✅ Both — hand-author FDG books AND import OPR books, but OPR import is a **one-time snapshot into bundled local `.fdgbook` files** (never a live-API runtime dependency; see "OPR versioning strategy"). Time-sensitive: capture the current version before the ~Sept 2026 OPR update.
3. **Screen strategy:** ✅ New `ArmyForgeScreen` alongside the freeform `ArmyBuilderScreen` (kept).
4. **Catalog location:** ✅ **Engine submodule** (`FDG.ArmyBuilding`) — *reversed 2026-07-01*. Originally chosen app-side to avoid touching the read-only submodule, but the user's portability requirement (any app on the library reuses the backend; the app is *only* the GUI) makes engine-side correct. The app keeps only `ArmyForgeScreen` + its formatters. Engine changes authorized for this feature; submodule-first commit cadence.
5. **Persistence:** ✅ A single `.fdgarmy` with the editable `BuilderList` (+ book) embedded — no separate file, and no engine change (engine ignores the extra block).
6. **Embedded-book granularity:** ✅ **Full book snapshot** — embed the entire source book, so an old army stays reproducible and re-editable as-authored even after the catalog book changes in a later version. Embedded book is authoritative on open; migrating to a newer book is an explicit opt-in action, never automatic.

### Locked 2026-07-02 (four forks, user sign-off)
7. **Rule-definition home:** ✅ definitions live **in the files themselves** (book/army embed them; version-behind armies stay self-contained), sourced from the curated supplement `GdfRuleSupplement.json` merged at import/apply time. Hand-authoring made safe by the strict loader + `--validate-rules` (full authoring UI stays #087).
8. **Combined squads (#107):** ✅ **two linked copies** — add the unit twice, link as Combined (hero-join UX/Id pattern); each copy buys upgrades under normal bounds (GDF "pay for both individually"); the compiler merges into one big unit; duplicate-copy composition counts 2 for free. Rejected: single-row toggle (every per-copy bound would need conditional doubling).
9. **Lobby launch gate:** ✅ **warn + host confirm** — host validates every received army at LAUNCH; Errors raise a confirm dialog listing them (default = cancel). Advisory philosophy preserved; house rules stay possible.
10. **Counts-as-terrain:** ✅ **rule-vocabulary primitive** — new counts-as Difficult/Dangerous Effect kinds + query points in the movement cap and dangerous-terrain stage; the synthesized-rule spell channel then covers the last 4 corpus spells (47/47). Rejected: token shortcut (one-off, not reusable by passives).
11. **Share/export (P6):** ✅ **closed as done-by-design** — the `.fdgarmy` is fully self-contained, so sharing is sending the file. Text export only if a real need appears.

### Still open (sub-decisions, not blockers)
- **Should the freeform `ArmyBuilderScreen` be able to open an embedded `.fdgarmy`** for off-catalog tweaks, and if so does a manual edit invalidate the embedded selections? Lean: allow open, and mark the embedded block stale on manual edit.

## Explicit non-goals / deferrals (record, don't cut silently)

- No engine changes at all (by design — the embedded-list file works precisely *because* the engine ignores unknown JSON).
- OPR *content distribution* is out of scope until OPR clears it; the import adapter (P0b) may still be built and format-tested against public data.
- share / export (P6) is optional and out of the core.
- Not deleting or replacing the freeform `ArmyBuilderScreen` (kept alongside).
- Faction-level special detachment rules / army-wide upgrades beyond per-unit sections are out of scope until a book needs them.
- **Explicit per-model loadout control** is out of scope — weapon assignment is aggregate (counts), realized by the engine's round-robin (audit finding 1). Fine for OPR-style upgrades.
- **Inter-upgrade dependencies / mutual exclusivity** (`requires`/`excludes` predicates) are deferred to P4/P5 (audit finding 7); many books don't need them, and the compiler stays correct without them (it just won't *prevent* an illegal combo until then).

## Rough effort (relative)

P0 (M, compiler + round-trip file is the risk) · P0b (M, adapter, runs in parallel) · P1 (S) · P2 (M) · P3 (M–L, most UI) · P4 (M) · P5 (M) · P6 (S–M, export). Core (P0–P4) is the bulk of the value.

## Notes

- 2026-07-02: **Corpus-wide smoke sweep — 47/47 factions play full headless games on the branch build.** Stabilization pass before hand-verify/merge: every snapshot re-imported (with supplement) + totality-compiled + played to game end with the current build (all of today's engine changes baked in). **Results: 47/47 exit 0**; 46/47 ended as real games (25 ties / 21 decisive — plausible for 4-unit EOF-default AI games); zero import warnings (spell parsing 47/47 confirmed in anger). The one non-game ending was **High Elf Fleets hitting the known #154 cohesion error** (1/47, plus 1/6 in a follow-up HEF repeat run → ~2/8 today, consistent-ish with the filed ⅓–½ rate) — **notable: every occurrence ends gracefully with EXIT 0** via "Game ended: Game error: … DefinePathStage …", so the #154 index's "loop until non-zero exit" repro guidance would never fire; corrected the index line to grep the log instead (left the other instance's appended #094 notes untouched). **Observability gap found:** the unresolved-rule load skips in `ArmyListRuleResolution` go to `Debug.WriteLine` only — invisible in headless output, i.e. effectively silent in real runs, at odds with the skip-with-WARNING intent; recommend a small follow-up routing them through the game log / `ITextOutput`. No code changed in this pass; sweep script + per-faction logs in session scratch.
- 2026-07-02: **Lobby launch gate — decision 9 implemented (warn + host confirm).** New fixture-free `ArmyBuilding.LaunchGate.ValidateArmies(players, lobbyPointsLimit)`: flags any army over the **lobby's** points setting (the saved list's own limit is builder-time advice; the catalog validator's points line is filtered in favour of the lobby's) and, for Forge-built armies, every catalog-validation **Error** attributed per player. `ILobbyViewModel.ValidateArmiesForLaunch()` → host delegates to the gate; client returns empty (only the host launches). **Wire-fidelity fix found en route:** `ArmyListUpdateMessage.FromArmy` serialized by DECLARED type and `DecodeArmy` deserialized as base `ArmyListFile`, so a Forge army's embedded book+selections were silently stripped both on the wire and in the app's lobby load — now encode-by-runtime-type / decode-as-`BuiltArmyFile` (plain armies just leave the embedded fields null), pinned by round-trip tests both ways. App: LAUNCH click runs the gate; problems open a "Launch anyway?" modal listing them in red — **Cancel first and default-focused**, "Launch anyway" is the explicit house-rules override; Resume skips the gate (armies already in play). +6 engine tests (clean; over-lobby-points beats saved limit; Forge Unique error attributed; hand-authored gets points-only; both wire round-trips). Engine 1035/0, app 68/0, full build clean, AH smoke exit 0. **Hand-verify:** load an over-points or two-Unique army in a lobby, click LAUNCH → confirm dialog; Cancel keeps the lobby, Launch anyway starts the game. Engine `0a92a70`, superproject (this commit). With this, all four 2026-07-02 fork decisions (8–11) are implemented or closed.
- 2026-07-02: **Combined squads (#107) — decision 8 implemented.** `BuilderUnit.CombinedWithId` links the second copy to its partner (same author-stable Id machinery as hero-join); each copy buys upgrades under its own normal bounds (GDF "pay for both individually"), and `ListCompiler.Compile` folds linked pairs into one play-time unit — models/cost summed, weapons profile-merged, rules deduped, name "+(Combined)", a hero joined to the absorbed copy remapped to the host. The user's `BuilderList` is **never mutated** (it's the embedded source of truth); only valid same-roster links merge — anything else compiles as separate units and the validator flags it (dangling target, cross-roster, single-model, >2 copies in one combine — all Warnings). **Row-alignment refactor:** merging breaks the positional `list.Units[i]` ↔ `compiled.Units[i]` pairing the validator and the whole Forge screen relied on, so both now work from a new `ListCompiler.CompileRows` (per-row, unmerged) view — and composition/force-org + hero-join validation deliberately runs on the UNMERGED rows so a combined pair still counts as two copies for GDF's duplicate caps. Screen: "Combine with" combo on eligible rows (multi-model, non-hero, not already a combine target), partner-side "Combined with:" breadcrumb, stale-link warning — mirrors the hero-join UX (**hand-verify**: combine two Hive Warriors, save, confirm the saved army plays as one 10-model unit and re-opens as two editable rows). +7 engine tests (merge sums models/cost/weapons + keeps list untouched; per-copy upgrades both land; dangling/cross-roster stays separate; hero-join follows the absorbed copy; validator: valid pair clean + still 2 copies for force-org, dangling/cross-roster/3-copy warnings), +2 app tests (candidate filtering incl. already-linked exclusion; combined pair saves as one merged unit). Engine 1029/0, app 68/0, full build clean, AH smoke exit 0. Engine `82ba5a6`, superproject (this commit).
- 2026-07-02: **Counts-as-terrain primitive — corpus spells 47/47; one-shot movement-grant consumption bug found & fixed.** Decision 10 implemented: new `Effect.CountAsInTerrain(ECountAsTerrain.Difficult|Dangerous)` (param named `Terrain` — a `Kind` property would collide with the polymorphic "kind" discriminator, which STJ rejects as a duplicate) + matching RuleOperation + `MovementRuleQueries.CountsAsInTerrain`. Difficult consumer: `MovementActionContext` caps all three action budgets at `DIFFICULT_TERRAIN_MOVE_CAP_INCHES` **after** bonuses (requests/validation/GUI/AI all inherit the cap — zero resolver changes); Dangerous consumer: `ApplyNonMovementTerrainEffectsStage`/`MovementExecutor` force every moving model to test even with no dangerous terrain on the table. Ignore-terrain rules (Strider/Flying, per scope) beat the counted-as effect. Importer parses "counts as being in X Terrain once" via the synthesized-rule channel → **all 47 books now import every spell** (re-sweep: 0 skips, 47/47 compile). **Bug found while wiring consumption: the shipped movement-grant synthesis was broken** — `MovementActionContext` init ran three LIVE `EvaluateAll` calls (Advance/Rush/Charge), so the FIRST call spent a NextTrigger movement grant and Rush/Charge never saw it (granted Shock Speed gave +2 Advance but no +4 Rush/Charge). Fix: init projection is now read-only (`EvaluateAllNamed`), and a new explicit `RuleEvaluator.ConsumeOneShotGrants(context, participants)` (shares the #101 spend logic) runs in `ExecuteMoveStage` when the move resolves — at both movement hooks — so grants are readable all move (incl. by the dangerous-terrain stage, which runs before ExecuteMove) and spent exactly once; backing out of a move consumes nothing. +8 engine tests (grant boosts all three budgets across repeated projections; spent on move-resolve; difficult cap after bonuses; Strider waives; forced dangerous test with empty table; Flying waives; both terrain spells parse corpus-verbatim). Engine 1022/0, full build clean, Havoc Brothers smoke (embeds "Cursed Stride Effect") exit 0 / zero warnings, AH regression smoke exit 0. Bundled HEF/AH unchanged (no terrain spells; no regen). Engine `1da6b12`, superproject (this commit).
- 2026-07-02: **Pick-bound fix — OPR `select` now imported; inter-upgrade-dependency gap resolved by investigation.** Corpus key-enumeration showed the importer never consumed `section.select` (the PICK bound: "Upgrade with one/any/up to two:", distinct from `affects` = models touched), so every section defaulted to `MaxPicks = 1` — mis-bounding the corpus's 118 "any" + 4 "up to two" sections: the P4 validator false-flagged legal multi-picks and the Forge screen wrongly rendered those sections as mutually-exclusive radios. Now mapped: `any` → options count, `exactly`/`up to` → value (423 "exactly 1" sections were accidentally right all along). The screen keys radio-vs-checkbox and mutual exclusion off `MaxPicks`, so correct data fixes the UI control choice with no app change (**hand-verify:** Great Elemental / Titan Elemental "Upgrade with any / up to two" should now be checkboxes allowing 2 picks). Also noted in-code: OPR variant `attachment` ("Take one X attachment", 47 sections) is additive — the existing fall-through to Upgrade is semantically right, now deliberate. **Both bundled books regenerated from the new snapshots** (first regen through `--import-opr <snapshot> <book> <supplement>` end-to-end): HEF gets maxPicks 2 on the two Elemental sections, AH gets 3 on two counted replace sections (harmless — steppers govern counted sections), supplement re-embedded (5/10), ids churned as usual. **Investigation result recorded:** OPR encodes NO inter-upgrade dependency/prerequisite/exclusivity data anywhere in the 47-book corpus (key enumeration over all 2,629 sections / 6,965 options: no such fields) — section structure + our target-clamping already express the constraints, so the "inter-upgrade dependencies" gap is closed as not-applicable rather than deferred. Verified: engine 1016/0 (+1 select-mapping test), full build clean, both books totality-compile (28+41), AH smoke exit 0 / zero warnings. Engine `5c48c99`, superproject (this commit).
- 2026-07-02: **Spell-shape extension — corpus spell skips 13 → 4.** `TryParseSpell` now handles three more corpus shapes: (1) **morale-test-then** ("must take a morale test. If failed, it becomes fatigued / you may move it by up to N\"") → `Effect.MoraleTestThen(ApplyFatigue | TriggeredMove(N, optional))` — the #034 primitives whose doc comments literally name Terrifying Fury and Deep Hypnosis; covers 4 spells. (2) **movement-modifier grant** ("moves ±A\" when using Advance actions and ±R\" when using Rush/Charge actions once") → the importer **synthesizes** a "<Spell> Effect" `SpecialRuleDefinition` (three signed `MovementBonus` hooks, valence by sign) into `book.RuleDefinitions` — the #059 embedded channel, regenerated with the book so nothing is hand-maintained — and the spell grants it by name with NextTrigger like the grant family; covers Shock Speed ×3 + Deceleration Rune (which has a trailing space in the OPR data — spell names now trimmed). (3) **AP-loss** ("loses AP(1) when shooting once") → synthesized attacker-side `ReduceArmorPenetration` at `Shooting_OnHitRollComplete` gated `Not(IsMelee)` (same floored-at-AP(0) primitive Fortified uses defender-side); covers Corrode Weapons. Synthesized definitions are RuleValidator-checked at import — a violation skips the spell rather than shipping a book that fails the load gate; `BookRuleSupplement.Apply` leaves them alone (replace-by-name only touches supplement names). **Still skipped (recorded): the counts-as-terrain family** — Cursed Stride (Dangerous) + Aura of Pestilence ×3 (Difficult) need a "counts as being in X terrain" engine primitive; defer to #100 territory. Verified: engine 1015/0 (+1 corpus-verbatim importer test covering all five new shapes incl. the honest terrain skip), full build clean, re-sweep 47/47 import+compile with only the 4 terrain skips remaining, bundled HEF/AH regen-diff clean (ids only), and a Human Defense Force smoke (embeds synthesized "Shock Speed Effect" + supplement "Unique") plays headless to game end, exit 0, zero unknown-rule warnings. Engine `45dca49`, superproject (this commit).
- 2026-07-02: **Unique enforcement — one copy per army.** Closes the gap recorded in the rule-supplement slice: `ListValidator` now flags a second copy of any roster unit carrying the Unique rule as an **Error** on that list row ("the army may only include one copy") — an absolute OPR list-building rule, unlike the size-dependent force-org copy caps which stay Warnings. Detection is alias-aware + case-insensitive via a new shared `HasRule` helper (matches an alias's own name and recurses into the aliased rule, same semantics as the resolver). Surfaces through the existing P4 badge/row-marker/issues-list UI with no app change. +3 engine tests (second copy → exactly one Error on index 1; single copy clean; non-Unique duplicates untouched). Engine 1014/0, full build clean; validator has no play-path callers (per the hero-join slice investigation) so no smoke needed. Engine `96daae2`, superproject (this commit).
- 2026-07-02: **OPR snapshot capture + corpus-wide importer sweep — the pre-Sept-2026 constraint is closed.** All **47 official GrimDark Future army-book JSONs** captured from the Army Forge API (index: `/api/army-books?filters=official&gameSystemSlug=grimdark-future`; books: `/api/army-books/<uid>?gameSystem=2`) → **off-repo** `../GDF Armies/opr-json-snapshots/` (+ `_index.json` + README with date/endpoints/rules). Off-repo because the raw dumps include background/lore prose — in-repo `.fdgbook`s stay functional-data-only per the standing constraint; snapshots are the **importer inputs** that make books regenerable (this also closes the risk found during the supplement slice: no source JSONs existed on disk at all). Versions: 3.5.3 main corpus, 3.5.2 the four Titan Lords sub-books. **Reproducibility proven:** fresh `--import-opr` + supplement of HEF/AH reproduces the committed books exactly except regenerated upgrade-section/option ids (functionally inert — saved armies embed their own book snapshot, so id drift can't dangle). **Sweep: 47/47 books import AND compile every unit** (`--book-to-army` totality, 8–41 units each). Supplement embeds land cross-faction (Unique ~14 books, Re-Position Artillery 14, Infiltrate 5, Destructive 3, …) and a corpus text check confirms **every supplement rule name has exactly one description variant across all 47 books** — OPR's shared vocabulary is consistent, so by-name embedding is faithful. **13 spell-parse skip warnings recorded (not cut):** the unparsed spell shapes are movement-modifier grants ("moves ±X when using Advance/Rush/Charge … once"), dangerous-terrain-once (Cursed Stride), and AP-loss-once (Corrode Weapons) — a future `TryParseSpell` extension; affected: Dwarf Guilds, Havoc Bros, HDF, Human Inquisition, Machine Cults, Plague Disciples, Soul-Snatchers, Titan Lords (+2 subs), War Disciples, Wormhole Plague/War. No in-repo code changed (bundled books remain HEF+AH by design; sweep artifacts in scratch). Also refreshed the stale #153 index line (was still "PROPOSAL / no code written").
- 2026-07-02: **Rule-supplement slice — 14 faction rules authored; HEF 43/49, AH 49/62 resolve.** Design fork resolved with user: **definitions live in the files themselves** (books/armies stay self-contained so a version-behind army still works), sourced from a curated supplement — `FdgRaylib/Assets/Books/GdfRuleSupplement.json`, an array of `SpecialRuleDefinition` in the same RuleJson kind-schema `ruleDefinitions` already uses. Engine `BookRuleSupplement` (`FDG.ArmyBuilding`) merges it into a book: seeds from every rule name the book references (unit/weapon/item/upgrade entries incl. alias targets, plus rule names the book's **spells** grant), transitively pulls in definitions that included definitions grant (aura → boost), **hard-fail validates** (RuleValidator capability fit + granted names must resolve in core∪supplement + duplicates + null shapes), and embeds replace-by-name (idempotent re-apply). Hand-authoring safety: the loader is **strict** (`JsonUnmappedMemberHandling.Disallow` — a `hookId`-for-`hookID` typo throws instead of silently defaulting); `--validate-rules <file>` is the standalone authoring check (the "utility" from the user's sign-off; full authoring UI stays #087). CLI: `--apply-rules <book> <supplement>` retrofits existing books in place (needed: **no OPR source JSONs exist on disk** — only the off-repo PDFs — so books can't be regenerated by re-import today; re-snapshot before ~Sept 2026 still pending); `--import-opr` takes an optional third supplement arg for future imports. Small engine primitive added: `TargetSelector.RequiredRule` (case-insensitive rule filter in `PreAttackTargeting`, for "pick one friendly with Artillery"). **Authored (initial JSON generated from compiler-checked C#, then committed as source of truth):** HEF — Highborn (+2" Adv/Rush/Charge), Highborn Boost (+2" more if Highborn), Highborn Boost Aura, Re-Position Artillery (pre-attack: move a friendly Artillery unit ≤9", uses RequiredRule), Unique (inert marker w/ description; builder-side one-copy enforcement **not** done — recorded gap); AH — Hive Bond (+1 morale), Hive Bond Boost (+1 more), Hive Bond Boost Aura, Destructive (weapon-scope Crack-with-AP(+4)), Predator Fighter (melee 6s → extra hit, same approximation as core Furious/Relentless), Infiltrate (Ambush-shaped DeferDeployment at 3"), Breath Attack (**approximation: the "roll 2+ to activate" gate is omitted** — DealHits(1, Blast(3), AP(1)) always fires when used), Precision Debuff (StatModifier −1 hit, NextTrigger), Stealth Buff (AddRule Stealth, NextTrigger). **Still inert (deferred, #100 Parts 2/3 shapes):** Caster Group, Spell Conduit, Teleport, Crossing Attack, Piercing Spotter/Tag/Growth, Unwieldy Debuff (needs a StrikeLast primitive), Ravage, Regenerative Strength, Retaliate, No Retreat, Self-Destruct, Spawn, Surprise Attack, Takedown Strike, Unpredictable Fighter Mark (Unpredictable family needs a roll-branch primitive). Verified: engine 1011/0 (12 new supplement tests + RequiredRule targeting test), full build clean, both books re-applied idempotently (HEF embeds 5, AH 10 — boosts arrive transitively), `--book-to-army` totality 28+41 units, AH `--army` headless smoke exit 0 with **zero unresolved-rule warnings** (previously Hive Bond warned). Engine `61a81d5`+`e5bfce3`, superproject (this commit). **Awaiting GUI hand-verification** (e.g. Breath Attack/Stealth Buff offered pre-attack in play; Hive Lord morale +1).
- 2026-07-02: **Rule-coverage measurement + next-slice sketch (authored, not yet started).** Measured each bundled book's distinct special-rule names against the core catalog's 112 canonical definitions (case-insensitive exact match, same semantics as `RuleResolver`): **High Elf Fleets 39/49 resolve**, **Alien Hives 40/62** — the rest load as inert name refs (engine skip+warn; games still play). Inert HEF: Caster Group, Crossing Attack, Highborn (+Boost Aura), Piercing Spotter, Re-Position Artillery, Spell Conduit, Teleport, Unique, Unwieldy Debuff. Inert AH: Breath Attack, Caster Group, Destructive, Hive Bond (+Boost Aura), Infiltrate, No Retreat, Piercing Growth, Piercing Tag, Precision Debuff, Predator Fighter, Ravage, Regenerative Strength, Retaliate, Self-Destruct, Spawn, Spell Conduit, Stealth Buff, Surprise Attack, Takedown Strike, Unique, Unpredictable Fighter Mark. **Next slice (user-approved direction): author the aura/boost/buff-shaped inert rules as embedded `RuleDefinitions`** — the #059 Layer-2 pipeline already works end-to-end (`BookFile.RuleDefinitions` → `ListCompiler` copies into the compiled army → load registers/overrides), so no engine change expected for that subset. **Design fork to surface before building:** `.fdgbook` files are regenerated artifacts of `--import-opr`, so hand-edits to the books get wiped on re-import — the authored definitions need a durable home (likely a curated supplement the importer merges by rule name; decide at slice start). Genuinely-new-primitive rules (Teleport, Spawn, Self-Destruct, dice-pool/marker shapes) stay deferred to #100 Parts 2/3. Ground truth for rule text: the OPR PDFs at `../GDF Armies/` (off-repo, never commit) and the source OPR JSON's rule descriptions.
- 2026-07-02: **Spell-import slice — OPR spell lists become real, castable SpellDefinitions.** `OprBookImporter.TryParseSpell` parses the formulaic OPR effect prose into the #033 vocabulary — three corpus shapes: **damage** ("Pick one enemy unit/model within N\", which takes X hits with R1, R2") → `Effect.DealHits` (AP folded into `ArmorPenetration` like weapon import, other rules ride `WithRules` for load-time resolution; a "model" target sets `TargetSelector.SingleModel`, LoS required); **grant** ("…which get(s) RULE once (next time…)") → `Effect.AddRule(RULE, ELifetime.NextTrigger)`; **mark** ("…which friendly units get RULE against once") → `Effect.MarkTarget` (#034 family; matched before grant since its text also contains "gets…once"). Anything else is **skipped with a warning, never silently or half-wrong** (CLI prints each skip + a spell count). Result: **12 of 12 real spells import** (6 Alien Hives + 6 High Elf Fleets — Terror Seeker needed the mark shape). Compiler already carried `Book.Spells` → army; `FDGServer` resolves them at load (`ResolveSpells`, tolerance for OPR-only rule names like "Destructive" verified by smoke). Forge roster pane lists the book's spells via the engine's `SpellText.Describe`. Casters come from upgrade options (e.g. "High Oracle (Caster(2))") which already import. +2 importer tests (corpus-verbatim shapes incl. mark; unparseable → warn). Engine 998/0, app 66/0; spell-carrying army loads + plays to game end. **Awaiting GUI hand-verification** (spells block in roster pane; a real cast needs a Caster upgrade picked + GUI play).
- 2026-07-02: **Hero-join slice — #006 joins are authorable in the Forge.** Hero units (compiled `ForceOrgValidator.IsHero`) get a **"Joins unit"** combo in the config pane: hosts are other multi-model non-Hero list units (duplicate squads disambiguated "#2"), link by author-stable `BuilderUnit.Id` (GUID `EnsureId`, generated on demand — mirrors the freeform builder); hosts show a "Joined by:" breadcrumb. Engine `ListValidator.ValidateHeroJoins` mirrors #006 eligibility as **Warnings** (join set but no Hero rule; hero not single-model; `Tough > HeroJoinResolver.MaxHeroToughForJoin`; target missing; target ineligible), plus a shared alias-aware `TryGetToughValue`. Compiler already passed `Id`/`JoinsUnitId` through — now pinned by test. +4 engine tests (valid join compiles through + 3 warning paths), +2 app seam tests. Engine 997/0, app 66/0. **Smoke-flake investigation:** the `--army` HEF smoke intermittently dies with a `DefinePathStage` cohesion game error (~half of runs). **Proven pre-existing:** stashed both worktrees and reproduced at clean HEAD (1/3 runs); `ListValidator` has zero callers in the play path (Forge screen + tests only). It's the known intermittent AI-movement cohesion crash (#017 note; #094/#108 territory), evidently easy to trigger with this High Elf composition — **filed as #154** (2026-07-02) with the repro recipe. Alien Hives smoke passes. **Awaiting GUI hand-verification** (join combo + breadcrumb).
- 2026-07-01: **Base-size slice — OPR `bases` now import.** Closes the gap recorded in the item slice: `OprBookImporter.MapBase` maps `bases: {round, square}` (mm) → `BaseFileEntry`. Prefers the **round** basing (GDF standard): `"32"` → 32mm Circle; `"120x92"` (an oval) → our **Rectangle** footprint (engine has no oval; #149's rectangle is the dimension-faithful stand-in); `"none"`/empty → falls back to the square spec (`"175x125"` → Rectangle, `"40"` → 40×40); neither → default 28mm circle. Tolerant parse (invariant culture, `LooseStringConverter` insurance for number-typed strings). The compiler already copies `roster.Base` into the compiled unit, so the whole chain lit up from the importer alone — Hive Lord is now a 4.72"×3.62" rectangle in the regenerated books and **played through a full headless game** (deployment/movement on `RectangleBase` exercised via `--army` smoke). +1 importer test covering every observed variant. Engine 993/0, app 64/0. Both books regenerated. Engine `c490616`, superproject `3a997bf`.
- 2026-07-01: **Item-fidelity slice — OPR items are first-class.** New `ItemEntry` (name + quantity + granted rules) in `FDG.ArmyBuilding`: **unit-level items now import** (previously dropped entirely — e.g. Retributors' Combat Shields never made it into the roster), and **item gains keep their names** (`UpgradeOption.ItemsGained`; previously flattened to bare rules, losing "Sgt. Armor" etc.). Compiler: Replace targets now match+deduct **items as well as weapons**; at the end of compile, item rules flatten into the unit's `SpecialRules` (deduped) so play is unchanged. Two correctness upgrades found while re-deriving semantics: (1) **combined targets ("X and Y") bound One/Any by the MIN across targets** — each application consumes one of each, so you can't take the swap unless every part is present (was sum); All keeps max. (2) **Choices now apply in the book's section order, not click order** — "Replace one Shard Carbine" clicked before "Replace all → Shard Carbines" now works regardless of click sequence. Screen: items display as gray wargear lines ("5x Combat Shields (rules)") in roster preview + compiled unit; availability/steppers item-aware; stepper budget fix (an option's own count returns to its cap so you can step back up). +4 engine tests (unit-item import, named item gain, Retributors-shaped replace-all w/ item deduction + rule un-flatten, combined-target min, section-order). Engine 992/0, app 64/0, app build 0 warnings; both books regenerated (Retributors now carry `Combat Shield x5 (Shielded)` — verified in the generated file); all 28 HEF units compile. **Newly recorded gap:** OPR's `bases` field (per-unit base sizes in mm) is not imported — every roster unit gets the default 28mm circle; vehicles/monsters should be bigger (future slice, pairs with #149 base shapes). Full-game headless smoke completed via a new `--headless --army <path>` flag (added because the printf-pipe idiom was blocked by a transient tool outage; it's the better scripted-smoke interface anyway — both players load the file, then EOF defaults). Committed: engine `f5398f1`, superproject `b42cf6f` (ledger note updated post-commit).
- 2026-07-01: **Hand-verify round 1 — GUI works! Fixed the reported issues.** User verified in-window (High Elf Fleets); it builds/plays. **Batch A (UI, `18f8f6d`):** remove-`x` button (full-width Selectable ate the click → `SetNextItemAllowOverlap`); double-click a roster unit to add; roster shows gray Qua/Def, center list shows a per-unit stat block (Qua/Def + weapons + rules); book-switch now confirms before discarding a non-empty list. **Batch B (upgrade semantics):** the deep correctness fixes — captured OPR's `affects` **bound** on import (`UpgradeSection.MaxApplications`; `exactly/up-to N`: N≤1→**One** toggle, N>1→bounded **Any** stepper; `any`→unbounded stepper; `all`→toggle over all matched). Compiler now **clamps replacements to targets actually present** (so "Replace one Shard Carbine" with no Shard Carbine is a no-op, not a phantom add) and does **plural-tolerant target matching** (`ListCompiler.TargetMatches`/`AvailableTargets`, normalize case + one trailing `s`) — which fixes "Replace all Energy Swords" removing 0 / adding 1 (now removes all 5, adds 5). UI: **radios** for pick-one-of-several, **checkboxes** for single/multi toggles, **steppers** for any/add-models, and Replace controls **gray out with "(none to replace)"** when the target is absent. Books regenerated. +3 compiler tests (no-target no-op, plural all, up-to-N bound). Engine 988/0, app 64/0, build clean, imported army plays exit 0. **Remaining gaps (recorded):** rule/item targets (e.g. "Combat Shield") aren't removed on replace — only weapon targets; still deferred: hero-join UI, spell lists, lobby hard-block.
- 2026-07-01: **P4 landed — validation/legality (logic-first).** Engine `ListValidator` (`FDG.ArmyBuilding`): points-limit (Error), per-unit **model-count range** and per-section **pick caps** (catalog-specific), reusing the engine `ForceOrgValidator` for composition **warnings** (hero/copy/all-heroes). Pure/headless — 5 `ListValidatorTests`. Promoted `UpgradeSection.IsCounted` to the engine (shared by validator + screen; removed the app duplicate). **Minimal UI surface** (kept low-commitment so it survives any layout rework after hand-verify): a legality badge in the toolbar (`[Legal]` green / `[N error(s)]` red / `[N warning(s)]` yellow), a red `!` marker on offending list rows, and a wrapped issues list at the bottom of the list pane; over-limit points stay red. Advisory only — it flags, doesn't hard-block Save (matches Army Forge; lobby-side launch-blocking is a separate follow-up). 2 app tests via a new `Issues()` seam. Engine 985/0, app 64/0, build clean, headless exit 0. **Awaiting GUI hand-verification.** With P4 the plan's core (P0–P4) is complete; remaining are import-fidelity depth + the deferred items (MaxPicks>1 already validated; size/exact-N bounds, hero-join UI, lobby hard-block).
- 2026-07-01: **P0b landed — OPR importer + Alien Hives & High Elf Fleets books.** Engine `OprBookImporter` (`FDG.ArmyBuilding`) maps OnePageRules Army Forge JSON → `BookFile`: unit stats, weapons (AP folded into `ArmorPenetration`), rule names (ratings tolerant of number-or-string), and upgrade sections (variant replace/upgrade, affects any/all/one, targets, options with weapon/rule gains + `ArmyBookItem` flattened into its bundled rules). **Functional game data only — never the background/lore prose.** Data is OPR's, used under **CC-BY-SA 4.0**: each book carries `Source`+`License` (new `BookFile` fields) shown in-app under the roster. Two thin CLIs (`--import-opr`, `--book-to-army`) drive the engine importer/compiler. Generated + bundled `FdgRaylib/Assets/Books/{AlienHives,HighElfFleets}.fdgbook` (198/165 KB); `ArmyForgeScreen` now loads all bundled `.fdgbook` + the demo book with a book-picker combo. **Verified:** all 41 + 28 units compile (9245 / 7125 pts); an imported Alien Hives army plays a full headless game to exit 0; 4 engine `OprBookImporterTests` (synthetic input) lock the mapping. Engine 980/0, app 62/0, build clean. **Recorded fidelity gaps (v1, not silently cut):** `affects` exactly-N / up-to-N → mapped to Any with the bound *not yet enforced* (P4); combined-unit / "+N models" size upgrades **not** synthesized (units import at base size); caster **spell lists not imported** (need engine Effect graphs); **custom OPR rules import as inert name refs** (no gameplay effect — engine already skips unimplemented rules). **Awaiting GUI hand-verification.** The reusable engine importer means re-snapshotting for the ~Sept OPR update is re-running `--import-opr`.
- 2026-07-01: **P3 landed — interactive upgrades; core army builder is complete.** The config pane's upgrade sections are now editable for the selected list unit: single-select (`Replace`/`Upgrade` Affects=One) as mutually-exclusive checkboxes, `Replace-All` as on/off, `AddModels`/`Any` as a numeric stepper (clamped to a computed max). Each toggle writes an `UpgradeChoice` into the `BuilderUnit`; the per-frame recompile re-costs + re-statblocks live. Testable seams `IsCounted`/`ChoiceCount`/`IsChosen`/`SetChoice` — 4 new tests proving the UI path reproduces the compiler's costs (warriors 106, gunners 165). **End-to-end verified:** a fully customized army (plasma + 2 added warriors + 3 missiles, 271 pts) built via the choice path plays a full headless game to exit 0. App 62/0, engine 976/0, build 0 warnings. **Deferred (recorded):** MaxPicks>1 caps, hero-join UI, points-limit *enforcement* (header flags over-limit red but doesn't block) → P4. **Awaiting GUI hand-verification** (P1+P2+P3). Next: P4 validation/legality, then P0b OPR snapshot.
- 2026-07-01: **P2 landed — list-building in `ArmyForgeScreen`.** The list pane is real: select a roster unit → "+ Add to list", per-row remove (`x`), and the points header live-recompiles via `ListCompiler` every frame (red when over limit). **Save** writes the single embedded `.fdgarmy` (serialized as `BuiltArmyFile` so the selections + full book ride along); **Load** deserializes as `BuiltArmyFile` and `AdoptLoaded` reopens it into an editable session (a plain army with no embed is refused with a hint → use the Army Builder). The compiled `.fdgarmy` loads straight into the lobby's existing "Load Army" — no new launch wiring. Config pane shows the selected list unit's compiled stats; **upgrade-option *editing* is still read-only (P3).** Testable seams `AddToList`/`RemoveFromList`/`Compile`/`AdoptLoaded` — 5 new app tests incl. a Save→Load round-trip (185 pts base). App 58/0, engine 976/0 (untouched), build 0 warnings, headless exit 0. **Awaiting GUI hand-verification** (P1+P2 together). Next: P3 — wire upgrade options to mutate + re-cost.
- 2026-07-01: **Architecture correction — backend moved into the engine submodule (`FDG.ArmyBuilding`).** User flagged that army definition/compile/import must be library-portable (any app reuses it; the app is only the GUI). I had wrongly built it all app-side (decision 4, to dodge the read-only submodule). Reversed with the user's authorization: `BookModel`/`BuilderList`/`BuiltArmyFile`/`ListCompiler`/`DemoBook` → `FutureOfDarkGrimness/ArmyBuilding/` (namespace `FDG.ArmyBuilding`); backend tests → engine `FDG.Tests`; only `ArmyForgeScreen` + its formatters remain in `FdgRaylib/` (usings → `FDG.ArmyBuilding`). Engine 976/0 (+7 moved tests), app 53/0, headless smoke exit 0 (compiled army still plays). Submodule-first cadence: submodule branch `153-army-forge-builder` committed first, then the superproject bumps the gitlink. Future OPR importer (P0b) also lands engine-side.
- 2026-07-01: **P1 landed — `ArmyForgeScreen` (three-pane, user's chosen layout), wired into the app.** New `IAppScreen`: roster pane (browse the book's units) | list pane (empty placeholder; building is P2) | config pane (selected unit's stat line + weapons + rules + upgrade sections, read-only) + right-aligned points header. Loads `DemoBook`. Wired: `RaylibRenderer.ArmyForge`, a MainMenu "Army Forge" button (menu now 6 buttons; start/gap tightened), `Program.cs` nav + Back. Reuses `ArmyBuilderScreen.WeaponSummary` + `SpecialRuleEntry.PrintableName`. 4 `ArmyForgeScreenTests` on the pure formatters. App 60/0, build 0 warnings, headless exit 0. **Awaiting GUI hand-verification** (no display in the build env — please eyeball the screen + Back nav). Next: P2 — add/remove units, live compile, Save `.fdgarmy`.
- 2026-07-01: **P0 slice 2 landed — the `ListCompiler` (the heart).** `DemoBook` (our own FDG "Dark Vanguard" — original IP, core rules only) + `ListCompiler.Compile(book, list) → BuiltArmyFile`: aggregate weapon-count ops (replace-one, replace-all cost-scaled by target count, add-models count-scaled with the added model's default weapon, upgrade→rule grant), carries rule/spell defs for the RuleValidator gate, embeds selections + full book. 4 `ArmyForgeCompilerTests` (deterministic loadouts + costs). **Headless smoke: the compiled army played a full 4-round game to exit 0**, with the Missile Launcher's `AP(2)`/`Blast(3)` resolving in the real engine — strongest end-to-end proof. App 56/0, engine 969/0, build clean. Compiler-first per user (defers OPR snapshot, still ahead of the ~Sept deadline). Next: P1 `ArmyForgeScreen` UI. Data-source decision refined → OPR import is a one-time snapshot to a bundled local `.fdgbook` library (see "OPR versioning strategy").
- 2026-07-01: **BUILD STARTED (user green-lit). P0 slice 1 landed** — `FdgRaylib/ArmyBuilding/` catalog + selection + embedded-file types (`BookFile`/`RosterUnit`/`UpgradeSection`/`UpgradeOption`, `BuilderList`/`BuilderUnit`/`UpgradeChoice`, `BuiltArmyFile : ArmyListFile`) and `ArmyForgeSerializationTests` (3 tests). Proven: the format round-trips through `RuleJson.Options`, and the **engine reads a builder-saved file as a plain `ArmyListFile`, dropping the embedded `selections`/`book`** — the no-engine-change claim, now under test. App suite 52/0, engine 969/0 (untouched), full build clean 0 warnings. Next: P0 slice 2 = `ListCompiler`. Deeper audit (2 codebase probes + PDF check) folded into the plan first — see "Audit findings (2026-07-01)".
- 2026-07-01: Locked the embedded-book granularity (decision 6) to **full book snapshot** at user's request — an old army stays reproducible/re-editable as-authored even after the catalog book drifts; embedded book is authoritative on open, migration is explicit opt-in. Still a proposal — not building yet.
- 2026-07-01: Refined after locking the four scoping decisions (see Decisions): full scope; both data sources in parallel; new screen keeping freeform; single self-contained `.fdgarmy` with the editable list embedded. Verified the embed needs **no submodule change** — `RuleJson.Options` (`RuleJson.cs:9`) leaves STJ's `UnmappedMemberHandling` at the default (Skip), so the engine ignores the extra block. Still a proposal — not building yet.
- 2026-06-30: Plan authored at user's request as a hold-until-go artifact. User is undecided on whether to build at all; a green-light (and, for OPR data, OPR clearance) unblocks it. Investigated Army Forge's live API + our army-file/runtime seam to ground the plan; no code written.
