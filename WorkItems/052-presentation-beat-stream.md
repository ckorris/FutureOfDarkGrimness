# 052 — Presentation beat stream (engine-owned pacing for lifelike play)

**Status**: in-progress
**Related**: engine submodule branch `052-presentation-beat-stream`

## Goal
The game currently teleports models, vanishes the dead instantly, and surfaces "what happened" only as flat text logs. Make play feel lifelike: moving models glide, getting shot plays projectile → save/hurt → death beats with real pacing, dice tumble, and stage/decision changes flash. "Done" for the *architecture* slice: the engine owns a **presentation-beat stream** — an ordered sequence of semantic, paced beats emitted inline from stages via `await context.Present(beat)` — and the Raylib app consumes it as animations while headless degrades it to text. Any future app on the same submodule would reuse the beat stream and its pacing one-to-one; only the rendering differs.

## Core architecture (decided 2026-06-07/08)

**Ownership principle (user's):** anything a brand-new app on this submodule would re-derive identically belongs in the engine; only the literal pixels belong in the app.

**Engine owns:**
- The **beat stream**: ordered, semantic beats (`UnitMoved`, `ProjectileFired`, `SaveMade`, `WoundDealt`, `ModelDied`, `DiceRolled`, `StageEntered`, …), each carrying its render payload **and a nominal duration**.
- **Pacing**, via `await context.Present(beat)` — emits the beat to all consumers, then awaits its nominal duration on a wall clock. **Not** an ack handshake with the renderer; the engine never waits on the consumer, it sleeps a duration *it* owns.
- Replication of the stream host→clients (it's canonical engine output).

**App owns:**
- The retained **visual model** / per-model actor state machines (idle/flinch-saved/flinch-hurt/dying/dead), tweening, dice meshes, banner layout — the stuff a 3D client would reimplement differently.
- A **delta-time tween/timeline** system (none exists today; `GetFrameTime()` is unused).
- **Cosmetic sub-timing within the engine's duration envelope** — engine says "ranged volley, these outcomes, ~1500ms"; app distributes tracers/sparks/death inside that 1500ms however it likes.

## Decisions
- **Free-running, not blocking.** The sim is paced on a wall clock it owns, never gated on the renderer. This is what avoids the "awful pause" *and* the Civ-style catch-up artifact: a real-time producer leaves no backlog to replay in slow motion. (Rejected: engine dumps beats into a queue that the app drains at nominal speed — that *is* the Civ model.)
- **Inline emission, not a decorator/observer.** Under the ownership principle an outside observer can't own pacing and can't see mid-resolution state (only aggregate histograms / per-model wounds), so emission must be inline in stages — shaped like the existing `context.Log()` calls. The decorator idea dissolves under the principle. Cost: combat stage files gain `Present()` calls (additive, narration-like).
- **Engine emits *semantic* beats with *total* duration; app sub-divides cosmetic timing.** Keeps the engine from having to model individual tracers while still owning the pacing envelope.
- **Multiplayer = synchronized / host-authoritative.** Falls out for free: the beat stream is canonical engine output, so it replicates; the host paces by sleeping, clients receive in real time → same sequence, same tempo, same real dice values everywhere. Likely **subsumes the existing `AddTempVisualMessage` channel** — decide whether to formalize or retire it.
- **Headless degrade = duration scale 0×.** Keep *state computation* instant always; only *emission* sleeps, and that sleep is injectable so tests/automation stay fast and rules logic stays deterministic (no real-time dependency in the rules).
- **Logs and beats are siblings, not parent/child.** Existing `Log()` calls keep firing unchanged. A beat *may optionally* carry a text projection for CLI/unified-feed convenience, but logs are not forced through beats nor vice-versa. (User correction to an earlier "log is just a projection of the stream" idea.)
- **Dice beat carries histogram + side count + threshold + roller mode — not "dice".** `IDiceResults` is a per-face *float* histogram. `RealisticDiceRoller` → integer face counts → render N tumbling dice on those faces, highlight ≥ threshold. `ProbabilisticDiceRoller` → `rollCount/sideCount` per face → no discrete dice exist; render a different vocabulary (probability bar / fractional / expected-value). Mode rides on the beat (don't infer from integrality — probabilistic counts can be whole by coincidence).
- **Probabilistic mode is fractional end-to-end.** Hits, saves, *wounds*, and *deaths* are fractional (a model dies when fractional accumulated wounds cross its total). `WoundDealt`/`ModelDied` beats, health/damage representation, and the app's whole vocabulary must tolerate fractions in this mode — not just the dice widget. App keeps two presentation vocabularies keyed off the mode flag.

## Relevant existing-code facts (from Apr/Jun 2026 survey)
- Rendering is pure immediate-mode off *final* state (`RaylibRenderer.DrawModels` reads `model.Position` live each frame); dead models are `GetIsAlive()`-skipped and the engine never removes them → app already has full control of death visuals.
- `OnPositionChanged` / `OnWoundsDealt` are `DataValueChangedHandler<T>` → carry **(old, new)**; the deltas to animate exist at the instant of change but nothing captures them.
- Combat resolves as one synchronous aggregate burst; results are histograms, no per-shot target identity. **`AssignWoundsResults.PendingWounds` is per-model**, so which model took/lost how many is recoverable; per-shot→per-model mapping for saves must be synthesized app-side.
- Only blocking engine→app backchannel today is the resolver request/reply (a TCS the engine awaits). Logs + data-sync + an existing `TempVisual` channel are fire-and-forget.

## Testing stance
Solid unit tests required throughout (user ask). Key targets:
- `Present()` pacing: with duration scale 0× emission is instant and ordering preserved; with non-zero scale total elapsed ≈ sum of nominal durations (use an injectable clock, not real `Task.Delay`, so tests are deterministic and fast).
- Beat *ordering & payload* per stage: a scripted shoot/melee resolution emits the expected beat sequence with correct per-model wound/kill data and dice histograms.
- Dice beat fidelity in both roller modes (integer multiset vs balanced fractional); threshold/success counts correct.
- Replication: beats emitted host-side arrive client-side in order with identical payloads (incl. real dice values).
- Headless: beat stream present but instant; existing logs still fire unchanged.

## Notes
- 2026-06-08: **Dice beats — app rendering done (#11).** `PresentationPlayer` tracks the active `DiceRolledBeat` + progress (`TryGetActiveDice`); since it's an active beat, `IsAnimating` already gates the resolver prompts so you see the roll before reacting. New `DiceOverlay` (FdgRaylib) draws a bottom-center forefront strip over the table area (`layout.LogX` width): **Realistic** → expands the histogram into pip-face dice, a settle flicker for the first 40% then locks, successes green / failures gray, with label + tally; **Probabilistic** → a labeled success bar (no discrete dice). Renderer calls it after `DrawModels`. Full solution builds. **User note:** the Probabilistic view may want to be *hidden* entirely later (it leaks expected-value info) — bar is a placeholder for now. **Pending:** GUI eyeball of the actual look (needs a display). App-side `DiceOverlay`/`PresentationPlayer` untested per the library-tests-only call.
- 2026-06-08: **Dice beats — engine side done.** `DiceRolledBeat` (FDG.Presentation.Beats) carries per-face counts as `float[]` (fractional-safe), `SideMin`, `SuccessThreshold`, `ERandomnessType Mode`, and a `Label`; computed `Total`/`Successes`/`SideMax`/`Text`; `From(IDiceResults,…)` factory; `PresentationDurations.DiceRoll` = 700ms. Mode rides on the beat (not inferred from integrality, which probabilistic rolls can hit by coincidence). Emitted inline: `RollToHitStage` ("To Hit", the natural roll — synthetic extra-hits excluded) and `RollToSaveStage` (one "To Save" beat per AP group). Tests added to `PresentationBeatSerializationTests` (3): `From` capture + success math, Realistic wire round-trip, Probabilistic round-trip preserving fractions + mode. Full suite **303 green**; headless plays to victory (rolls happen, instant clock, null sink). **Deferred:** dangerous-terrain d6 (sync/out-of-band, no beat yet). **Pending:** app-side forefront dice rendering (#11) — has feel decisions (placement, pips/numbers, tumble vs settle, fractional vocabulary) to settle with the user. Engine dice changes uncommitted.
- 2026-06-08: **#6 complete — TempVisual fully retired.** The `FutureOfDarkGrimness/TempVisuals/` folder is excluded from compilation (`<Compile Remove="TempVisuals/**/*.cs" />` + `<None Include>` to keep it visible) and kept on disk with a `_RETIRED.md` pointer — preserved, not deleted. All woven references removed: the `ITempVisualDrawer? TempVisualDrawer` property off `IGameContext`/`GameContext`/`IFDGGame`/`IPlayerController` (+ Local/Network/Computer controllers + `FDGGame_AsLocal`/`AsClient`), the `tempVisualDrawer` parameter off the `GameContext` ctor and `AssignInterfaces` (and all 3 call sites: ComputerPlayerController, CliApp, LobbyScreen), `FDGServer`'s relayer field/creation/ctor-arg, and `FDGGame_AsClient`'s 5 message registrations + 5 handlers. App-side `CliTempVisualDrawer` disabled via `#if false` (kept for reference). The dead `ChooseMapSideStage.TempTestVisuals` probe stays commented. Test doubles updated (`NullTempVisualDrawer` removed; 5 contexts de-referenced). Decision recorded: chose csproj-exclude over hand-commenting 10 interdependent files (cleaner, keeps them readable). **NOTE for later:** the "show activity outside a resolver" use case the probe gestured at (e.g. previewing deployment-zone options during the opponent's choice) should return as a *presentation beat*, not by reviving TempVisual. Full solution builds; **300 tests green**; headless plays to victory.
- 2026-06-08: **Engine tests for the presentation library + #6 started.** Added `Tests/PresentationRelayerTests.cs` (2 tests): the relayer fans a beat to every player's sink and waits on the clock exactly once; null sinks / empty slots are skipped. Library-side presentation coverage is now solid (ModelID 4, contract/clock 9, beat serialization 4, relayer 2 = **300 total green**). Emission-in-context stays integration-only (state-machine-driven; impractical as a unit test) and app-side `PresentationPlayer` is intentionally untested per user (library tests only). **#6 (retire TempVisual):** commented out the one stage-level TempVisual usage — `ChooseMapSideStage.TempTestVisuals`, a never-called debug probe — with a note (kept, not deleted). **Held the full sweep pending a decision** (see Decisions): the TempVisual system is woven into `IGameContext`/`IFDGGame`/`IPlayerController` as a property and into the `GameContext` ctor + `AssignInterfaces` as parameters, and a parameter can't be "commented out with a note" — it has to be actually removed, a signature change rippling to ~20 files across both repos for zero functional benefit (TempVisual is already entirely dead/dormant). Current changes (relayer test + probe comment) are **uncommitted**.
- 2026-06-08: **App-side presentation built + beat shape finalized.** Engine: `ModelMove` now carries the full waypoint polyline (`[currentPos] + entry.Positions`) and `UnitMovedBeat` carries its `NominalDuration` (get-only override, round-trips) so move pacing can later vary by distance/action — multi-node round-trip + duration covered by the serialization tests. `MovementUtilities` already validates paths per-segment, so "around the corner, not through it" is enforced at commit; the beat just replays the validated polyline. App (`FdgRaylib/Rendering/Presentation/PresentationPlayer.cs`): `IPresentationSink` + a single-lock timeline + retained visual model — `GlideState` walks the polyline with time distributed by segment length (3 segments = one constant-speed move), `DeathState` does red-flash-then-fade; overrides pre-registered at enqueue to avoid a one-frame jump/vanish. `RaylibRenderer` calls `Update(GetFrameTime())`, draws via `GetModelDrawState` (glide pos / flash+fade / hide / authoritative), and **gates the resolver overlay on `IsAnimating`** so prompts wait for the queue to drain. `PresentationPlayer` created in `LobbyScreen.HandleLaunch`, passed as the sink AND forwarded through `OnGameLaunched`→`TransitionToGame`. `LobbyViewModel_Host` injects a `RealtimePresentationClock` (GUI host self-paces; headless/CliApp keeps the default instant clock). Full solution builds; headless plays to victory (no regression); **298 engine tests green**. **Gap:** `PresentationPlayer` is app-side and there's no `FdgRaylib` test project — adding one is folded into #7 (it can't move to the engine without violating the pacing-lives-in-engine / render-lives-in-app split). Visual feel needs a GUI run to eyeball.
- 2026-06-08: **Slice-1 beats defined + emitted inline (engine).** `FDG.Presentation.Beats`: `UnitMovedBeat` (per-model `ModelMove(ModelID, From, To)`) and `ModelDiedBeat` (model/unit ids + name + death `Position`); nominal durations as constants in `PresentationDurations` (UnitMove 600ms, ModelDeath 500ms). Emission: `ExecuteMoveStage.Enter` captures each model's start position *before* `CommitPositions`, then `await Present(UnitMovedBeat)`; `ApplyWoundsStage.RunStage` emits a `ModelDiedBeat` per model that `GetIsDead()` after `DealWounds`. `Tests/PresentationBeatSerializationTests.cs` round-trips both beats through the real bus serializer (`MessageSerializer` + `TypeNameHandling.Auto`) inside a `PresentBeatMessage`, asserting concrete type + payload — validates the network path. Full suite **298 green**; instant clock = zero timing effect, so no existing test regressed. **Deferred:** (a) full emission-in-context integration test → #7 (driving stages standalone is impractical: `StageBinding.Activate` needs a bound transition + real parent); (b) dangerous-terrain deaths (`MovementExecutor.ApplyDangerousTerrainEffects`, sync/void) and out-of-band triggered moves (`MovementExecutor.Commit`/`TryMove`) don't emit beats yet — only the normal player move/combat-death flows do.
- 2026-06-08: **`IPresenter` wired into `IGameContext` end-to-end (engine + app), mirroring the TempVisual path so #6 is a clean swap.** `IGameContext.Presenter` (+ `GameContext` ctor param). Server-side `PresentationRelayer : IPresenter` fans each beat to every player's sink then awaits the clock **once** (single host-side wait = shared tempo, no client backlog). `IPlayerController.PresentationSink` on all three controllers (Local → `_localPlayer.PresentationSink`; Network → new `NetworkedPresentationSink` using `SendCommandToSingleAsync` to target the specific client — cleaner than TempVisual's send-to-all; Computer → null). New `PresentBeatMessage` record; `FDGGame_AsClient` receives it and forwards to its local sink (mirrors the TempVisual receive handlers). `IFDGGame`/`AssignInterfaces` gained an `IPresentationSink? presentationSink` param; `FDGGame_AsLocal`/`AsClient` expose `PresentationSink`. `FDGServer` takes an optional `IPresentationClock? presentationClock = null` → **defaults to `InstantPresentationClock`** so headless/automated/tests stay instant & deterministic; the GUI host will inject a `RealtimePresentationClock` in #5. Updated `TestGameContext` + 4 inline test contexts + 2 app `AssignInterfaces` call sites (sink = null for now; real sink is #5). Full solution builds; **294 tests green**. NOTE: a dedicated relayer-fan-out / replication round-trip test is deferred to the #7 test pass (relayer logic is a trivial loop + one wait, and will be exercised by the #4 emission tests).
- 2026-06-08: **Contract types landed (engine).** New `FDG.Presentation` namespace: `IPresentationClock` (Scale + `Wait`), `PresentationBeat` (abstract: `NominalDuration` + optional `Text`), `IPresenter` (`Present`), `IPresentationSink` (`OnBeat`), `LocalPresenter` (emit-to-sink-then-await-clock), and `RealtimePresentationClock` / `InstantPresentationClock`. Test doubles in `Tests/Doubles/PresentationDoubles.cs` (`FakePresentationClock` records waits, `RecordingPresentationSink`, `TestBeat`). `Tests/PresentationContractTests.cs` — 9 tests green (emit-then-pace, ordering, null-sink still paces, instant/zero/negative-scale edges, text opt-in). Networked presenter + per-player fan-out deferred to the context-wiring step.
- 2026-06-08: **`ModelID` landed (engine).** `readonly record struct ModelID(Guid ID)` mirroring `UnitID`; `IModel.ID` + `ModelData.ID` (fresh GUID in both runtime ctors; `ModelID? id = null` tail param on the `[JsonConstructor]` so it survives JSON/network round-trips). `Tests/ModelIDTests.cs` mirrors `UnitIDTests` (assigned/unique/explicit-id/round-trip) — 4 green. Full suite 285→ green, full solution builds.
- 2026-06-08: Branch `052-presentation-beat-stream` created on both repos off master. Architecture above agreed in design discussion. Next: draft the `Present()` / beat contract as an interface sketch (engine submodule) before any rendering work — it's the seam everything hangs off. No implementation yet.
</content>
</invoke>
