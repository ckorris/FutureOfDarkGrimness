# Local-dev secrets, read ONLY by `wrangler dev` — production uses real Wrangler secrets
# (`npx wrangler secret put ADMIN_TOKEN`). This value is deliberately public and dev-only;
# smoke.sh uses the same default so the reports checks work out of the box.
ADMIN_TOKEN = "dev-admin-token"
