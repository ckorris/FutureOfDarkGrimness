bin/
obj/
*.user
.vs/
.idea/
*.DotSettings.user

# Distributable build output (scripts/build-dist.sh)
dist/

# Runtime ImGui window-layout state
imgui.ini

# #162 field harness artifacts (written to CWD by --field-harness)
FieldHarnessOut/
FdgLab/reports/

# List server (#264) local artifacts
tools/list-server/node_modules/
tools/list-server/.wrangler/
# #226: local secrets, and fetched bug reports - the latter carry other people's player
# names, army lists and full game saves, so they must never land in the repo.
tools/list-server/.dev.vars
tools/list-server/reports/
# #191 step 4: self-play generation output - regenerable data, not source (tens of GB over
# the campaign's generation window).
FdgLab/data/
